# import pandas as pd
# from PyPDF2 import PdfReader, PdfWriter
# from reportlab.pdfgen import canvas
# from reportlab.lib.pagesizes import letter
# from reportlab.pdfbase.ttfonts import TTFont
# from reportlab.pdfbase import pdfmetrics
# from reportlab.lib.colors import HexColor, Color
# import io
# from svglib.svglib import svg2rlg
# from reportlab.graphics import renderPDF
# from reportlab.graphics.shapes import Drawing, Group, Path, String
# import tkinter as tk
# from tkinter import messagebox, simpledialog

# no_of_events = simpledialog.askinteger("Select File", "Select number of events you have in this competition", minvalue=1, maxvalue= 17)
# # this is in (x,y) format values for y will be same
# # want to adjust the position of your svg's?
# # change the x part of it and try to maintain the distance between x2 and x1, x3 and x2 and so on
# # in this the difference is 50 in every x cooardinates
# # last comp was DCC 2024 having 11 events so 11 positions for svg's
# # 276 point is the centre point
# if no_of_events == 1:
#     svg_positions = [ 
#         (276, 735),
#     ]
# elif no_of_events == 2:
#         svg_positions = [ 
#         (226, 735), (326, 735),
#     ]
# elif no_of_events == 3:
#         svg_positions = [ 
#         (196, 735), (276, 735),  (356, 735)
#     ]
# elif no_of_events == 4:
#     svg_positions = [
#     (96, 760), (216, 760), (336, 760),
#     (456, 760),
# ]
# elif no_of_events == 5:
#     svg_positions = [
#     (76, 735), (176, 735), (276, 735), (376, 735),
#     (476, 735),
# ]
# elif no_of_events == 6:
#     svg_positions = [
#    (25, 753) ,(125, 753), (225, 753), (325, 753),
#     (425, 753), (525, 753), 
# ]
# elif no_of_events == 7:
#     pass
# elif no_of_events == 8:
#     pass
# elif no_of_events == 9:
#     pass
# elif no_of_events == 10:
#     pass
# elif no_of_events == 11:
#     svg_positions = [
#     (30, 735), (80, 735), (130, 735),
#     (180, 735), (230, 735), (280, 735),
#     (330, 735), (380, 735), (430, 735),
#     (480, 735), (530, 735),
# ]
# elif no_of_events == 12:
#     pass
# elif no_of_events == 13:
#     pass
# elif no_of_events == 14:
#     pass
# elif no_of_events == 15:
#     pass
# elif no_of_events == 16:
#     pass
# elif no_of_events == 17:
#     pass

# def change_color_to_gray(drawing):
#     light_gray = Color(0.7294117647, 0.6862745098, 0.6274509804)  # Light gray color
#     for element in drawing.contents:
#         if hasattr(element, 'fillColor') and element.fillColor:
#             element.fillColor = light_gray
#         if isinstance(element, Group):
#             change_color_to_gray(element)
#         elif isinstance(element, Path) or isinstance(element, String):
#             element.fillColor = light_gray

# def create_certificate(template_path, name, transparency_values):
#     name = name.upper()
#     reader = PdfReader(template_path)
#     writer = PdfWriter()
#     packet = io.BytesIO()
#     can = canvas.Canvas(packet, pagesize=letter)
#     pdfmetrics.registerFont(TTFont('HammersmithOne', r'.\Hammersmith_One16\HammersmithOne-Regular.ttf'))
#     max_width = letter[0] - 200  # Calculate the width of the text and adjust font size if necessary
#     font_size = 45.3  # Starting font size
#     while can.stringWidth(name, "HammersmithOne", font_size) > max_width and font_size > 10:
#         font_size -= 15

#     can.setFont("HammersmithOne", font_size)
    
#     fill_color = HexColor("#000000") # #595353
#     can.setFillColor(fill_color)
#     can.setStrokeColor(fill_color)

#     # Calculate the width of the text and the starting x position for center alignment
#     text_width = sum(can.stringWidth(char, "HammersmithOne", font_size) for char in name) + (len(name) - 1) * 5  # Add spacing between characters
#     page_width = letter[0]
#     x = (page_width - text_width) / 2  # Centered x position
#     y = 350  # y position

#     # Draw the name on the certificate with character spacing and slight offsets for bold effect
#     char_spacing = 6  # Adjust character spacing as needed
#     offsets = [(-1,0),(1,0),(-0.5, 0), (0.5, 0), (0, -0.5), (0, 0.5),(0,-1),(0,1)]  # Adjust offsets as needed
#     for char in name:
#         for dx, dy in offsets:
#             can.drawString(x + dx, y + dy, char)
#         can.drawString(x, y, char)  # Draw the original text on top
#         x += can.stringWidth(char, "HammersmithOne", font_size) + char_spacing

#     svg_paths = [
#         r'.\events\333.svg',
#         r'.\events\222.svg',
#         r'.\events\555.svg',
#         r'.\events\333bf.svg',
#         r'.\events\pyram.svg',
#         r'.\events\skewb.svg',
#     ]
    

#     # this is in (x,y) format values for y will be same
#     # want to adjust the position of your svg's?
#     # change the x part of it and try to maintain the distance between x2 and x1, x3 and x2 and so on
#     # in this the difference is 50 in every x cooardinates
#     # last comp was DCC 2024 having 11 events so 11 positions for svg's
#     # svg_positions = [
#     #     (30, 735), (80, 735), (130, 735),
#     #     (180, 735), (230, 735), (280, 735),
#     #     (330, 735), (380, 735), (430, 735),
#     #     (480, 735), (530, 735),
#     # ]

#     # Desired size in points (13 mm converted to points)
#     # I know the answer of this 72 / 25.4 is 2.8346456693 and please don't change it. If you will change it the code will not give you proper result 
#     # 13 here is the size of svg's
#     desired_width_in_points = 13 * (72 / 25.4)
#     desired_height_in_points = 13 * (72 / 25.4)

#     for svg_path, (svg_x, svg_y), transparency in zip(svg_paths, svg_positions, transparency_values):
#         drawing = svg2rlg(svg_path)
#         original_width = drawing.width
#         original_height = drawing.height
#         scale_x = desired_width_in_points / original_width
#         scale_y = desired_height_in_points / original_height
#         drawing.scale(scale_x, scale_y)
#         if transparency == 0:
#             change_color_to_gray(drawing)

#         renderPDF.draw(drawing, can, svg_x, svg_y)
    
#     can.save()

#     packet.seek(0)
#     new_pdf = PdfReader(packet)
#     page = reader.pages[0]
#     page.merge_page(new_pdf.pages[0])
    
#     return page

# # Path to the PDF template (1 single pdf templatre)
# template_path = r'./templates_and_suggested_fonts/Template2/template.pdf'

# # Registered participants data in xlsx from WCA with name and event they are participating
# # sort all the participant name in excel file A to Z
# # Let say there are 146 participants in whole comp for first 73 participants in the file give odd numbers and for next 73 give even numbers
# # now sort those numbers from 1 to 146 and if you are printing two certificates on a single page then all the certificates will be sort orderly
# excel_file = 'comp.xlsx'

# # output file
# output_file = 'newcertis.pdf'
# df = pd.read_excel(excel_file)
# combined_writer = PdfWriter()

# for index, row in df.iterrows():
#     name = row['Name']

#     transparency_values = row[1:].tolist() 
#     certificate_page = create_certificate(template_path, name, transparency_values)
#     combined_writer.add_page(certificate_page)

# with open(output_file, "wb") as outputStream:
#     combined_writer.write(outputStream)

# print("Combined certificates created successfully!")

# # import fitz  # PyMuPDF

# # with fitz.open(r'D:\IT Department\Saiyam Jain\Required Files and folders\scd\templates_and_suggested_fonts\Template1\template.pdf') as doc:
# #     page = doc[0]
# #     width_pt = page.mediabox.width
# #     height_pt = page.mediabox.height
# #     print(f"Width: {width_pt} points")
# #     print(f"Height: {height_pt} points")


# import io
# import os
# import pandas as pd
# from PyPDF2 import PdfReader, PdfWriter
# from reportlab.pdfgen import canvas
# from reportlab.pdfbase.ttfonts import TTFont
# from reportlab.pdfbase import pdfmetrics
# from reportlab.lib.colors import HexColor, Color
# from reportlab.graphics import renderPDF
# from reportlab.graphics.shapes import Group, Path, String
# from svglib.svglib import svg2rlg

# import tkinter as tk
# from tkinter import simpledialog, messagebox

# # -----------------------------
# # CONFIG / PATHS
# # -----------------------------
# TEMPLATE_PATH = r'./templates_and_suggested_fonts/Template2/template.pdf'
# EXCEL_FILE   = 'comp.xlsx'
# OUTPUT_FILE  = 'newcertis.pdf'

# # Your event SVGs (extend this list to match your competition)
# svg_paths = [
#     r'.\events\333.svg',
#     r'.\events\222.svg',
#     r'.\events\555.svg',
#     r'.\events\333bf.svg',
#     r'.\events\pyram.svg',
#     r'.\events\skewb.svg',
# ]

# # -----------------------------
# # TK DIALOG FOR # OF EVENTS
# # -----------------------------
# root = tk.Tk()
# root.withdraw()
# try:
#     no_of_events = simpledialog.askinteger(
#         "Select File",
#         "Select number of events you have in this competition",
#         minvalue=1,
#         maxvalue=17
#     )
#     if not no_of_events:
#         raise ValueError("No selection made.")
# except Exception as e:
#     messagebox.showerror("Error", f"Could not read number of events: {e}")
#     root.destroy()
#     raise
# root.destroy()

# # -----------------------------
# # READ TEMPLATE ONCE (BYTES) + PAGE SIZE
# # -----------------------------
# if not os.path.exists(TEMPLATE_PATH):
#     raise FileNotFoundError(f"Template PDF not found at: {TEMPLATE_PATH}")

# with open(TEMPLATE_PATH, 'rb') as _tf:
#     TEMPLATE_BYTES = _tf.read()

# # Get template page size in points
# tmp_reader_for_size = PdfReader(io.BytesIO(TEMPLATE_BYTES))
# base_page = tmp_reader_for_size.pages[0]
# # PyPDF2 mediabox values can be Decimal/Float/String-like – cast to float
# page_width  = float(base_page.mediabox.width)
# page_height = float(base_page.mediabox.height)
# pdfmetrics.registerFont(TTFont('HammersmithOne', r'.\Hammersmith_One16\HammersmithOne-Regular.ttf'))

# # -----------------------------
# # POSITIONS
# # -----------------------------
# def linear_positions(n, y, left_margin=30, right_margin=None):
#     """Evenly space n icons horizontally from left_margin to right_margin at height y."""
#     if right_margin is None:
#         right_margin = page_width - 30
#     if n == 1:
#         return [( (left_margin + right_margin) / 2.0, y )]
#     span = (right_margin - left_margin)
#     step = span / (n - 1)
#     xs = [left_margin + i * step for i in range(n)]
#     return [(round(x, 0), y) for x in xs]

# def get_svg_positions(n):
#     """
#     Uses your exact hand-tuned layouts where provided,
#     else falls back to evenly-spaced positions across the page.
#     y-values mirror your examples; default is 735.
#     """
#     if n == 1:
#         return [(276, 735)]
#     elif n == 2:
#         return [(226, 735), (326, 735)]
#     elif n == 3:
#         return [(196, 735), (276, 735), (356, 735)]
#     elif n == 4:
#         return [(96, 760), (216, 760), (336, 760), (456, 760)]
#     elif n == 5:
#         return [(76, 735), (176, 735), (276, 735), (376, 735), (476, 735)]
#     elif n == 6:
#         return [(25, 753), (125, 753), (225, 753), (325, 753), (425, 753), (525, 753)]
#     elif n == 11:
#         return [(30, 735), (80, 735), (130, 735), (180, 735), (230, 735),
#                 (280, 735), (330, 735), (380, 735), (430, 735), (480, 735), (530, 735)]
#     else:
#         # Generic fallback: evenly spaced, keep your usual y=735
#         return linear_positions(n, y=735, left_margin=30, right_margin=page_width - 30)

# svg_positions = get_svg_positions(no_of_events)

# # -----------------------------
# # COLOR UTILITY
# # -----------------------------
# def change_color_to_gray(drawing):
#     # Light gray color from your exact RGBA triple
#     light_gray = Color(0.7294117647, 0.6862745098, 0.6274509804)

#     def _recurse(el):
#         # If element has fillColor, set it
#         if hasattr(el, 'fillColor') and getattr(el, 'fillColor'):
#             el.fillColor = light_gray
#         # For Path/String, also enforce fill
#         if isinstance(el, (Path, String)):
#             el.fillColor = light_gray
#         # Recurse into groups/drawings
#         if hasattr(el, 'contents') and el.contents:
#             for child in el.contents:
#                 _recurse(child)

#     _recurse(drawing)

# # -----------------------------
# # OVERLAY BUILDER
# # -----------------------------
# def draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, page_w, page_h):
#     """
#     Build an in-memory one-page PDF overlay containing:
#     - The participant's name (centered with character spacing and outline effect)
#     - The event icons (SVGs), colored gray when transparency value == 0
#     """
#     name = str(name).upper()

#     packet = io.BytesIO()
#     can = canvas.Canvas(packet, pagesize=(page_w, page_h))

#     # Fit name width
#     max_width = page_w - 200
#     font_size = 45.3
#     while can.stringWidth(name, "HammersmithOne", font_size) > max_width and font_size > 10:
#         font_size -= 15
#     can.setFont("HammersmithOne", font_size)

#     # Black fill for the text
#     fill_color = HexColor("#000000")
#     can.setFillColor(fill_color)
#     can.setStrokeColor(fill_color)

#     # Center the name with custom char spacing and a bold-ish outline effect
#     char_spacing = 6
#     offsets = [(-1, 0), (1, 0), (-0.5, 0), (0.5, 0), (0, -0.5), (0, 0.5), (0, -1), (0, 1)]
#     text_width = sum(can.stringWidth(ch, "HammersmithOne", font_size) for ch in name) + (len(name) - 1) * (char_spacing - 1)
#     x = (page_w - text_width) / 2
#     y = 350  # keep your original baseline

#     for ch in name:
#         for dx, dy in offsets:
#             can.drawString(x + dx, y + dy, ch)
#         can.drawString(x, y, ch)
#         x += can.stringWidth(ch, "HammersmithOne", font_size) + char_spacing

#     # Draw event SVGs (13 mm square -> points). DO NOT change 72/25.4 factor per your note.
#     desired_w = 13 * (72 / 25.4)
#     desired_h = 13 * (72 / 25.4)

#     # Zip stops at the shortest list, which is safe if svg_paths < positions or vice-versa
#     for svg_path, (svg_x, svg_y), transparency in zip(svg_paths, svg_positions, transparency_values):
#         drawing = svg2rlg(svg_path)
#         if drawing is None:
#             continue  # skip missing/bad svgs gracefully
#         # Scale to desired size
#         scale_x = desired_w / float(drawing.width or desired_w)
#         scale_y = desired_h / float(drawing.height or desired_h)
#         drawing.scale(scale_x, scale_y)
#         # Gray-out if transparency (participation) flag is 0
#         if int(transparency) == 0:
#             change_color_to_gray(drawing)
#         renderPDF.draw(drawing, can, float(svg_x), float(svg_y))

#     can.save()
#     packet.seek(0)
#     return packet  # BytesIO

# # -----------------------------
# # PAGE CREATOR (SELF-CONTAINED)
# # -----------------------------
# def create_certificate_page_bytes(name, transparency_values, svg_paths, svg_positions):
#     """
#     Merge overlay onto a fresh template page and return bytes of a single-page,
#     standalone PDF. This avoids dangling references that Acrobat dislikes.
#     """
#     # Build overlay (in-memory PDF)
#     overlay_stream = draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, page_width, page_height)
#     overlay_reader = PdfReader(overlay_stream)

#     # Fresh template reader from in-memory bytes each time
#     base_reader = PdfReader(io.BytesIO(TEMPLATE_BYTES))
#     page = base_reader.pages[0]
#     page.merge_page(overlay_reader.pages[0])

#     # Materialize to its own, complete one-page PDF
#     out_buf = io.BytesIO()
#     one = PdfWriter()
#     one.add_page(page)
#     one.write(out_buf)
#     out_buf.seek(0)
#     return out_buf.getvalue()

# # -----------------------------
# # MAIN: READ EXCEL, BUILD PAGES
# # -----------------------------
# if not os.path.exists(EXCEL_FILE):
#     raise FileNotFoundError(f"Excel file not found at: {EXCEL_FILE}")

# df = pd.read_excel(EXCEL_FILE)

# combined = PdfWriter()

# for _, row in df.iterrows():
#     name = row['Name']
#     if name == "Nevan Jain":
#         pass
#     transparency_values = row[1:].tolist()  # event flags: 1 = colored, 0 = gray

#     page_bytes = create_certificate_page_bytes(name, transparency_values, svg_paths, svg_positions)
#     page_reader = PdfReader(io.BytesIO(page_bytes))
#     combined.add_page(page_reader.pages[0])

# with open(OUTPUT_FILE, "wb") as out:
#     combined.write(out)

# print("Combined certificates created successfully!")

######################### NEW CODE ###############################

# import io
# import os
# import sys
# import math
# import pandas as pd
# from PyPDF2 import PdfReader, PdfWriter
# from reportlab.pdfgen import canvas
# from reportlab.pdfbase.ttfonts import TTFont
# from reportlab.pdfbase import pdfmetrics
# from reportlab.lib.colors import HexColor, Color
# from reportlab.graphics import renderPDF
# from reportlab.graphics.shapes import Group, Path, String
# from svglib.svglib import svg2rlg

# import tkinter as tk
# from tkinter import simpledialog, messagebox

# # -----------------------------
# # CONFIG / PATHS
# # -----------------------------
# TEMPLATE_PATH = r'./templates_and_suggested_fonts/Template2/template.pdf'
# EXCEL_FILE   = 'comp.xlsx'
# OUTPUT_FILE  = 'newcertis.pdf'
# FONT_PATH    = r'.\Hammersmith_One16\HammersmithOne-Regular.ttf'

# # Your event SVGs (extend this list to match your competition)
# # IMPORTANT: The left-to-right order here is the same as your Excel's event columns.
# svg_paths_master = [
#     r'.\events\333.svg',
#     r'.\events\222.svg',
#     r'.\events\555.svg',
#     r'.\events\333bf.svg',
#     r'.\events\pyram.svg',
#     r'.\events\skewb.svg',
# ]

# # -----------------------------
# # TK DIALOG FOR # OF EVENTS
# # -----------------------------
# root = tk.Tk()
# root.withdraw()
# try:
#     no_of_events = simpledialog.askinteger(
#         "Select # of Events",
#         "Select number of events you have in this competition",
#         minvalue=1,
#         maxvalue=min(17, len(svg_paths_master))
#     )
#     if not no_of_events:
#         raise ValueError("No selection made.")
# except Exception as e:
#     messagebox.showerror("Error", f"Could not read number of events: {e}")
#     root.destroy()
#     raise
# root.destroy()

# # Slice SVGs to the selected count
# svg_paths = svg_paths_master[:no_of_events]

# # -----------------------------
# # READ TEMPLATE ONCE (BYTES) + PAGE SIZE
# # -----------------------------
# if not os.path.exists(TEMPLATE_PATH):
#     raise FileNotFoundError(f"Template PDF not found at: {TEMPLATE_PATH}")
# with open(TEMPLATE_PATH, 'rb') as _tf:
#     TEMPLATE_BYTES = _tf.read()

# tmp_reader_for_size = PdfReader(io.BytesIO(TEMPLATE_BYTES))
# base_page = tmp_reader_for_size.pages[0]
# page_width  = float(base_page.mediabox.width)
# page_height = float(base_page.mediabox.height)

# if not os.path.exists(FONT_PATH):
#     raise FileNotFoundError(f"Font not found at: {FONT_PATH}")
# pdfmetrics.registerFont(TTFont('HammersmithOne', FONT_PATH))

# # -----------------------------
# # POSITIONS
# # -----------------------------
# def linear_positions(n, y, left_margin=30, right_margin=None):
#     """Evenly space n icons horizontally from left_margin to right_margin at height y."""
#     if right_margin is None:
#         right_margin = page_width - 30
#     if n <= 0:
#         return []
#     if n == 1:
#         return [((left_margin + right_margin) / 2.0, y)]
#     span = (right_margin - left_margin)
#     step = span / (n - 1)
#     xs = [left_margin + i * step for i in range(n)]
#     return [(float(round(x, 2)), float(y)) for x in xs]

# def get_svg_positions(n):
#     """
#     Uses your exact hand-tuned layouts where provided,
#     else falls back to evenly-spaced positions across the page (y=735).
#     """
#     if n == 1:
#         return [(276, 735)]
#     elif n == 2:
#         return [(226, 735), (326, 735)]
#     elif n == 3:
#         return [(196, 735), (276, 735), (356, 735)]
#     elif n == 4:
#         return [(96, 760), (216, 760), (336, 760), (456, 760)]
#     elif n == 5:
#         return [(76, 735), (176, 735), (276, 735), (376, 735), (476, 735)]
#     elif n == 6:
#         return [(25, 753), (125, 753), (225, 753), (325, 753), (425, 753), (525, 753)]
#     elif n == 11:
#         return [(30, 735), (80, 735), (130, 735), (180, 735), (230, 735),
#                 (280, 735), (330, 735), (380, 735), (430, 735), (480, 735), (530, 735)]
#     else:
#         return linear_positions(n, y=735, left_margin=30, right_margin=page_width - 30)

# svg_positions = get_svg_positions(no_of_events)

# # -----------------------------
# # COLOR UTILITY
# # -----------------------------
# def change_color_to_gray(drawing):
#     # Light gray color from your exact RGBA triple
#     light_gray = Color(0.7294117647, 0.6862745098, 0.6274509804)

#     def _recurse(el):
#         # If element has fillColor, set it
#         if hasattr(el, 'fillColor') and getattr(el, 'fillColor'):
#             el.fillColor = light_gray
#         # For Path/String, also enforce fill
#         if isinstance(el, (Path, String)):
#             el.fillColor = light_gray
#         # Recurse into groups/drawings
#         if hasattr(el, 'contents') and el.contents:
#             for child in el.contents:
#                 _recurse(child)

#     _recurse(drawing)

# # -----------------------------
# # OVERLAY BUILDER
# # -----------------------------
# def draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, page_w, page_h):
#     """
#     Build an in-memory one-page PDF overlay containing:
#     - The participant's name (centered with character spacing and outline effect)
#     - The event icons (SVGs), colored gray when transparency value == 0
#     """
#     # Normalize/upper the name for consistent rendering
#     name = ("" if pd.isna(name) else str(name)).strip().upper()

#     packet = io.BytesIO()
#     can = canvas.Canvas(packet, pagesize=(page_w, page_h))

#     # Fit name width
#     max_width = page_w - 200
#     font_size = 45.3
#     while can.stringWidth(name, "HammersmithOne", font_size) > max_width and font_size > 10:
#         font_size -= 1.0
#     can.setFont("HammersmithOne", font_size)

#     # Black fill for the text
#     fill_color = HexColor("#000000")
#     can.setFillColor(fill_color)
#     can.setStrokeColor(fill_color)

#     # Center the name with custom char spacing and a bold-ish outline effect
#     char_spacing = 6
#     offsets = [(-1, 0), (1, 0), (-0.5, 0), (0.5, 0), (0, -0.5), (0, 0.5), (0, -1), (0, 1)]
#     # Compute character-by-character placement (more stable for mixed glyph widths)
#     glyph_widths = [can.stringWidth(ch, "HammersmithOne", font_size) for ch in name]
#     text_width = sum(glyph_widths) + max(0, (len(name) - 1)) * (char_spacing - 1)
#     x = (page_w - text_width) / 2.0
#     y = 350  # baseline

#     for ch, w in zip(name, glyph_widths):
#         for dx, dy in offsets:
#             can.drawString(x + dx, y + dy, ch)
#         can.drawString(x, y, ch)
#         x += w + char_spacing

#     # Draw event SVGs (13 mm square -> points). DO NOT change 72/25.4 factor.
#     desired_w = 13 * (72 / 25.4)
#     desired_h = 13 * (72 / 25.4)

#     for idx, (svg_path, (svg_x, svg_y), transparency) in enumerate(zip(svg_paths, svg_positions, transparency_values)):
#         try:
#             if not os.path.exists(svg_path):
#                 # Keep spacing even if SVG is missing
#                 continue
#             drawing = svg2rlg(svg_path)
#             if drawing is None:
#                 continue  # skip missing/bad svgs gracefully
#             # Scale to desired size
#             scale_x = desired_w / float(drawing.width or desired_w)
#             scale_y = desired_h / float(drawing.height or desired_h)
#             drawing.scale(scale_x, scale_y)
#             # Gray-out if transparency (participation) flag is 0
#             tval = 0
#             try:
#                 tval = int(float(transparency)) if (transparency is not None and str(transparency) != "nan") else 0
#             except Exception:
#                 tval = 0
#             if tval == 0:
#                 change_color_to_gray(drawing)
#             renderPDF.draw(drawing, can, float(svg_x), float(svg_y))
#         except Exception as e:
#             # Never let a single bad SVG stop the page
#             print(f"[WARN] SVG render failed (row name={name}, idx={idx}, path={svg_path}): {e}", file=sys.stderr)
#             continue

#     can.save()
#     packet.seek(0)
#     return packet  # BytesIO

# # -----------------------------
# # PAGE CREATOR (SELF-CONTAINED)
# # -----------------------------
# def create_certificate_page_bytes(name, transparency_values, svg_paths, svg_positions):
#     """
#     Merge overlay onto a fresh template page and return bytes of a single-page,
#     standalone PDF. This avoids dangling references that Acrobat dislikes.
#     """
#     # Build overlay (in-memory PDF)
#     overlay_stream = draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, page_width, page_height)
#     overlay_reader = PdfReader(overlay_stream)

#     # Fresh template reader from in-memory bytes each time
#     base_reader = PdfReader(io.BytesIO(TEMPLATE_BYTES))
#     page = base_reader.pages[0]
#     # Merge on a copy-like page object each iteration
#     page.merge_page(overlay_reader.pages[0])

#     # Materialize to its own, complete one-page PDF
#     out_buf = io.BytesIO()
#     one = PdfWriter()
#     one.add_page(page)
#     one.write(out_buf)
#     out_buf.seek(0)
#     return out_buf.getvalue()

# # -----------------------------
# # MAIN: READ EXCEL, BUILD PAGES
# # -----------------------------
# if not os.path.exists(EXCEL_FILE):
#     raise FileNotFoundError(f"Excel file not found at: {EXCEL_FILE}")

# # Read and clean the dataframe
# df = pd.read_excel(EXCEL_FILE)
# # Normalize headers
# df.columns = [str(c).strip() for c in df.columns]
# if 'Name' not in df.columns:
#     # Try common variants
#     for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#         if alt in df.columns:
#             df.rename(columns={alt: 'Name'}, inplace=True)
#             break
# if 'Name' not in df.columns:
#     raise KeyError("Could not find a 'Name' column in the Excel file.")

# # Fix typical Excel issues: merged/blank name cells => ffill, strip whitespace
# df['Name'] = df['Name'].astype(object)
# df['Name'] = df['Name'].ffill()  # forward-fill merged/blank names
# df['Name'] = df['Name'].apply(lambda x: "" if pd.isna(x) else str(x).strip())

# # Drop rows that are fully empty (all NaN/blank) after 'Name' processing
# if df.shape[1] > 1:
#     non_name_cols = [c for c in df.columns if c != 'Name']
#     # If every non-name value is NaN/blank, drop the row
#     def _row_all_blank(s):
#         return all((pd.isna(v) or str(v).strip()=="" ) for v in s)
#     mask_keep = ~df[non_name_cols].apply(_row_all_blank, axis=1)
#     df = df[mask_keep].copy()

# # Build a clean matrix of event flags, matching chosen count
# # Assumes the event columns are everything after 'Name' in your sheet (left-to-right).
# # If your sheet has extra columns at the end, place events contiguously right after 'Name'.
# name_idx = list(df.columns).index('Name')
# event_cols = [c for c in df.columns[name_idx+1: name_idx+1+no_of_events]]

# if len(event_cols) < no_of_events:
#     print(f"[WARN] Only found {len(event_cols)} event columns after 'Name'. "
#           f"Rendering with {len(event_cols)} icons.", file=sys.stderr)
#     svg_paths = svg_paths_master[:len(event_cols)]
#     svg_positions = get_svg_positions(len(svg_paths))

# # Coerce event flags to 0/1 safely
# def _to_flag(x):
#     if pd.isna(x):
#         return 0
#     s = str(x).strip().lower()
#     if s in {"1", "true", "yes", "y", "✓", "check", "x"}:
#         return 1
#     try:
#         return 1 if int(float(s)) != 0 else 0
#     except Exception:
#         return 0

# df_events = df[event_cols].applymap(_to_flag)

# # Final sanity: if duplicate names exist, we still render one page per row
# combined = PdfWriter()

# total = len(df)
# print(f"[INFO] Preparing {total} certificate(s)...")
# rendered_count = 0

# for idx in range(total):
#     row = df.iloc[idx]
#     name = row['Name']
#     # Safety: ensure string for rendering
#     safe_name = "" if pd.isna(name) else str(name).strip()
#     # If somehow name is empty, label as 'PARTICIPANT'
#     if not safe_name:
#         safe_name = "PARTICIPANT"

#     # Event flags for this row (truncate to our svg count)
#     flags = df_events.iloc[idx].tolist()
#     flags = flags[:len(svg_paths)]  # align to icons we will draw

#     try:
#         page_bytes = create_certificate_page_bytes(safe_name, flags, svg_paths, svg_positions)
#         page_reader = PdfReader(io.BytesIO(page_bytes))
#         combined.add_page(page_reader.pages[0])
#         rendered_count += 1
#         print(f"  - [{idx+1}/{total}] {safe_name}")
#     except Exception as e:
#         print(f"[ERROR] Failed for row {idx+1} (name={safe_name}): {e}", file=sys.stderr)
#         continue

# # Write combined PDF
# if rendered_count == 0:
#     raise RuntimeError("No pages were rendered. Check your Excel and paths.")

# with open(OUTPUT_FILE, "wb") as out:
#     combined.write(out)

# print(f"Combined certificates created successfully! Pages written: {rendered_count}/{total} -> {OUTPUT_FILE}")


# import io
# import os
# import sys
# import math
# import warnings
# import pandas as pd

# # PDF / drawing libs
# from reportlab.pdfgen import canvas
# from reportlab.pdfbase.ttfonts import TTFont
# from reportlab.pdfbase import pdfmetrics
# from reportlab.lib.colors import HexColor, Color
# from reportlab.graphics import renderPDF
# from reportlab.graphics.shapes import Group, Path, String

# from svglib.svglib import svg2rlg

# # PDF readers/writers
# try:
#     # modern package name
#     from pypdf import PdfReader, PdfWriter, PdfMerger
# except Exception:
#     # fallback to older name
#     from PyPDF2 import PdfReader, PdfWriter, PdfMerger

# # Tiny UI to ask #events (safe fallback if Tk not available)
# try:
#     import tkinter as tk
#     from tkinter import simpledialog, messagebox
#     TK_AVAILABLE = True
# except Exception:
#     TK_AVAILABLE = False

# # -----------------------------
# # CONFIG / PATHS
# # -----------------------------
# TEMPLATE_PATH = r'./templates_and_suggested_fonts/Template2/template.pdf'
# EXCEL_FILE   = 'comp.xlsx'
# OUTPUT_FILE  = 'newcertis.pdf'
# FONT_PATH    = r'.\Hammersmith_One16\HammersmithOne-Regular.ttf'

# # Your event SVGs (extend this list to match your competition)
# # IMPORTANT: The left-to-right order here is the same as your Excel's event columns.
# svg_paths_master = [
#     r'.\events\333.svg',
#     r'.\events\222.svg',
#     r'.\events\444.svg',
#     r'.\events\333oh.svg',
#     r'.\events\pyram.svg',
#     # add more here if needed (the dialog caps to len(svg_paths_master))
# ]

# # Optional: if you ever want to force unique certificates per unique name only, set True
# DEDUP_BY_NAME = False

# # -----------------------------
# # ASK USER: NUMBER OF EVENTS
# # -----------------------------
# def ask_number_of_events(max_events: int) -> int:
#     """
#     Ask (via Tk) how many events are in this competition.
#     Falls back to max_events if Tk is unavailable or the prompt is canceled.
#     """
#     if max_events <= 0:
#         raise ValueError("No SVG event icons configured in svg_paths_master.")

#     if not TK_AVAILABLE:
#         print("[INFO] Tkinter not available; defaulting to all events:", max_events)
#         return max_events

#     root = tk.Tk()
#     root.withdraw()
#     try:
#         no_of_events = simpledialog.askinteger(
#             "Select # of Events",
#             "Select number of events you have in this competition",
#             minvalue=1,
#             maxvalue=min(17, max_events)
#         )
#         if not no_of_events:
#             raise ValueError("No selection made.")
#     except Exception as e:
#         try:
#             messagebox.showerror("Error", f"Could not read number of events: {e}")
#         except Exception:
#             pass
#         root.destroy()
#         print("[INFO] Falling back to all events:", max_events)
#         return max_events
#     root.destroy()
#     return int(no_of_events)

# # -----------------------------
# # READ TEMPLATE (BYTES) + PAGE SIZE + FONT
# # -----------------------------
# if not os.path.exists(TEMPLATE_PATH):
#     raise FileNotFoundError(f"Template PDF not found at: {TEMPLATE_PATH}")
# with open(TEMPLATE_PATH, 'rb') as _tf:
#     TEMPLATE_BYTES = _tf.read()

# tmp_reader_for_size = PdfReader(io.BytesIO(TEMPLATE_BYTES))
# base_page = tmp_reader_for_size.pages[0]
# page_width  = float(base_page.mediabox.width)
# page_height = float(base_page.mediabox.height)

# if not os.path.exists(FONT_PATH):
#     raise FileNotFoundError(f"Font not found at: {FONT_PATH}")
# pdfmetrics.registerFont(TTFont('HammersmithOne', FONT_PATH))

# # -----------------------------
# # POSITIONS
# # -----------------------------
# def linear_positions(n, y, left_margin=30, right_margin=None):
#     """Evenly space n icons horizontally from left_margin to right_margin at height y."""
#     if right_margin is None:
#         right_margin = page_width - 30
#     if n <= 0:
#         return []
#     if n == 1:
#         return [((left_margin + right_margin) / 2.0, y)]
#     span = (right_margin - left_margin)
#     step = span / (n - 1)
#     xs = [left_margin + i * step for i in range(n)]
#     return [(float(round(x, 2)), float(y)) for x in xs]

# def get_svg_positions(n):
#     """
#     Uses hand-tuned layouts where provided, else falls back to evenly-spaced (y=735).
#     Customize as you like.
#     """
#     if n == 1:
#         return [(276, 735)]
#     elif n == 2:
#         return [(226, 735), (326, 735)]
#     elif n == 3:
#         return [(196, 735), (276, 735), (356, 735)]
#     elif n == 4:
#         return [(96, 760), (216, 760), (336, 760), (456, 760)]
#     elif n == 5:
#         return [(76, 735), (176, 735), (276, 735), (376, 735), (476, 735)]
#     elif n == 6:
#         return [(25, 753), (125, 753), (225, 753), (325, 753), (425, 753), (525, 753)]
#     elif n == 11:
#         return [(30, 735), (80, 735), (130, 735), (180, 735), (230, 735),
#                 (280, 735), (330, 735), (380, 735), (430, 735), (480, 735), (530, 735)]
#     else:
#         return linear_positions(n, y=735, left_margin=30, right_margin=page_width - 30)

# # -----------------------------
# # COLOR UTILITY
# # -----------------------------
# def change_color_to_gray(drawing):
#     # Light gray color from your exact RGBA triple
#     light_gray = Color(0.7294117647, 0.6862745098, 0.6274509804)

#     def _recurse(el):
#         # If element has fillColor, set it
#         if hasattr(el, 'fillColor') and getattr(el, 'fillColor'):
#             el.fillColor = light_gray
#         # For Path/String, also enforce fill
#         if isinstance(el, (Path, String)):
#             el.fillColor = light_gray
#         # Recurse into groups/drawings
#         if hasattr(el, 'contents') and el.contents:
#             for child in el.contents:
#                 _recurse(child)

#     _recurse(drawing)

# # -----------------------------
# # OVERLAY BUILDER
# # -----------------------------
# def draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, page_w, page_h):
#     """
#     Build an in-memory one-page PDF overlay containing:
#     - The participant's name (centered with character spacing and outline effect)
#     - The event icons (SVGs), colored gray when transparency flag == 0
#     """
#     # Normalize/upper the name for consistent rendering
#     name = ("" if pd.isna(name) else str(name)).strip().upper()

#     packet = io.BytesIO()
#     can = canvas.Canvas(packet, pagesize=(page_w, page_h))

#     # Fit name width
#     max_width = page_w - 200
#     font_size = 45.3
#     while can.stringWidth(name, "HammersmithOne", font_size) > max_width and font_size > 10:
#         font_size -= 1.0
#     can.setFont("HammersmithOne", font_size)

#     # Colors
#     fill_color = HexColor("#000000")
#     can.setFillColor(fill_color)
#     can.setStrokeColor(fill_color)

#     # --- draw participant name (single text object; no per-char repeats) ---
#     char_spacing = 6
#     # width incl. char spacing so we can center correctly
#     text_width = can.stringWidth(name, "HammersmithOne", font_size) + max(0, len(name) - 1) * char_spacing
#     x = (page_w - text_width) / 2.0
#     y = 350  # baseline

#     can.saveState()
#     can.setLineWidth(1.2)  # outline thickness; tweak to taste

#     txt = can.beginText()
#     txt.setTextOrigin(x, y)
#     txt.setFont("HammersmithOne", font_size)
#     try:
#         txt.setCharSpace(char_spacing)
#     except Exception:
#         # old reportlab may not have setCharSpace; we'll approximate by manual spacing if needed
#         pass

#     # Fill + stroke (outline) in one go; no glyph duplication in text layer
#     render_mode_set = False
#     try:
#         txt.setTextRenderMode(2)  # 0=fill, 1=stroke, 2=fill+stroke
#         render_mode_set = True
#     except Exception:
#         # Fallback for very old reportlab: inject raw PDF op for "2 Tr"
#         # This sets the text rendering mode in the stream so the following textLine uses fill+stroke.
#         try:
#             can._code.append("2 Tr")
#         except Exception:
#             pass

#     txt.textLine(name)
#     can.drawText(txt)
#     can.restoreState()

#     # Draw event SVGs (13 mm square -> points). DO NOT change 72/25.4 factor.
#     desired_w = 13 * (72 / 25.4)
#     desired_h = 13 * (72 / 25.4)

#     for idx, (svg_path, (svg_x, svg_y), transparency) in enumerate(zip(svg_paths, svg_positions, transparency_values)):
#         try:
#             if not os.path.exists(svg_path):
#                 # Keep spacing even if SVG is missing
#                 continue
#             drawing = svg2rlg(svg_path)
#             if drawing is None:
#                 continue  # skip missing/bad svgs gracefully

#             # Scale to desired size
#             scale_x = desired_w / float(drawing.width or desired_w)
#             scale_y = desired_h / float(drawing.height or desired_h)
#             drawing.scale(scale_x, scale_y)

#             # Gray-out if transparency (participation) flag is 0
#             tval = 0
#             try:
#                 tval = int(float(transparency)) if (transparency is not None and str(transparency) != "nan") else 0
#             except Exception:
#                 tval = 0
#             if tval == 0:
#                 change_color_to_gray(drawing)

#             renderPDF.draw(drawing, can, float(svg_x), float(svg_y))
#         except Exception as e:
#             print(f"[WARN] SVG render failed (row name={name}, idx={idx}, path={svg_path}): {e}", file=sys.stderr)
#             continue

#     can.save()
#     packet.seek(0)
#     return packet  # BytesIO of overlay-only PDF

# # -----------------------------
# # PAGE CREATOR (SELF-CONTAINED)
# # -----------------------------
# def create_certificate_page_bytes(name, transparency_values, svg_paths, svg_positions):
#     """
#     Merge overlay onto a fresh template page and return bytes of a single-page,
#     standalone PDF. This avoids dangling references that Acrobat dislikes.
#     """
#     # Build overlay (in-memory PDF)
#     overlay_stream = draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, page_width, page_height)
#     overlay_reader = PdfReader(overlay_stream)

#     # Fresh template reader from in-memory bytes each time
#     base_reader = PdfReader(io.BytesIO(TEMPLATE_BYTES))
#     page = base_reader.pages[0]
#     # Merge overlay onto the template page
#     page.merge_page(overlay_reader.pages[0])

#     # Materialize to its own, complete one-page PDF
#     out_buf = io.BytesIO()
#     one = PdfWriter()
#     one.add_page(page)
#     one.write(out_buf)
#     out_buf.seek(0)
#     return out_buf.getvalue()

# # -----------------------------
# # MAIN
# # -----------------------------
# def main():
#     # Ask how many events
#     no_of_events = ask_number_of_events(len(svg_paths_master))
#     svg_paths = svg_paths_master[:no_of_events]
#     svg_positions = get_svg_positions(no_of_events)

#     if not os.path.exists(EXCEL_FILE):
#         raise FileNotFoundError(f"Excel file not found at: {EXCEL_FILE}")

#     # Read and clean the dataframe
#     try:
#         df = pd.read_excel(EXCEL_FILE)  # requires openpyxl for .xlsx
#     except Exception as e:
#         raise RuntimeError(
#             f"Failed to read '{EXCEL_FILE}'. If it's .xlsx, ensure 'openpyxl' is installed. Original error: {e}"
#         )

#     # Normalize headers
#     df.columns = [str(c).strip() for c in df.columns]
#     if 'Name' not in df.columns:
#         # Try common variants
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break
#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     # Fix typical Excel issues: merged/blank name cells => ffill, strip whitespace
#     df['Name'] = df['Name'].astype(object)
#     df['Name'] = df['Name'].ffill()  # forward-fill merged/blank names
#     df['Name'] = df['Name'].apply(lambda x: "" if pd.isna(x) else str(x).strip())

#     # Drop rows that are fully empty (all NaN/blank) after 'Name' processing
#     if df.shape[1] > 1:
#         non_name_cols = [c for c in df.columns if c != 'Name']
#         def _row_all_blank(s):
#             return all((pd.isna(v) or str(v).strip() == "") for v in s)
#         mask_keep = ~df[non_name_cols].apply(_row_all_blank, axis=1)
#         df = df[mask_keep].copy()

#     # Optional: keep one per unique name (not required for your case)
#     if DEDUP_BY_NAME:
#         before = len(df)
#         df = df.drop_duplicates(subset=['Name']).reset_index(drop=True)
#         after = len(df)
#         print(f"[INFO] Dedup by name: {before} -> {after}")

#     # Build a clean matrix of event flags, matching chosen count
#     # Assumes the event columns are everything after 'Name' in your sheet (left-to-right).
#     # If your sheet has extra columns at the end, place events contiguously right after 'Name'.
#     name_idx = list(df.columns).index('Name')
#     event_cols = [c for c in df.columns[name_idx+1: name_idx+1+no_of_events]]

#     if len(event_cols) < no_of_events:
#         print(f"[WARN] Only found {len(event_cols)} event columns after 'Name'. "
#               f"Rendering with {len(event_cols)} icons.", file=sys.stderr)
#         # shrink the icons/positions to what we have
#         svg_paths = svg_paths_master[:len(event_cols)]
#         svg_positions = get_svg_positions(len(svg_paths))

#     # Coerce event flags to 0/1 safely
#     def _to_flag(x):
#         if pd.isna(x):
#             return 0
#         s = str(x).strip().lower()
#         if s in {"1", "true", "yes", "y", "✓", "check", "x"}:
#             return 1
#         try:
#             return 1 if int(float(s)) != 0 else 0
#         except Exception:
#             return 0

#     df_events = df[event_cols].applymap(_to_flag)

#     total = len(df)
#     print(f"[INFO] Preparing {total} certificate(s)...")

#     # --- CRITICAL CHANGE: use PdfMerger to append full one-page PDFs ---
#     merger = PdfMerger()
#     rendered_count = 0

#     for idx in range(total):
#         row = df.iloc[idx]
#         safe_name = "" if pd.isna(row['Name']) else str(row['Name']).strip()
#         if not safe_name:
#             safe_name = "PARTICIPANT"

#         # Event flags for this row (truncate to our svg count)
#         flags = df_events.iloc[idx].tolist()
#         flags = flags[:len(svg_paths)]  # align to icons we will draw

#         try:
#             page_bytes = create_certificate_page_bytes(safe_name, flags, svg_paths, svg_positions)
#             # Append the WHOLE 1-page PDF; isolates resources and prevents page duplication
#             merger.append(io.BytesIO(page_bytes))
#             rendered_count += 1
#             print(f"  - [{idx+1}/{total}] {safe_name}")
#         except Exception as e:
#             print(f"[ERROR] Failed for row {idx+1} (name={safe_name}): {e}", file=sys.stderr)
#             continue

#     if rendered_count == 0:
#         merger.close()
#         raise RuntimeError("No pages were rendered. Check your Excel and paths.")

#     with open(OUTPUT_FILE, "wb") as out:
#         merger.write(out)
#     merger.close()

#     print(f"Combined certificates created successfully! Pages written: {rendered_count}/{total} -> {OUTPUT_FILE}")

# if __name__ == "__main__":
#     main()



# import io
# import os
# import sys
# import math
# import pandas as pd

# # PDF / drawing libs
# from reportlab.pdfgen import canvas
# from reportlab.pdfbase.ttfonts import TTFont
# from reportlab.pdfbase import pdfmetrics
# from reportlab.lib.colors import HexColor, Color
# from reportlab.graphics import renderPDF
# from reportlab.graphics.shapes import Path, String
# from svglib.svglib import svg2rlg

# # PDF readers/writers
# try:
#     from pypdf import PdfReader, PdfWriter, PdfMerger
# except Exception:
#     from PyPDF2 import PdfReader, PdfWriter, PdfMerger

# # -----------------------------
# # CONFIG / PATHS
# # -----------------------------
# TEMPLATE_PATH = r'./templates_and_suggested_fonts/Template2/template.pdf'
# EXCEL_FILE    = 'comp.xlsx'
# OUTPUT_FILE   = 'newcertis.pdf'
# FONT_PATH     = r'./Hammersmith_One16/HammersmithOne-Regular.ttf'
# ICON_MM = 13
# ICON_SIZE_PT = ICON_MM * (72 / 25.4)   # same size for every icon
# MAX_ICONS_PER_ROW = 6
# SIDE_MARGIN = 55
# TOP_ROW_Y = 748
# ROW_GAP = 36


# # Folder containing all event SVGs
# SVG_DIR       = r'./events'

# DEDUP_BY_NAME = False

# # -----------------------------
# # READ TEMPLATE + PAGE SIZE + FONT
# # -----------------------------
# if not os.path.exists(TEMPLATE_PATH):
#     raise FileNotFoundError(f"Template PDF not found at: {TEMPLATE_PATH}")

# with open(TEMPLATE_PATH, 'rb') as _tf:
#     TEMPLATE_BYTES = _tf.read()

# tmp_reader_for_size = PdfReader(io.BytesIO(TEMPLATE_BYTES))
# base_page = tmp_reader_for_size.pages[0]
# page_width = float(base_page.mediabox.width)
# page_height = float(base_page.mediabox.height)

# if not os.path.exists(FONT_PATH):
#     raise FileNotFoundError(f"Font not found at: {FONT_PATH}")

# pdfmetrics.registerFont(TTFont('HammersmithOne', FONT_PATH))

# # -----------------------------
# # HELPERS
# # -----------------------------
# def normalize_key(s: str) -> str:
#     """
#     Normalize Excel headers / filenames so matching is robust.
#     Example:
#     '333 OH' -> '333oh'
#     '333_oh' -> '333oh'
#     '333-oh' -> '333oh'
#     """
#     if s is None:
#         return ""
#     s = str(s).strip().lower()
#     for ch in [" ", "_", "-", "."]:
#         s = s.replace(ch, "")
#     return s

# # def linear_positions(n, y, left_margin=40, right_margin=None):
# #     if right_margin is None:
# #         right_margin = page_width - 40

# #     if n <= 0:
# #         return []

# #     if n == 1:
# #         return [((left_margin + right_margin) / 2.0, y)]

# #     span = right_margin - left_margin
# #     step = span / (n - 1)
# #     xs = [left_margin + i * step for i in range(n)]
# #     return [(float(round(x, 2)), float(y)) for x in xs]

# def linear_positions(n, y, left_margin=40, right_margin=None):
#     """
#     Returns CENTER positions, not bottom-left positions.
#     """
#     if right_margin is None:
#         right_margin = page_width - 40

#     if n <= 0:
#         return []

#     if n == 1:
#         return [((left_margin + right_margin) / 2.0, y)]

#     span = right_margin - left_margin
#     step = span / (n - 1)
#     xs = [left_margin + i * step for i in range(n)]
#     return [(float(round(x, 2)), float(y)) for x in xs]

# def distribute_across_rows(n, max_per_row=6):
#     """
#     Return row sizes distributed as evenly as possible.
#     Examples:
#     5  -> [5]
#     8  -> [4, 4]
#     11 -> [6, 5]
#     17 -> [6, 6, 5]
#     """
#     if n <= 0:
#         return []

#     rows = math.ceil(n / max_per_row)
#     base = n // rows
#     extra = n % rows

#     counts = []
#     for i in range(rows):
#         counts.append(base + (1 if i < extra else 0))
#     return counts

# # def get_svg_positions_auto(n):
# #     """
# #     Fully automatic layout based on number of events.
# #     Uses 1 row for small counts, 2 rows for medium, 3 rows for larger counts.
# #     """
# #     if n <= 0:
# #         return []

# #     row_counts = distribute_across_rows(n, max_per_row=6)

# #     # y positions for rows (top to bottom)
# #     if len(row_counts) == 1:
# #         ys = [735]
# #     elif len(row_counts) == 2:
# #         ys = [750, 715]
# #     else:
# #         ys = [760, 725, 690]

# #     positions = []
# #     for row_count, y in zip(row_counts, ys):
# #         positions.extend(
# #             linear_positions(
# #                 row_count,
# #                 y=y,
# #                 left_margin=55,
# #                 right_margin=page_width - 55
# #             )
# #         )
# #     return positions

# def get_svg_positions_auto(n, icon_size_pt=ICON_SIZE_PT):
#     """
#     Automatic centered layout.
#     Every returned point is the CENTER of one icon.
#     """
#     if n <= 0:
#         return []

#     row_counts = distribute_across_rows(n, max_per_row=MAX_ICONS_PER_ROW)

#     positions = []
#     for row_idx, row_count in enumerate(row_counts):
#         y = TOP_ROW_Y - (row_idx * ROW_GAP)

#         # Keep icons inside page margins by accounting for half icon width
#         left = SIDE_MARGIN + icon_size_pt / 2.0
#         right = page_width - SIDE_MARGIN - icon_size_pt / 2.0

#         positions.extend(
#             linear_positions(
#                 row_count,
#                 y=y,
#                 left_margin=left,
#                 right_margin=right
#             )
#         )

#     return positions


# def draw_svg_centered(can, svg_path, center_x, center_y, box_w, box_h, gray_out=False):
#     """
#     Draw SVG centered at (center_x, center_y).
#     Scale is based on ACTUAL visible bounds, not raw drawing.width/height.
#     This makes icons look visually consistent.
#     """
#     if not os.path.exists(svg_path):
#         return

#     drawing = svg2rlg(svg_path)
#     if drawing is None:
#         return

#     # Gray-out first if needed
#     if gray_out:
#         change_color_to_gray(drawing)

#     # Use actual visible bounds
#     x0, y0, x1, y1 = drawing.getBounds()
#     content_w = max(1.0, float(x1 - x0))
#     content_h = max(1.0, float(y1 - y0))

#     # Uniform scaling so every icon fits same target box
#     scale = min(box_w / content_w, box_h / content_h)
#     drawing.scale(scale, scale)

#     # Get scaled bounds after scaling
#     sx0, sy0, sx1, sy1 = drawing.getBounds()

#     # Translate so visible content center lands at requested center
#     draw_x = center_x - (sx0 + sx1) / 2.0
#     draw_y = center_y - (sy0 + sy1) / 2.0

#     renderPDF.draw(drawing, can, float(draw_x), float(draw_y))

# def build_svg_lookup(svg_dir):
#     """
#     Scan SVG_DIR and build:
#     {
#         '333': '/full/path/333.svg',
#         '222': '/full/path/222.svg',
#         '333oh': '/full/path/333oh.svg',
#         ...
#     }
#     """
#     if not os.path.isdir(svg_dir):
#         raise FileNotFoundError(f"SVG folder not found: {svg_dir}")

#     svg_map = {}
#     for root, _, files in os.walk(svg_dir):
#         for file in files:
#             if file.lower().endswith(".svg"):
#                 stem = os.path.splitext(file)[0]
#                 key = normalize_key(stem)
#                 full_path = os.path.join(root, file)
#                 svg_map[key] = full_path
#     return svg_map

# def find_event_columns_and_svgs(df, svg_dir):
#     """
#     Looks at all columns after 'Name' and keeps only those whose header
#     matches an available SVG file.
#     """
#     cols = [str(c).strip() for c in df.columns]
#     df.columns = cols

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     name_idx = list(df.columns).index('Name')
#     candidate_cols = df.columns[name_idx + 1:]

#     svg_lookup = build_svg_lookup(svg_dir)

#     event_cols = []
#     svg_paths = []
#     missing_headers = []

#     for col in candidate_cols:
#         col_clean = str(col).strip()
#         if not col_clean:
#             continue

#         key = normalize_key(col_clean)

#         if key in svg_lookup:
#             event_cols.append(col)
#             svg_paths.append(svg_lookup[key])
#         else:
#             # only note it as missing if the column is not completely blank
#             missing_headers.append(col)

#     if not event_cols:
#         raise RuntimeError(
#             "No event columns were matched with SVG files.\n"
#             f"Check your Excel headers and SVG names in: {svg_dir}"
#         )

#     if missing_headers:
#         print(
#             "[INFO] Ignored columns after 'Name' because no matching SVG was found:",
#             ", ".join(map(str, missing_headers))
#         )

#     return event_cols, svg_paths

# # -----------------------------
# # COLOR UTILITY
# # -----------------------------
# def change_color_to_gray(drawing):
#     light_gray = Color(0.7294117647, 0.6862745098, 0.6274509804)

#     def _recurse(el):
#         if hasattr(el, 'fillColor') and getattr(el, 'fillColor'):
#             el.fillColor = light_gray
#         if isinstance(el, (Path, String)):
#             el.fillColor = light_gray
#         if hasattr(el, 'contents') and el.contents:
#             for child in el.contents:
#                 _recurse(child)

#     _recurse(drawing)

# # -----------------------------
# # OVERLAY BUILDER
# # -----------------------------
# def draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, page_w, page_h):
#     name = ("" if pd.isna(name) else str(name)).strip().upper()

#     packet = io.BytesIO()
#     can = canvas.Canvas(packet, pagesize=(page_w, page_h))

#     # Fit name width
#     max_width = page_w - 200
#     font_size = 45.3
#     while can.stringWidth(name, "HammersmithOne", font_size) > max_width and font_size > 10:
#         font_size -= 1.0

#     can.setFont("HammersmithOne", font_size)

#     fill_color = HexColor("#000000")
#     can.setFillColor(fill_color)
#     can.setStrokeColor(fill_color)

#     # Draw participant name
#     char_spacing = 6
#     text_width = can.stringWidth(name, "HammersmithOne", font_size) + max(0, len(name) - 1) * char_spacing
#     x = (page_w - text_width) / 2.0
#     y = 350

#     can.saveState()
#     can.setLineWidth(1.2)

#     txt = can.beginText()
#     txt.setTextOrigin(x, y)
#     txt.setFont("HammersmithOne", font_size)

#     try:
#         txt.setCharSpace(char_spacing)
#     except Exception:
#         pass

#     try:
#         txt.setTextRenderMode(2)
#     except Exception:
#         try:
#             can._code.append("2 Tr")
#         except Exception:
#             pass

#     txt.textLine(name)
#     can.drawText(txt)
#     can.restoreState()

# #    # Event SVGs
#     # desired_w = 13 * (72 / 25.4)
#     # desired_h = 13 * (72 / 25.4)

#     # for idx, (svg_path, (svg_x, svg_y), transparency) in enumerate(zip(svg_paths, svg_positions, transparency_values)):
#     #     try:
#     #         if not os.path.exists(svg_path):
#     #             continue

#     #         drawing = svg2rlg(svg_path)
#     #         if drawing is None:
#     #             continue

#     #         scale_x = desired_w / float(drawing.width or desired_w)
#     #         scale_y = desired_h / float(drawing.height or desired_h)
#     #         drawing.scale(scale_x, scale_y)

#     #         try:
#     #             tval = int(float(transparency)) if (transparency is not None and str(transparency) != "nan") else 0
#     #         except Exception:
#     #             tval = 0

#     #         if tval == 0:
#     #             change_color_to_gray(drawing)

#     #         renderPDF.draw(drawing, can, float(svg_x), float(svg_y))

#     #     except Exception as e:
#     #         print(f"[WARN] SVG render failed (row name={name}, idx={idx}, path={svg_path}): {e}", file=sys.stderr)
#     #         continue

#     for idx, (svg_path, (svg_cx, svg_cy), transparency) in enumerate(zip(svg_paths, svg_positions, transparency_values)):
#         try:
#             try:
#                 tval = int(float(transparency)) if (transparency is not None and str(transparency) != "nan") else 0
#             except Exception:
#                 tval = 0

#             draw_svg_centered(
#                 can=can,
#                 svg_path=svg_path,
#                 center_x=svg_cx,
#                 center_y=svg_cy,
#                 box_w=ICON_SIZE_PT,
#                 box_h=ICON_SIZE_PT,
#                 gray_out=(tval == 0)
#             )

#         except Exception as e:
#             print(f"[WARN] SVG render failed (row name={name}, idx={idx}, path={svg_path}): {e}", file=sys.stderr)
#             continue

#     can.save()
#     packet.seek(0)
#     return packet

# # -----------------------------
# # PAGE CREATOR
# # -----------------------------
# def create_certificate_page_bytes(name, transparency_values, svg_paths, svg_positions):
#     overlay_stream = draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, page_width, page_height)
#     overlay_reader = PdfReader(overlay_stream)

#     base_reader = PdfReader(io.BytesIO(TEMPLATE_BYTES))
#     page = base_reader.pages[0]
#     page.merge_page(overlay_reader.pages[0])

#     out_buf = io.BytesIO()
#     one = PdfWriter()
#     one.add_page(page)
#     one.write(out_buf)
#     out_buf.seek(0)
#     return out_buf.getvalue()

# # -----------------------------
# # MAIN
# # -----------------------------
# def main():
#     if not os.path.exists(EXCEL_FILE):
#         raise FileNotFoundError(f"Excel file not found at: {EXCEL_FILE}")

#     try:
#         df = pd.read_excel(EXCEL_FILE)
#     except Exception as e:
#         raise RuntimeError(
#             f"Failed to read '{EXCEL_FILE}'. If it's .xlsx, ensure 'openpyxl' is installed. Original error: {e}"
#         )

#     # Normalize headers
#     df.columns = [str(c).strip() for c in df.columns]

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     # Clean names
#     df['Name'] = df['Name'].astype(object)
#     df['Name'] = df['Name'].ffill()
#     df['Name'] = df['Name'].apply(lambda x: "" if pd.isna(x) else str(x).strip())

#     # Drop fully blank rows except Name
#     if df.shape[1] > 1:
#         non_name_cols = [c for c in df.columns if c != 'Name']

#         def _row_all_blank(s):
#             return all((pd.isna(v) or str(v).strip() == "") for v in s)

#         mask_keep = ~df[non_name_cols].apply(_row_all_blank, axis=1)
#         df = df[mask_keep].copy()

#     if DEDUP_BY_NAME:
#         before = len(df)
#         df = df.drop_duplicates(subset=['Name']).reset_index(drop=True)
#         after = len(df)
#         print(f"[INFO] Dedup by name: {before} -> {after}")

#     # --- AUTO DETECT EVENTS + SVGs ---
#     event_cols, svg_paths = find_event_columns_and_svgs(df, SVG_DIR)
#     svg_positions = get_svg_positions_auto(len(svg_paths))

#     print("[INFO] Detected event columns:", event_cols)
#     print("[INFO] Matching SVG files:")
#     for col, path in zip(event_cols, svg_paths):
#         print(f"   {col} -> {path}")

#     # Convert event flags to 0/1
#     def _to_flag(x):
#         if pd.isna(x):
#             return 0
#         s = str(x).strip().lower()
#         if s in {"1", "true", "yes", "y", "✓", "check", "x"}:
#             return 1
#         try:
#             return 1 if int(float(s)) != 0 else 0
#         except Exception:
#             return 0

#     df_events = df[event_cols].applymap(_to_flag)

#     total = len(df)
#     print(f"[INFO] Preparing {total} certificate(s)...")

#     merger = PdfMerger()
#     rendered_count = 0

#     for idx in range(total):
#         row = df.iloc[idx]
#         safe_name = "" if pd.isna(row['Name']) else str(row['Name']).strip()
#         if not safe_name:
#             safe_name = "PARTICIPANT"

#         flags = df_events.iloc[idx].tolist()

#         try:
#             page_bytes = create_certificate_page_bytes(safe_name, flags, svg_paths, svg_positions)
#             merger.append(io.BytesIO(page_bytes))
#             rendered_count += 1
#             print(f"  - [{idx+1}/{total}] {safe_name}")
#         except Exception as e:
#             print(f"[ERROR] Failed for row {idx+1} (name={safe_name}): {e}", file=sys.stderr)
#             continue

#     if rendered_count == 0:
#         merger.close()
#         raise RuntimeError("No pages were rendered. Check your Excel and SVG paths.")

#     with open(OUTPUT_FILE, "wb") as out:
#         merger.write(out)
#     merger.close()

#     print(f"Combined certificates created successfully! Pages written: {rendered_count}/{total} -> {OUTPUT_FILE}")

# if __name__ == "__main__":
#     main()


# import io
# import os
# import sys
# import pandas as pd

# # PDF / drawing libs
# from reportlab.pdfgen import canvas
# from reportlab.pdfbase.ttfonts import TTFont
# from reportlab.pdfbase import pdfmetrics
# from reportlab.lib.colors import HexColor, Color
# from reportlab.graphics import renderPDF
# from reportlab.graphics.shapes import Path, String
# from svglib.svglib import svg2rlg

# # PDF readers/writers
# try:
#     from pypdf import PdfReader, PdfWriter, PdfMerger
# except Exception:
#     from PyPDF2 import PdfReader, PdfWriter, PdfMerger

# # -----------------------------
# # CONFIG / PATHS
# # -----------------------------
# TEMPLATE_PATH = r'./templates_and_suggested_fonts/Template2/template.pdf'
# EXCEL_FILE    = 'comp.xlsx'
# OUTPUT_FILE   = 'newcertis.pdf'
# FONT_PATH     = r'./Hammersmith_One16/HammersmithOne-Regular.ttf'

# SVG_DIR = r'./events'
# DEDUP_BY_NAME = False

# # -----------------------------
# # ICON LAYOUT CONFIG
# # -----------------------------
# ICON_MM = 13
# MAX_ICON_SIZE_PT = ICON_MM * (72 / 25.4)   # preferred maximum icon size

# MIN_ICON_MM = 6
# MIN_ICON_SIZE_PT = MIN_ICON_MM * (72 / 25.4)

# SIDE_MARGIN = 55
# ICON_ROW_Y = 748

# PREFERRED_GAP = 10   # preferred gap between icons
# MIN_GAP = 3          # minimum allowed gap

# # -----------------------------
# # READ TEMPLATE + PAGE SIZE + FONT
# # -----------------------------
# if not os.path.exists(TEMPLATE_PATH):
#     raise FileNotFoundError(f"Template PDF not found at: {TEMPLATE_PATH}")

# with open(TEMPLATE_PATH, 'rb') as _tf:
#     TEMPLATE_BYTES = _tf.read()

# tmp_reader_for_size = PdfReader(io.BytesIO(TEMPLATE_BYTES))
# base_page = tmp_reader_for_size.pages[0]
# page_width = float(base_page.mediabox.width)
# page_height = float(base_page.mediabox.height)

# if not os.path.exists(FONT_PATH):
#     raise FileNotFoundError(f"Font not found at: {FONT_PATH}")

# pdfmetrics.registerFont(TTFont('HammersmithOne', FONT_PATH))

# # -----------------------------
# # HELPERS
# # -----------------------------
# def normalize_key(s: str) -> str:
#     """
#     Normalize Excel headers / filenames so matching is robust.
#     Example:
#     '333 OH' -> '333oh'
#     '333_oh' -> '333oh'
#     '333-oh' -> '333oh'
#     """
#     if s is None:
#         return ""
#     s = str(s).strip().lower()
#     for ch in [" ", "_", "-", "."]:
#         s = s.replace(ch, "")
#     return s


# def get_svg_positions_single_row(n):
#     """
#     Put all SVGs in exactly one row.
#     Icon size and gap are automatically adjusted based on how many icons exist.

#     Returns:
#         positions -> list of (center_x, center_y)
#         icon_size_pt -> computed size to use for each icon
#     """
#     if n <= 0:
#         return [], MAX_ICON_SIZE_PT

#     available_width = page_width - (2 * SIDE_MARGIN)

#     if n == 1:
#         return [(page_width / 2.0, ICON_ROW_Y)], MAX_ICON_SIZE_PT

#     # Try preferred max size with preferred gap
#     icon_size_pt = MAX_ICON_SIZE_PT
#     gap = PREFERRED_GAP

#     total_needed = (n * icon_size_pt) + ((n - 1) * gap)

#     # If too wide, first reduce gap
#     if total_needed > available_width:
#         gap = max(MIN_GAP, (available_width - n * icon_size_pt) / (n - 1))

#     total_needed = (n * icon_size_pt) + ((n - 1) * gap)

#     # If still too wide, reduce icon size
#     if total_needed > available_width:
#         icon_size_pt = (available_width - (n - 1) * gap) / n

#     # Clamp upper bound
#     icon_size_pt = min(icon_size_pt, MAX_ICON_SIZE_PT)

#     # If too small, try minimum icon size
#     if icon_size_pt < MIN_ICON_SIZE_PT:
#         icon_size_pt = MIN_ICON_SIZE_PT
#         gap = (available_width - n * icon_size_pt) / (n - 1)

#         # If even minimum size doesn't fit, force fit with zero gap
#         if gap < 0:
#             gap = 0
#             icon_size_pt = available_width / n

#     total_used_width = n * icon_size_pt + (n - 1) * gap
#     start_x = (page_width - total_used_width) / 2.0 + (icon_size_pt / 2.0)

#     positions = []
#     for i in range(n):
#         cx = start_x + i * (icon_size_pt + gap)
#         positions.append((float(round(cx, 2)), float(ICON_ROW_Y)))

#     return positions, float(icon_size_pt)


# def draw_svg_centered(can, svg_path, center_x, center_y, box_w, box_h, gray_out=False):
#     """
#     Draw SVG centered at (center_x, center_y).
#     Scale is based on ACTUAL visible bounds, not raw drawing.width/height.
#     This makes icons look visually consistent.
#     """
#     if not os.path.exists(svg_path):
#         return

#     drawing = svg2rlg(svg_path)
#     if drawing is None:
#         return

#     # Gray-out first if needed
#     if gray_out:
#         change_color_to_gray(drawing)

#     # Use actual visible bounds
#     x0, y0, x1, y1 = drawing.getBounds()
#     content_w = max(1.0, float(x1 - x0))
#     content_h = max(1.0, float(y1 - y0))

#     # Uniform scaling so every icon fits same target box
#     scale = min(box_w / content_w, box_h / content_h)
#     drawing.scale(scale, scale)

#     # Get scaled bounds after scaling
#     sx0, sy0, sx1, sy1 = drawing.getBounds()

#     # Translate so visible content center lands at requested center
#     draw_x = center_x - (sx0 + sx1) / 2.0
#     draw_y = center_y - (sy0 + sy1) / 2.0

#     renderPDF.draw(drawing, can, float(draw_x), float(draw_y))


# def build_svg_lookup(svg_dir):
#     """
#     Scan SVG_DIR and build:
#     {
#         '333': '/full/path/333.svg',
#         '222': '/full/path/222.svg',
#         '333oh': '/full/path/333oh.svg',
#         ...
#     }
#     """
#     if not os.path.isdir(svg_dir):
#         raise FileNotFoundError(f"SVG folder not found: {svg_dir}")

#     svg_map = {}
#     for root, _, files in os.walk(svg_dir):
#         for file in files:
#             if file.lower().endswith(".svg"):
#                 stem = os.path.splitext(file)[0]
#                 key = normalize_key(stem)
#                 full_path = os.path.join(root, file)
#                 svg_map[key] = full_path
#     return svg_map


# def find_event_columns_and_svgs(df, svg_dir):
#     """
#     Looks at all columns after 'Name' and keeps only those whose header
#     matches an available SVG file.
#     """
#     cols = [str(c).strip() for c in df.columns]
#     df.columns = cols

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     name_idx = list(df.columns).index('Name')
#     candidate_cols = df.columns[name_idx + 1:]

#     svg_lookup = build_svg_lookup(svg_dir)

#     event_cols = []
#     svg_paths = []
#     missing_headers = []

#     for col in candidate_cols:
#         col_clean = str(col).strip()
#         if not col_clean:
#             continue

#         key = normalize_key(col_clean)

#         if key in svg_lookup:
#             event_cols.append(col)
#             svg_paths.append(svg_lookup[key])
#         else:
#             missing_headers.append(col)

#     if not event_cols:
#         raise RuntimeError(
#             "No event columns were matched with SVG files.\n"
#             f"Check your Excel headers and SVG names in: {svg_dir}"
#         )

#     if missing_headers:
#         print(
#             "[INFO] Ignored columns after 'Name' because no matching SVG was found:",
#             ", ".join(map(str, missing_headers))
#         )

#     return event_cols, svg_paths


# # -----------------------------
# # COLOR UTILITY
# # -----------------------------
# def change_color_to_gray(drawing):
#     light_gray = Color(0.7294117647, 0.6862745098, 0.6274509804)

#     def _recurse(el):
#         if hasattr(el, 'fillColor') and getattr(el, 'fillColor'):
#             el.fillColor = light_gray
#         if isinstance(el, (Path, String)):
#             el.fillColor = light_gray
#         if hasattr(el, 'contents') and el.contents:
#             for child in el.contents:
#                 _recurse(child)

#     _recurse(drawing)


# # -----------------------------
# # OVERLAY BUILDER
# # -----------------------------
# def draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, icon_size_pt, page_w, page_h):
#     name = ("" if pd.isna(name) else str(name)).strip().upper()

#     packet = io.BytesIO()
#     can = canvas.Canvas(packet, pagesize=(page_w, page_h))

#     # Fit name width
#     max_width = page_w - 200
#     font_size = 45.3
#     while can.stringWidth(name, "HammersmithOne", font_size) > max_width and font_size > 10:
#         font_size -= 1.0

#     can.setFont("HammersmithOne", font_size)

#     fill_color = HexColor("#000000")
#     can.setFillColor(fill_color)
#     can.setStrokeColor(fill_color)

#     # Draw participant name
#     char_spacing = 6
#     text_width = can.stringWidth(name, "HammersmithOne", font_size) + max(0, len(name) - 1) * char_spacing
#     x = (page_w - text_width) / 2.0
#     y = 350

#     can.saveState()
#     can.setLineWidth(1.2)

#     txt = can.beginText()
#     txt.setTextOrigin(x, y)
#     txt.setFont("HammersmithOne", font_size)

#     try:
#         txt.setCharSpace(char_spacing)
#     except Exception:
#         pass

#     try:
#         txt.setTextRenderMode(2)
#     except Exception:
#         try:
#             can._code.append("2 Tr")
#         except Exception:
#             pass

#     txt.textLine(name)
#     can.drawText(txt)
#     can.restoreState()

#     # Event SVGs - single row, dynamic size
#     for idx, (svg_path, (svg_cx, svg_cy), transparency) in enumerate(zip(svg_paths, svg_positions, transparency_values)):
#         try:
#             try:
#                 tval = int(float(transparency)) if (transparency is not None and str(transparency) != "nan") else 0
#             except Exception:
#                 tval = 0

#             draw_svg_centered(
#                 can=can,
#                 svg_path=svg_path,
#                 center_x=svg_cx,
#                 center_y=svg_cy,
#                 box_w=icon_size_pt,
#                 box_h=icon_size_pt,
#                 gray_out=(tval == 0)
#             )

#         except Exception as e:
#             print(f"[WARN] SVG render failed (row name={name}, idx={idx}, path={svg_path}): {e}", file=sys.stderr)
#             continue

#     can.save()
#     packet.seek(0)
#     return packet


# # -----------------------------
# # PAGE CREATOR
# # -----------------------------
# def create_certificate_page_bytes(name, transparency_values, svg_paths, svg_positions, icon_size_pt):
#     overlay_stream = draw_overlay_pdf(
#         name,
#         transparency_values,
#         svg_paths,
#         svg_positions,
#         icon_size_pt,
#         page_width,
#         page_height
#     )
#     overlay_reader = PdfReader(overlay_stream)

#     base_reader = PdfReader(io.BytesIO(TEMPLATE_BYTES))
#     page = base_reader.pages[0]
#     page.merge_page(overlay_reader.pages[0])

#     out_buf = io.BytesIO()
#     one = PdfWriter()
#     one.add_page(page)
#     one.write(out_buf)
#     out_buf.seek(0)
#     return out_buf.getvalue()


# # -----------------------------
# # MAIN
# # -----------------------------
# def main():
#     if not os.path.exists(EXCEL_FILE):
#         raise FileNotFoundError(f"Excel file not found at: {EXCEL_FILE}")

#     try:
#         df = pd.read_excel(EXCEL_FILE)
#     except Exception as e:
#         raise RuntimeError(
#             f"Failed to read '{EXCEL_FILE}'. If it's .xlsx, ensure 'openpyxl' is installed. Original error: {e}"
#         )

#     # Normalize headers
#     df.columns = [str(c).strip() for c in df.columns]

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     # Clean names
#     df['Name'] = df['Name'].astype(object)
#     df['Name'] = df['Name'].ffill()
#     df['Name'] = df['Name'].apply(lambda x: "" if pd.isna(x) else str(x).strip())

#     # Drop fully blank rows except Name
#     if df.shape[1] > 1:
#         non_name_cols = [c for c in df.columns if c != 'Name']

#         def _row_all_blank(s):
#             return all((pd.isna(v) or str(v).strip() == "") for v in s)

#         mask_keep = ~df[non_name_cols].apply(_row_all_blank, axis=1)
#         df = df[mask_keep].copy()

#     if DEDUP_BY_NAME:
#         before = len(df)
#         df = df.drop_duplicates(subset=['Name']).reset_index(drop=True)
#         after = len(df)
#         print(f"[INFO] Dedup by name: {before} -> {after}")

#     # --- AUTO DETECT EVENTS + SVGs ---
#     event_cols, svg_paths = find_event_columns_and_svgs(df, SVG_DIR)
#     svg_positions, icon_size_pt = get_svg_positions_single_row(len(svg_paths))

#     print("[INFO] Detected event columns:", event_cols)
#     print("[INFO] Matching SVG files:")
#     for col, path in zip(event_cols, svg_paths):
#         print(f"   {col} -> {path}")

#     print(f"[INFO] Computed icon size: {round(icon_size_pt, 2)} pt")
#     print(f"[INFO] Total events in one row: {len(svg_paths)}")

#     # Convert event flags to 0/1
#     def _to_flag(x):
#         if pd.isna(x):
#             return 0
#         s = str(x).strip().lower()
#         if s in {"1", "true", "yes", "y", "✓", "check", "x"}:
#             return 1
#         try:
#             return 1 if int(float(s)) != 0 else 0
#         except Exception:
#             return 0

#     df_events = df[event_cols].applymap(_to_flag)

#     total = len(df)
#     print(f"[INFO] Preparing {total} certificate(s)...")

#     merger = PdfMerger()
#     rendered_count = 0

#     for idx in range(total):
#         row = df.iloc[idx]
#         safe_name = "" if pd.isna(row['Name']) else str(row['Name']).strip()
#         if not safe_name:
#             safe_name = "PARTICIPANT"

#         flags = df_events.iloc[idx].tolist()

#         try:
#             page_bytes = create_certificate_page_bytes(
#                 safe_name,
#                 flags,
#                 svg_paths,
#                 svg_positions,
#                 icon_size_pt
#             )
#             merger.append(io.BytesIO(page_bytes))
#             rendered_count += 1
#             print(f"  - [{idx+1}/{total}] {safe_name}")
#         except Exception as e:
#             print(f"[ERROR] Failed for row {idx+1} (name={safe_name}): {e}", file=sys.stderr)
#             continue

#     if rendered_count == 0:
#         merger.close()
#         raise RuntimeError("No pages were rendered. Check your Excel and SVG paths.")

#     with open(OUTPUT_FILE, "wb") as out:
#         merger.write(out)
#     merger.close()

#     print(f"Combined certificates created successfully! Pages written: {rendered_count}/{total} -> {OUTPUT_FILE}")


# if __name__ == "__main__":
#     main()



# import io
# import os
# import sys
# import pandas as pd

# # PDF / drawing libs
# from reportlab.pdfgen import canvas
# from reportlab.pdfbase.ttfonts import TTFont
# from reportlab.pdfbase import pdfmetrics
# from reportlab.lib.colors import HexColor, Color
# from reportlab.graphics import renderPDF
# from reportlab.graphics.shapes import Path, String
# from svglib.svglib import svg2rlg

# # PDF readers/writers
# try:
#     from pypdf import PdfReader, PdfWriter, PdfMerger
# except Exception:
#     from PyPDF2 import PdfReader, PdfWriter, PdfMerger

# # -----------------------------
# # CONFIG / PATHS
# # -----------------------------
# TEMPLATE_PATH = r'./templates_and_suggested_fonts/Template2/template.pdf'
# EXCEL_FILE = 'comp.xlsx'
# OUTPUT_FILE = 'newcertis.pdf'
# FONT_PATH = r'./Hammersmith_One16/HammersmithOne-Regular.ttf'

# SVG_DIR = r'./events'
# DEDUP_BY_NAME = False

# # -----------------------------
# # ICON LAYOUT CONFIG
# # -----------------------------
# ICON_MM = 13
# MAX_ICON_SIZE_PT = ICON_MM * (72 / 25.4)

# MIN_ICON_MM = 4.0
# MIN_ICON_SIZE_PT = MIN_ICON_MM * (72 / 25.4)

# SIDE_MARGIN = 36
# BASE_ICON_ROW_Y = 748

# PREFERRED_GAP = 6
# MIN_GAP = 1.5
# MAX_GAP = 10

# # Use only a portion of the available row width
# # so the layout looks visually cleaner
# ROW_USAGE_RATIO = 0.84

# # SVGs are drawn slightly smaller than their computed box
# # so they do not feel oversized
# INNER_PAD_RATIO = 0.86

# # -----------------------------
# # READ TEMPLATE + PAGE SIZE + FONT
# # -----------------------------
# if not os.path.exists(TEMPLATE_PATH):
#     raise FileNotFoundError(f"Template PDF not found at: {TEMPLATE_PATH}")

# with open(TEMPLATE_PATH, 'rb') as _tf:
#     TEMPLATE_BYTES = _tf.read()

# tmp_reader_for_size = PdfReader(io.BytesIO(TEMPLATE_BYTES))
# base_page = tmp_reader_for_size.pages[0]
# page_width = float(base_page.mediabox.width)
# page_height = float(base_page.mediabox.height)

# if not os.path.exists(FONT_PATH):
#     raise FileNotFoundError(f"Font not found at: {FONT_PATH}")

# pdfmetrics.registerFont(TTFont('HammersmithOne', FONT_PATH))

# # -----------------------------
# # HELPERS
# # -----------------------------
# def normalize_key(s: str) -> str:
#     """
#     Normalize Excel headers / filenames so matching is robust.
#     Example:
#     '333 OH' -> '333oh'
#     '333_oh' -> '333oh'
#     '333-oh' -> '333oh'
#     """
#     if s is None:
#         return ""
#     s = str(s).strip().lower()
#     for ch in [" ", "_", "-", "."]:
#         s = s.replace(ch, "")
#     return s


# def get_svg_positions_single_row(n):
#     """
#     Cleaner one-row layout:
#     - all icons stay in one line
#     - icon size shrinks dynamically as count grows
#     - gap is computed dynamically
#     - row width intentionally uses only part of the page for better aesthetics

#     Returns:
#         positions -> list[(cx, cy)]
#         icon_size_pt -> float
#         gap -> float
#         row_y -> float
#     """
#     if n <= 0:
#         return [], MAX_ICON_SIZE_PT, PREFERRED_GAP, BASE_ICON_ROW_Y

#     if n == 1:
#         return [(page_width / 2.0, BASE_ICON_ROW_Y)], MAX_ICON_SIZE_PT, 0.0, BASE_ICON_ROW_Y

#     full_available_width = page_width - (2 * SIDE_MARGIN)
#     target_row_width = full_available_width * ROW_USAGE_RATIO

#     # First choose a visually nice icon size based on number of events
#     if n <= 6:
#         icon_size_pt = MAX_ICON_SIZE_PT
#     elif n <= 8:
#         icon_size_pt = MAX_ICON_SIZE_PT * 0.92
#     elif n <= 10:
#         icon_size_pt = MAX_ICON_SIZE_PT * 0.84
#     elif n <= 12:
#         icon_size_pt = MAX_ICON_SIZE_PT * 0.76
#     elif n <= 14:
#         icon_size_pt = MAX_ICON_SIZE_PT * 0.68
#     elif n <= 16:
#         icon_size_pt = MAX_ICON_SIZE_PT * 0.60
#     else:
#         # fallback for very large counts
#         icon_size_pt = target_row_width / (n * 1.18)

#     icon_size_pt = max(MIN_ICON_SIZE_PT, min(icon_size_pt, MAX_ICON_SIZE_PT))

#     used_by_icons = n * icon_size_pt
#     remaining = target_row_width - used_by_icons

#     if remaining > 0:
#         gap = remaining / (n - 1)
#     else:
#         gap = MIN_GAP

#     gap = max(MIN_GAP, min(gap, MAX_GAP))

#     total_width = n * icon_size_pt + (n - 1) * gap

#     # If too wide for the actual page width, shrink icons precisely
#     if total_width > full_available_width:
#         icon_size_pt = (full_available_width - (n - 1) * gap) / n
#         icon_size_pt = max(MIN_ICON_SIZE_PT, icon_size_pt)
#         total_width = n * icon_size_pt + (n - 1) * gap

#     # If still too wide because min size is enforced, reduce gap further
#     if total_width > full_available_width:
#         gap = max(0.0, (full_available_width - n * icon_size_pt) / (n - 1))
#         total_width = n * icon_size_pt + (n - 1) * gap

#     # Small vertical adjustment so different icon sizes look balanced
#     row_y = BASE_ICON_ROW_Y - ((icon_size_pt - MIN_ICON_SIZE_PT) * 0.18)

#     start_x = (page_width - total_width) / 2.0 + (icon_size_pt / 2.0)

#     positions = []
#     for i in range(n):
#         cx = start_x + i * (icon_size_pt + gap)
#         positions.append((float(round(cx, 2)), float(round(row_y, 2))))

#     return positions, float(icon_size_pt), float(gap), float(row_y)


# def draw_svg_centered(can, svg_path, center_x, center_y, box_w, box_h, gray_out=False):
#     """
#     Draw SVG centered at (center_x, center_y).
#     Scale is based on actual visible bounds, not raw width/height.
#     Extra inner padding is used so icons look cleaner and less oversized.
#     """
#     if not os.path.exists(svg_path):
#         return

#     drawing = svg2rlg(svg_path)
#     if drawing is None:
#         return

#     if gray_out:
#         change_color_to_gray(drawing)

#     x0, y0, x1, y1 = drawing.getBounds()
#     content_w = max(1.0, float(x1 - x0))
#     content_h = max(1.0, float(y1 - y0))

#     scale = min(
#         (box_w * INNER_PAD_RATIO) / content_w,
#         (box_h * INNER_PAD_RATIO) / content_h
#     )
#     drawing.scale(scale, scale)

#     sx0, sy0, sx1, sy1 = drawing.getBounds()

#     draw_x = center_x - (sx0 + sx1) / 2.0
#     draw_y = center_y - (sy0 + sy1) / 2.0

#     renderPDF.draw(drawing, can, float(draw_x), float(draw_y))


# def build_svg_lookup(svg_dir):
#     """
#     Scan SVG_DIR and build:
#     {
#         '333': '/full/path/333.svg',
#         '222': '/full/path/222.svg',
#         '333oh': '/full/path/333oh.svg',
#         ...
#     }
#     """
#     if not os.path.isdir(svg_dir):
#         raise FileNotFoundError(f"SVG folder not found: {svg_dir}")

#     svg_map = {}
#     for root, _, files in os.walk(svg_dir):
#         for file in files:
#             if file.lower().endswith(".svg"):
#                 stem = os.path.splitext(file)[0]
#                 key = normalize_key(stem)
#                 full_path = os.path.join(root, file)
#                 svg_map[key] = full_path
#     return svg_map


# def find_event_columns_and_svgs(df, svg_dir):
#     """
#     Looks at all columns after 'Name' and keeps only those whose header
#     matches an available SVG file.
#     """
#     cols = [str(c).strip() for c in df.columns]
#     df.columns = cols

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     name_idx = list(df.columns).index('Name')
#     candidate_cols = df.columns[name_idx + 1:]

#     svg_lookup = build_svg_lookup(svg_dir)

#     event_cols = []
#     svg_paths = []
#     missing_headers = []

#     for col in candidate_cols:
#         col_clean = str(col).strip()
#         if not col_clean:
#             continue

#         key = normalize_key(col_clean)

#         if key in svg_lookup:
#             event_cols.append(col)
#             svg_paths.append(svg_lookup[key])
#         else:
#             missing_headers.append(col)

#     if not event_cols:
#         raise RuntimeError(
#             "No event columns were matched with SVG files.\n"
#             f"Check your Excel headers and SVG names in: {svg_dir}"
#         )

#     if missing_headers:
#         print(
#             "[INFO] Ignored columns after 'Name' because no matching SVG was found:",
#             ", ".join(map(str, missing_headers))
#         )

#     return event_cols, svg_paths


# # -----------------------------
# # COLOR UTILITY
# # -----------------------------
# def change_color_to_gray(drawing):
#     light_gray = Color(0.7294117647, 0.6862745098, 0.6274509804)

#     def _recurse(el):
#         if hasattr(el, 'fillColor') and getattr(el, 'fillColor'):
#             el.fillColor = light_gray
#         if isinstance(el, (Path, String)):
#             el.fillColor = light_gray
#         if hasattr(el, 'contents') and el.contents:
#             for child in el.contents:
#                 _recurse(child)

#     _recurse(drawing)


# # -----------------------------
# # OVERLAY BUILDER
# # -----------------------------
# def draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, icon_size_pt, page_w, page_h):
#     name = ("" if pd.isna(name) else str(name)).strip().upper()

#     packet = io.BytesIO()
#     can = canvas.Canvas(packet, pagesize=(page_w, page_h))

#     # Fit name width
#     max_width = page_w - 200
#     font_size = 45.3
#     while can.stringWidth(name, "HammersmithOne", font_size) > max_width and font_size > 10:
#         font_size -= 1.0

#     can.setFont("HammersmithOne", font_size)

#     fill_color = HexColor("#000000")
#     can.setFillColor(fill_color)
#     can.setStrokeColor(fill_color)

#     # Draw participant name
#     char_spacing = 6
#     text_width = can.stringWidth(name, "HammersmithOne", font_size) + max(0, len(name) - 1) * char_spacing
#     x = (page_w - text_width) / 2.0
#     y = 350

#     can.saveState()
#     can.setLineWidth(1.2)

#     txt = can.beginText()
#     txt.setTextOrigin(x, y)
#     txt.setFont("HammersmithOne", font_size)

#     try:
#         txt.setCharSpace(char_spacing)
#     except Exception:
#         pass

#     try:
#         txt.setTextRenderMode(2)
#     except Exception:
#         try:
#             can._code.append("2 Tr")
#         except Exception:
#             pass

#     txt.textLine(name)
#     can.drawText(txt)
#     can.restoreState()

#     # Event SVGs - single row, dynamic size
#     for idx, (svg_path, (svg_cx, svg_cy), transparency) in enumerate(
#         zip(svg_paths, svg_positions, transparency_values)
#     ):
#         try:
#             try:
#                 tval = int(float(transparency)) if (transparency is not None and str(transparency) != "nan") else 0
#             except Exception:
#                 tval = 0

#             draw_svg_centered(
#                 can=can,
#                 svg_path=svg_path,
#                 center_x=svg_cx,
#                 center_y=svg_cy,
#                 box_w=icon_size_pt,
#                 box_h=icon_size_pt,
#                 gray_out=(tval == 0)
#             )

#         except Exception as e:
#             print(f"[WARN] SVG render failed (row name={name}, idx={idx}, path={svg_path}): {e}", file=sys.stderr)
#             continue

#     can.save()
#     packet.seek(0)
#     return packet


# # -----------------------------
# # PAGE CREATOR
# # -----------------------------
# def create_certificate_page_bytes(name, transparency_values, svg_paths, svg_positions, icon_size_pt):
#     overlay_stream = draw_overlay_pdf(
#         name,
#         transparency_values,
#         svg_paths,
#         svg_positions,
#         icon_size_pt,
#         page_width,
#         page_height
#     )
#     overlay_reader = PdfReader(overlay_stream)

#     base_reader = PdfReader(io.BytesIO(TEMPLATE_BYTES))
#     page = base_reader.pages[0]
#     page.merge_page(overlay_reader.pages[0])

#     out_buf = io.BytesIO()
#     one = PdfWriter()
#     one.add_page(page)
#     one.write(out_buf)
#     out_buf.seek(0)
#     return out_buf.getvalue()


# # -----------------------------
# # MAIN
# # -----------------------------
# def main():
#     if not os.path.exists(EXCEL_FILE):
#         raise FileNotFoundError(f"Excel file not found at: {EXCEL_FILE}")

#     try:
#         df = pd.read_excel(EXCEL_FILE)
#     except Exception as e:
#         raise RuntimeError(
#             f"Failed to read '{EXCEL_FILE}'. If it's .xlsx, ensure 'openpyxl' is installed. Original error: {e}"
#         )

#     # Normalize headers
#     df.columns = [str(c).strip() for c in df.columns]

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     # Clean names
#     df['Name'] = df['Name'].astype(object)
#     df['Name'] = df['Name'].ffill()
#     df['Name'] = df['Name'].apply(lambda x: "" if pd.isna(x) else str(x).strip())

#     # Drop fully blank rows except Name
#     if df.shape[1] > 1:
#         non_name_cols = [c for c in df.columns if c != 'Name']

#         def _row_all_blank(s):
#             return all((pd.isna(v) or str(v).strip() == "") for v in s)

#         mask_keep = ~df[non_name_cols].apply(_row_all_blank, axis=1)
#         df = df[mask_keep].copy()

#     if DEDUP_BY_NAME:
#         before = len(df)
#         df = df.drop_duplicates(subset=['Name']).reset_index(drop=True)
#         after = len(df)
#         print(f"[INFO] Dedup by name: {before} -> {after}")

#     # Detect event columns + SVGs
#     event_cols, svg_paths = find_event_columns_and_svgs(df, SVG_DIR)
#     svg_positions, icon_size_pt, gap_pt, row_y = get_svg_positions_single_row(len(svg_paths))

#     print("[INFO] Detected event columns:", event_cols)
#     print("[INFO] Matching SVG files:")
#     for col, path in zip(event_cols, svg_paths):
#         print(f"   {col} -> {path}")

#     print(f"[INFO] Computed icon size: {round(icon_size_pt, 2)} pt")
#     print(f"[INFO] Computed icon gap: {round(gap_pt, 2)} pt")
#     print(f"[INFO] Computed row Y: {round(row_y, 2)}")
#     print(f"[INFO] Total events in one row: {len(svg_paths)}")

#     # Convert event flags to 0/1
#     def _to_flag(x):
#         if pd.isna(x):
#             return 0
#         s = str(x).strip().lower()
#         if s in {"1", "true", "yes", "y", "✓", "check", "x"}:
#             return 1
#         try:
#             return 1 if int(float(s)) != 0 else 0
#         except Exception:
#             return 0

#     df_events = df[event_cols].applymap(_to_flag)

#     total = len(df)
#     print(f"[INFO] Preparing {total} certificate(s)...")

#     merger = PdfMerger()
#     rendered_count = 0

#     for idx in range(total):
#         row = df.iloc[idx]
#         safe_name = "" if pd.isna(row['Name']) else str(row['Name']).strip()
#         if not safe_name:
#             safe_name = "PARTICIPANT"

#         flags = df_events.iloc[idx].tolist()

#         try:
#             page_bytes = create_certificate_page_bytes(
#                 safe_name,
#                 flags,
#                 svg_paths,
#                 svg_positions,
#                 icon_size_pt
#             )
#             merger.append(io.BytesIO(page_bytes))
#             rendered_count += 1
#             print(f"  - [{idx + 1}/{total}] {safe_name}")
#         except Exception as e:
#             print(f"[ERROR] Failed for row {idx + 1} (name={safe_name}): {e}", file=sys.stderr)
#             continue

#     if rendered_count == 0:
#         merger.close()
#         raise RuntimeError("No pages were rendered. Check your Excel and SVG paths.")

#     with open(OUTPUT_FILE, "wb") as out:
#         merger.write(out)
#     merger.close()

#     print(f"Combined certificates created successfully! Pages written: {rendered_count}/{total} -> {OUTPUT_FILE}")


# if __name__ == "__main__":
#     main()



# import io
# import os
# import sys
# import pandas as pd

# # PDF / drawing libs
# from reportlab.pdfgen import canvas
# from reportlab.pdfbase.ttfonts import TTFont
# from reportlab.pdfbase import pdfmetrics
# from reportlab.lib.colors import HexColor, Color
# from reportlab.graphics import renderPDF
# from reportlab.graphics.shapes import Path, String
# from svglib.svglib import svg2rlg

# # PDF readers/writers
# try:
#     from pypdf import PdfReader, PdfWriter, PdfMerger
# except Exception:
#     from PyPDF2 import PdfReader, PdfWriter, PdfMerger

# # -----------------------------
# # CONFIG / PATHS
# # -----------------------------
# TEMPLATE_PATH = r'./templates_and_suggested_fonts/Template2/template.pdf'
# EXCEL_FILE = 'comp.xlsx'
# OUTPUT_FILE = 'newcertis.pdf'
# FONT_PATH = r'./Hammersmith_One16/HammersmithOne-Regular.ttf'

# SVG_DIR = r'./events'
# DEDUP_BY_NAME = False

# # -----------------------------
# # ICON LAYOUT CONFIG
# # -----------------------------
# ICON_MM = 13
# MAX_ICON_SIZE_PT = ICON_MM * (72 / 25.4)

# MIN_ICON_MM = 4.8
# MIN_ICON_SIZE_PT = MIN_ICON_MM * (72 / 25.4)

# SIDE_MARGIN = 28
# BASE_ICON_ROW_Y = 748

# # How much smaller to draw inside the allocated icon box
# INNER_PAD_RATIO = 0.86

# # Gap safety bounds
# MIN_GAP_PT = 1.0
# MAX_GAP_PT = 12.0

# # -----------------------------
# # READ TEMPLATE + PAGE SIZE + FONT
# # -----------------------------
# if not os.path.exists(TEMPLATE_PATH):
#     raise FileNotFoundError(f"Template PDF not found at: {TEMPLATE_PATH}")

# with open(TEMPLATE_PATH, 'rb') as _tf:
#     TEMPLATE_BYTES = _tf.read()

# tmp_reader_for_size = PdfReader(io.BytesIO(TEMPLATE_BYTES))
# base_page = tmp_reader_for_size.pages[0]
# page_width = float(base_page.mediabox.width)
# page_height = float(base_page.mediabox.height)

# if not os.path.exists(FONT_PATH):
#     raise FileNotFoundError(f"Font not found at: {FONT_PATH}")

# pdfmetrics.registerFont(TTFont('HammersmithOne', FONT_PATH))

# # -----------------------------
# # HELPERS
# # -----------------------------
# def normalize_key(s: str) -> str:
#     """
#     Normalize Excel headers / filenames so matching is robust.
#     Example:
#     '333 OH' -> '333oh'
#     '333_oh' -> '333oh'
#     '333-oh' -> '333oh'
#     """
#     if s is None:
#         return ""
#     s = str(s).strip().lower()
#     for ch in [" ", "_", "-", "."]:
#         s = s.replace(ch, "")
#     return s


# def get_svg_positions_single_row(n):
#     """
#     Dynamic single-row layout using slot-based spacing.

#     Returns:
#         positions, icon_size_pt, gap_pt, row_y
#     """
#     if n <= 0:
#         return [], MAX_ICON_SIZE_PT, 0.0, BASE_ICON_ROW_Y

#     if n == 1:
#         return [(page_width / 2.0, BASE_ICON_ROW_Y)], MAX_ICON_SIZE_PT, 0.0, BASE_ICON_ROW_Y

#     full_available_width = page_width - (2 * SIDE_MARGIN)

#     # Use less width for fewer events, more width for many events
#     if n <= 3:
#         row_usage_ratio = 0.45
#     elif n <= 5:
#         row_usage_ratio = 0.58
#     elif n <= 7:
#         row_usage_ratio = 0.70
#     elif n <= 9:
#         row_usage_ratio = 0.80
#     elif n <= 11:
#         row_usage_ratio = 0.86
#     elif n <= 13:
#         row_usage_ratio = 0.90
#     elif n <= 16:
#         row_usage_ratio = 0.94
#     else:
#         row_usage_ratio = 0.98

#     target_row_width = full_available_width * row_usage_ratio

#     # Slot width per icon
#     slot_w = target_row_width / n

#     # Gap is a percentage of slot width
#     gap_pt = slot_w * 0.22
#     gap_pt = max(MIN_GAP_PT, min(gap_pt, MAX_GAP_PT))

#     # Icon box is the rest of the slot
#     icon_size_pt = slot_w - gap_pt
#     icon_size_pt = min(icon_size_pt, MAX_ICON_SIZE_PT)

#     # If too small, use the full available width and recompute
#     if icon_size_pt < MIN_ICON_SIZE_PT:
#         target_row_width = full_available_width
#         slot_w = target_row_width / n
#         gap_pt = max(0.5, min(6.0, slot_w * 0.10))
#         icon_size_pt = slot_w - gap_pt

#     # Final lower bound
#     icon_size_pt = max(icon_size_pt, min(MIN_ICON_SIZE_PT, MAX_ICON_SIZE_PT))

#     # Final safety: ensure everything fits
#     total_width = n * icon_size_pt + (n - 1) * gap_pt
#     if total_width > full_available_width:
#         gap_pt = max(0.0, (full_available_width - n * icon_size_pt) / (n - 1))
#         total_width = n * icon_size_pt + (n - 1) * gap_pt

#     # If still overflowing, shrink icon size slightly
#     if total_width > full_available_width:
#         icon_size_pt = (full_available_width - (n - 1) * gap_pt) / n
#         total_width = n * icon_size_pt + (n - 1) * gap_pt

#     # Small vertical adjustment for visual balance
#     row_y = BASE_ICON_ROW_Y - max(0.0, (icon_size_pt - MIN_ICON_SIZE_PT) * 0.10)

#     start_x = (page_width - total_width) / 2.0 + (icon_size_pt / 2.0)

#     positions = []
#     for i in range(n):
#         cx = start_x + i * (icon_size_pt + gap_pt)
#         positions.append((float(round(cx, 2)), float(round(row_y, 2))))

#     return positions, float(icon_size_pt), float(gap_pt), float(row_y)


# def draw_svg_centered(can, svg_path, center_x, center_y, box_w, box_h, gray_out=False):
#     """
#     Draw SVG centered at (center_x, center_y) without mutating the SVG geometry.
#     This is safer than drawing.scale(...) for many tightly packed icons.
#     """
#     if not os.path.exists(svg_path):
#         return

#     drawing = svg2rlg(svg_path)
#     if drawing is None:
#         return

#     if gray_out:
#         change_color_to_gray(drawing)

#     x0, y0, x1, y1 = drawing.getBounds()
#     content_w = max(1.0, float(x1 - x0))
#     content_h = max(1.0, float(y1 - y0))

#     scale = min(
#         (box_w * INNER_PAD_RATIO) / content_w,
#         (box_h * INNER_PAD_RATIO) / content_h
#     )

#     # Important:
#     # Use canvas transforms instead of drawing.scale(scale, scale)
#     # to avoid distorting / damaging the SVG shapes at high counts.
#     can.saveState()
#     can.translate(center_x, center_y)
#     can.scale(scale, scale)
#     can.translate(-((x0 + x1) / 2.0), -((y0 + y1) / 2.0))
#     renderPDF.draw(drawing, can, 0, 0)
#     can.restoreState()


# def build_svg_lookup(svg_dir):
#     """
#     Scan SVG_DIR and build:
#     {
#         '333': '/full/path/333.svg',
#         '222': '/full/path/222.svg',
#         '333oh': '/full/path/333oh.svg',
#         ...
#     }
#     """
#     if not os.path.isdir(svg_dir):
#         raise FileNotFoundError(f"SVG folder not found: {svg_dir}")

#     svg_map = {}
#     for root, _, files in os.walk(svg_dir):
#         for file in files:
#             if file.lower().endswith(".svg"):
#                 stem = os.path.splitext(file)[0]
#                 key = normalize_key(stem)
#                 full_path = os.path.join(root, file)
#                 svg_map[key] = full_path
#     return svg_map


# def find_event_columns_and_svgs(df, svg_dir):
#     """
#     Looks at all columns after 'Name' and keeps only those whose header
#     matches an available SVG file.
#     """
#     cols = [str(c).strip() for c in df.columns]
#     df.columns = cols

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     name_idx = list(df.columns).index('Name')
#     candidate_cols = df.columns[name_idx + 1:]

#     svg_lookup = build_svg_lookup(svg_dir)

#     event_cols = []
#     svg_paths = []
#     missing_headers = []

#     for col in candidate_cols:
#         col_clean = str(col).strip()
#         if not col_clean:
#             continue

#         key = normalize_key(col_clean)

#         if key in svg_lookup:
#             event_cols.append(col)
#             svg_paths.append(svg_lookup[key])
#         else:
#             missing_headers.append(col)

#     if not event_cols:
#         raise RuntimeError(
#             "No event columns were matched with SVG files.\n"
#             f"Check your Excel headers and SVG names in: {svg_dir}"
#         )

#     if missing_headers:
#         print(
#             "[INFO] Ignored columns after 'Name' because no matching SVG was found:",
#             ", ".join(map(str, missing_headers))
#         )

#     return event_cols, svg_paths


# # -----------------------------
# # COLOR UTILITY
# # -----------------------------
# def change_color_to_gray(drawing):
#     light_gray = Color(0.7294117647, 0.6862745098, 0.6274509804)

#     def _recurse(el):
#         if hasattr(el, 'fillColor') and getattr(el, 'fillColor'):
#             el.fillColor = light_gray
#         if hasattr(el, 'strokeColor') and getattr(el, 'strokeColor'):
#             el.strokeColor = light_gray
#         if isinstance(el, (Path, String)):
#             el.fillColor = light_gray
#             try:
#                 el.strokeColor = light_gray
#             except Exception:
#                 pass
#         if hasattr(el, 'contents') and el.contents:
#             for child in el.contents:
#                 _recurse(child)

#     _recurse(drawing)


# # -----------------------------
# # OVERLAY BUILDER
# # -----------------------------
# def draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, icon_size_pt, page_w, page_h):
#     name = ("" if pd.isna(name) else str(name)).strip().upper()

#     packet = io.BytesIO()
#     can = canvas.Canvas(packet, pagesize=(page_w, page_h))

#     # Fit name width
#     max_width = page_w - 200
#     font_size = 45.3
#     while can.stringWidth(name, "HammersmithOne", font_size) > max_width and font_size > 10:
#         font_size -= 1.0

#     can.setFont("HammersmithOne", font_size)

#     fill_color = HexColor("#000000")
#     can.setFillColor(fill_color)
#     can.setStrokeColor(fill_color)

#     # Draw participant name
#     char_spacing = 6
#     text_width = can.stringWidth(name, "HammersmithOne", font_size) + max(0, len(name) - 1) * char_spacing
#     x = (page_w - text_width) / 2.0
#     y = 350

#     can.saveState()
#     can.setLineWidth(1.2)

#     txt = can.beginText()
#     txt.setTextOrigin(x, y)
#     txt.setFont("HammersmithOne", font_size)

#     try:
#         txt.setCharSpace(char_spacing)
#     except Exception:
#         pass

#     try:
#         txt.setTextRenderMode(2)
#     except Exception:
#         try:
#             can._code.append("2 Tr")
#         except Exception:
#             pass

#     txt.textLine(name)
#     can.drawText(txt)
#     can.restoreState()

#     # Event SVGs
#     for idx, (svg_path, (svg_cx, svg_cy), transparency) in enumerate(
#         zip(svg_paths, svg_positions, transparency_values)
#     ):
#         try:
#             try:
#                 tval = int(float(transparency)) if (transparency is not None and str(transparency) != "nan") else 0
#             except Exception:
#                 tval = 0

#             draw_svg_centered(
#                 can=can,
#                 svg_path=svg_path,
#                 center_x=svg_cx,
#                 center_y=svg_cy,
#                 box_w=icon_size_pt,
#                 box_h=icon_size_pt,
#                 gray_out=(tval == 0)
#             )

#         except Exception as e:
#             print(
#                 f"[WARN] SVG render failed (row name={name}, idx={idx}, path={svg_path}): {e}",
#                 file=sys.stderr
#             )
#             continue

#     can.save()
#     packet.seek(0)
#     return packet


# # -----------------------------
# # PAGE CREATOR
# # -----------------------------
# def create_certificate_page_bytes(name, transparency_values, svg_paths, svg_positions, icon_size_pt):
#     overlay_stream = draw_overlay_pdf(
#         name,
#         transparency_values,
#         svg_paths,
#         svg_positions,
#         icon_size_pt,
#         page_width,
#         page_height
#     )
#     overlay_reader = PdfReader(overlay_stream)

#     base_reader = PdfReader(io.BytesIO(TEMPLATE_BYTES))
#     page = base_reader.pages[0]
#     page.merge_page(overlay_reader.pages[0])

#     out_buf = io.BytesIO()
#     one = PdfWriter()
#     one.add_page(page)
#     one.write(out_buf)
#     out_buf.seek(0)
#     return out_buf.getvalue()


# # -----------------------------
# # MAIN
# # -----------------------------
# def main():
#     if not os.path.exists(EXCEL_FILE):
#         raise FileNotFoundError(f"Excel file not found at: {EXCEL_FILE}")

#     try:
#         df = pd.read_excel(EXCEL_FILE)
#     except Exception as e:
#         raise RuntimeError(
#             f"Failed to read '{EXCEL_FILE}'. If it's .xlsx, ensure 'openpyxl' is installed. Original error: {e}"
#         )

#     # Normalize headers
#     df.columns = [str(c).strip() for c in df.columns]

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     # Clean names
#     df['Name'] = df['Name'].astype(object)
#     df['Name'] = df['Name'].ffill()
#     df['Name'] = df['Name'].apply(lambda x: "" if pd.isna(x) else str(x).strip())

#     # Drop fully blank rows except Name
#     if df.shape[1] > 1:
#         non_name_cols = [c for c in df.columns if c != 'Name']

#         def _row_all_blank(s):
#             return all((pd.isna(v) or str(v).strip() == "") for v in s)

#         mask_keep = ~df[non_name_cols].apply(_row_all_blank, axis=1)
#         df = df[mask_keep].copy()

#     if DEDUP_BY_NAME:
#         before = len(df)
#         df = df.drop_duplicates(subset=['Name']).reset_index(drop=True)
#         after = len(df)
#         print(f"[INFO] Dedup by name: {before} -> {after}")

#     # Detect event columns + SVGs
#     event_cols, svg_paths = find_event_columns_and_svgs(df, SVG_DIR)
#     svg_positions, icon_size_pt, gap_pt, row_y = get_svg_positions_single_row(len(svg_paths))

#     print("[INFO] Detected event columns:", event_cols)
#     print("[INFO] Matching SVG files:")
#     for col, path in zip(event_cols, svg_paths):
#         print(f"   {col} -> {path}")

#     print(f"[INFO] Computed icon size: {round(icon_size_pt, 2)} pt")
#     print(f"[INFO] Computed icon gap: {round(gap_pt, 2)} pt")
#     print(f"[INFO] Computed row Y: {round(row_y, 2)}")
#     print(f"[INFO] Total events in one row: {len(svg_paths)}")

#     # Convert event flags to 0/1
#     def _to_flag(x):
#         if pd.isna(x):
#             return 0
#         s = str(x).strip().lower()
#         if s in {"1", "true", "yes", "y", "✓", "check", "x"}:
#             return 1
#         try:
#             return 1 if int(float(s)) != 0 else 0
#         except Exception:
#             return 0

#     df_events = df[event_cols].copy()
#     for col in event_cols:
#         df_events[col] = df_events[col].map(_to_flag)

#     total = len(df)
#     print(f"[INFO] Preparing {total} certificate(s)...")

#     merger = PdfMerger()
#     rendered_count = 0

#     for idx in range(total):
#         row = df.iloc[idx]
#         safe_name = "" if pd.isna(row['Name']) else str(row['Name']).strip()
#         if not safe_name:
#             safe_name = "PARTICIPANT"

#         flags = df_events.iloc[idx].tolist()

#         try:
#             page_bytes = create_certificate_page_bytes(
#                 safe_name,
#                 flags,
#                 svg_paths,
#                 svg_positions,
#                 icon_size_pt
#             )
#             merger.append(io.BytesIO(page_bytes))
#             rendered_count += 1
#             print(f"  - [{idx + 1}/{total}] {safe_name}")
#         except Exception as e:
#             print(f"[ERROR] Failed for row {idx + 1} (name={safe_name}): {e}", file=sys.stderr)
#             continue

#     if rendered_count == 0:
#         merger.close()
#         raise RuntimeError("No pages were rendered. Check your Excel and SVG paths.")

#     with open(OUTPUT_FILE, "wb") as out:
#         merger.write(out)
#     merger.close()

#     print(f"Combined certificates created successfully! Pages written: {rendered_count}/{total} -> {OUTPUT_FILE}")


# if __name__ == "__main__":
#     main()



# import io
# import os
# import sys
# import pandas as pd

# # PDF / drawing libs
# from reportlab.pdfgen import canvas
# from reportlab.pdfbase.ttfonts import TTFont
# from reportlab.pdfbase import pdfmetrics
# from reportlab.lib.colors import HexColor, Color
# from reportlab.graphics import renderPDF
# from reportlab.graphics.shapes import Path, String
# from svglib.svglib import svg2rlg

# # PDF readers/writers
# try:
#     from pypdf import PdfReader, PdfWriter, PdfMerger
# except Exception:
#     from PyPDF2 import PdfReader, PdfWriter, PdfMerger

# # -----------------------------
# # CONFIG / PATHS
# # -----------------------------
# TEMPLATE_PATH = r'./templates_and_suggested_fonts/Template2/template.pdf'
# EXCEL_FILE = 'comp.xlsx'
# OUTPUT_FILE = 'newcertis.pdf'
# FONT_PATH = r'./Hammersmith_One16/HammersmithOne-Regular.ttf'

# SVG_DIR = r'./events'
# DEDUP_BY_NAME = False

# # -----------------------------
# # ICON LAYOUT CONFIG
# # -----------------------------
# ICON_MM = 13
# MAX_ICON_SIZE_PT = ICON_MM * (72 / 25.4)

# MIN_ICON_MM = 6.2
# MIN_ICON_SIZE_PT = MIN_ICON_MM * (72 / 25.4)

# SIDE_MARGIN = 30
# BASE_ICON_ROW_Y = 748
# ROW_GAP_PT = 30

# # Draw SVG slightly smaller inside its slot
# INNER_PAD_RATIO = 0.84

# MIN_GAP_PT = 4.0
# MAX_GAP_PT = 14.0

# # Keep one row only up to this many events
# MAX_ICONS_SINGLE_ROW = 12

# # -----------------------------
# # READ TEMPLATE + PAGE SIZE + FONT
# # -----------------------------
# if not os.path.exists(TEMPLATE_PATH):
#     raise FileNotFoundError(f"Template PDF not found at: {TEMPLATE_PATH}")

# with open(TEMPLATE_PATH, 'rb') as _tf:
#     TEMPLATE_BYTES = _tf.read()

# tmp_reader_for_size = PdfReader(io.BytesIO(TEMPLATE_BYTES))
# base_page = tmp_reader_for_size.pages[0]
# page_width = float(base_page.mediabox.width)
# page_height = float(base_page.mediabox.height)

# if not os.path.exists(FONT_PATH):
#     raise FileNotFoundError(f"Font not found at: {FONT_PATH}")

# pdfmetrics.registerFont(TTFont('HammersmithOne', FONT_PATH))

# # -----------------------------
# # HELPERS
# # -----------------------------
# def normalize_key(s: str) -> str:
#     """
#     Normalize Excel headers / filenames so matching is robust.
#     Example:
#     '333 OH' -> '333oh'
#     '333_oh' -> '333oh'
#     '333-oh' -> '333oh'
#     """
#     if s is None:
#         return ""
#     s = str(s).strip().lower()
#     for ch in [" ", "_", "-", "."]:
#         s = s.replace(ch, "")
#     return s


# def _balanced_row_counts(n, max_single_row=MAX_ICONS_SINGLE_ROW):
#     """
#     Use 1 row up to max_single_row.
#     Otherwise split into 2 visually balanced rows.
#     """
#     if n <= max_single_row:
#         return [n]

#     top = (n + 1) // 2
#     bottom = n // 2
#     return [top, bottom]


# def _fit_common_row_layout(max_count, available_width, preferred_icon_size):
#     """
#     Compute a common icon size + gap that fits the row with the most icons.
#     This same icon size is reused on all rows for consistency.
#     """
#     if max_count <= 0:
#         return preferred_icon_size, 0.0

#     if max_count == 1:
#         return preferred_icon_size, 0.0

#     icon_size_pt = preferred_icon_size
#     gap_pt = max(MIN_GAP_PT, min(MAX_GAP_PT, icon_size_pt * 0.28))

#     total_width = max_count * icon_size_pt + (max_count - 1) * gap_pt

#     # If too wide, reduce gap first
#     if total_width > available_width:
#         gap_pt = max(MIN_GAP_PT, (available_width - max_count * icon_size_pt) / (max_count - 1))
#         total_width = max_count * icon_size_pt + (max_count - 1) * gap_pt

#     # If still too wide, reduce icon size
#     if total_width > available_width:
#         icon_size_pt = (available_width - (max_count - 1) * gap_pt) / max_count
#         icon_size_pt = max(MIN_ICON_SIZE_PT, min(icon_size_pt, MAX_ICON_SIZE_PT))
#         total_width = max_count * icon_size_pt + (max_count - 1) * gap_pt

#     # Final safety
#     if total_width > available_width:
#         gap_pt = max(0.0, (available_width - max_count * icon_size_pt) / (max_count - 1))
#         total_width = max_count * icon_size_pt + (max_count - 1) * gap_pt

#     # If still too wide, reduce icon size one more time
#     if total_width > available_width:
#         icon_size_pt = (available_width - (max_count - 1) * gap_pt) / max_count
#         total_width = max_count * icon_size_pt + (max_count - 1) * gap_pt

#     return float(icon_size_pt), float(gap_pt)


# def _build_row_positions(count, row_y, icon_size_pt, gap_pt):
#     """
#     Build centered positions for one row using a fixed icon size and gap.
#     """
#     if count <= 0:
#         return []

#     if count == 1:
#         return [(float(page_width / 2.0), float(row_y))]

#     total_width = count * icon_size_pt + (count - 1) * gap_pt
#     start_x = (page_width - total_width) / 2.0 + (icon_size_pt / 2.0)

#     positions = []
#     for i in range(count):
#         cx = start_x + i * (icon_size_pt + gap_pt)
#         positions.append((float(round(cx, 2)), float(round(row_y, 2))))
#     return positions


# def get_svg_positions_auto(n):
#     """
#     Auto layout:
#     - 1 row for smaller counts
#     - 2 rows for larger counts

#     Returns:
#         positions, icon_size_pt, gap_pt, row_count
#     """
#     if n <= 0:
#         return [], MAX_ICON_SIZE_PT, 0.0, 0

#     row_counts = _balanced_row_counts(n)

#     full_available_width = page_width - (2 * SIDE_MARGIN)

#     if len(row_counts) == 1:
#         available_width = full_available_width * 0.94
#         preferred_icon_size = MAX_ICON_SIZE_PT
#         row_ys = [BASE_ICON_ROW_Y]
#     else:
#         # Two rows: give a little more breathing room and slightly smaller preferred icons
#         available_width = full_available_width * 0.88
#         preferred_icon_size = min(MAX_ICON_SIZE_PT, 30.0)
#         row_ys = [BASE_ICON_ROW_Y + 10, BASE_ICON_ROW_Y - ROW_GAP_PT]

#     icon_size_pt, gap_pt = _fit_common_row_layout(
#         max_count=max(row_counts),
#         available_width=available_width,
#         preferred_icon_size=preferred_icon_size
#     )

#     all_positions = []
#     for row_count, row_y in zip(row_counts, row_ys):
#         all_positions.extend(
#             _build_row_positions(
#                 count=row_count,
#                 row_y=row_y,
#                 icon_size_pt=icon_size_pt,
#                 gap_pt=gap_pt
#             )
#         )

#     return all_positions, float(icon_size_pt), float(gap_pt), len(row_counts)


# def draw_svg_centered(can, svg_path, center_x, center_y, box_w, box_h, gray_out=False):
#     """
#     Draw SVG centered at (center_x, center_y) using canvas transforms.
#     This is safer than mutating the drawing geometry.
#     """
#     if not os.path.exists(svg_path):
#         return

#     drawing = svg2rlg(svg_path)
#     if drawing is None:
#         return

#     if gray_out:
#         change_color_to_gray(drawing)

#     x0, y0, x1, y1 = drawing.getBounds()
#     content_w = max(1.0, float(x1 - x0))
#     content_h = max(1.0, float(y1 - y0))

#     scale = min(
#         (box_w * INNER_PAD_RATIO) / content_w,
#         (box_h * INNER_PAD_RATIO) / content_h
#     )

#     can.saveState()
#     can.translate(center_x, center_y)
#     can.scale(scale, scale)
#     can.translate(-((x0 + x1) / 2.0), -((y0 + y1) / 2.0))
#     renderPDF.draw(drawing, can, 0, 0)
#     can.restoreState()


# def build_svg_lookup(svg_dir):
#     """
#     Scan SVG_DIR and build:
#     {
#         '333': '/full/path/333.svg',
#         '222': '/full/path/222.svg',
#         '333oh': '/full/path/333oh.svg',
#         ...
#     }
#     """
#     if not os.path.isdir(svg_dir):
#         raise FileNotFoundError(f"SVG folder not found: {svg_dir}")

#     svg_map = {}
#     for root, _, files in os.walk(svg_dir):
#         for file in files:
#             if file.lower().endswith(".svg"):
#                 stem = os.path.splitext(file)[0]
#                 key = normalize_key(stem)
#                 full_path = os.path.join(root, file)
#                 svg_map[key] = full_path
#     return svg_map


# def find_event_columns_and_svgs(df, svg_dir):
#     """
#     Looks at all columns after 'Name' and keeps only those whose header
#     matches an available SVG file.
#     """
#     cols = [str(c).strip() for c in df.columns]
#     df.columns = cols

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     name_idx = list(df.columns).index('Name')
#     candidate_cols = df.columns[name_idx + 1:]

#     svg_lookup = build_svg_lookup(svg_dir)

#     event_cols = []
#     svg_paths = []
#     missing_headers = []

#     for col in candidate_cols:
#         col_clean = str(col).strip()
#         if not col_clean:
#             continue

#         key = normalize_key(col_clean)

#         if key in svg_lookup:
#             event_cols.append(col)
#             svg_paths.append(svg_lookup[key])
#         else:
#             missing_headers.append(col)

#     if not event_cols:
#         raise RuntimeError(
#             "No event columns were matched with SVG files.\n"
#             f"Check your Excel headers and SVG names in: {svg_dir}"
#         )

#     if missing_headers:
#         print(
#             "[INFO] Ignored columns after 'Name' because no matching SVG was found:",
#             ", ".join(map(str, missing_headers))
#         )

#     return event_cols, svg_paths


# # -----------------------------
# # COLOR UTILITY
# # -----------------------------
# def change_color_to_gray(drawing):
#     light_gray = Color(0.7294117647, 0.6862745098, 0.6274509804)

#     def _recurse(el):
#         if hasattr(el, 'fillColor') and getattr(el, 'fillColor'):
#             el.fillColor = light_gray
#         if hasattr(el, 'strokeColor') and getattr(el, 'strokeColor'):
#             el.strokeColor = light_gray
#         if isinstance(el, (Path, String)):
#             el.fillColor = light_gray
#             try:
#                 el.strokeColor = light_gray
#             except Exception:
#                 pass
#         if hasattr(el, 'contents') and el.contents:
#             for child in el.contents:
#                 _recurse(child)

#     _recurse(drawing)


# # -----------------------------
# # OVERLAY BUILDER
# # -----------------------------
# def draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, icon_size_pt, page_w, page_h):
#     name = ("" if pd.isna(name) else str(name)).strip().upper()

#     packet = io.BytesIO()
#     can = canvas.Canvas(packet, pagesize=(page_w, page_h))

#     # Fit name width
#     max_width = page_w - 200
#     font_size = 45.3
#     while can.stringWidth(name, "HammersmithOne", font_size) > max_width and font_size > 10:
#         font_size -= 1.0

#     can.setFont("HammersmithOne", font_size)

#     fill_color = HexColor("#000000")
#     can.setFillColor(fill_color)
#     can.setStrokeColor(fill_color)

#     # Draw participant name
#     char_spacing = 6
#     text_width = can.stringWidth(name, "HammersmithOne", font_size) + max(0, len(name) - 1) * char_spacing
#     x = (page_w - text_width) / 2.0
#     y = 350

#     can.saveState()
#     can.setLineWidth(1.2)

#     txt = can.beginText()
#     txt.setTextOrigin(x, y)
#     txt.setFont("HammersmithOne", font_size)

#     try:
#         txt.setCharSpace(char_spacing)
#     except Exception:
#         pass

#     try:
#         txt.setTextRenderMode(2)
#     except Exception:
#         try:
#             can._code.append("2 Tr")
#         except Exception:
#             pass

#     txt.textLine(name)
#     can.drawText(txt)
#     can.restoreState()

#     # Event SVGs
#     for idx, (svg_path, (svg_cx, svg_cy), transparency) in enumerate(
#         zip(svg_paths, svg_positions, transparency_values)
#     ):
#         try:
#             try:
#                 tval = int(float(transparency)) if (transparency is not None and str(transparency) != "nan") else 0
#             except Exception:
#                 tval = 0

#             draw_svg_centered(
#                 can=can,
#                 svg_path=svg_path,
#                 center_x=svg_cx,
#                 center_y=svg_cy,
#                 box_w=icon_size_pt,
#                 box_h=icon_size_pt,
#                 gray_out=(tval == 0)
#             )

#         except Exception as e:
#             print(
#                 f"[WARN] SVG render failed (row name={name}, idx={idx}, path={svg_path}): {e}",
#                 file=sys.stderr
#             )
#             continue

#     can.save()
#     packet.seek(0)
#     return packet


# # -----------------------------
# # PAGE CREATOR
# # -----------------------------
# def create_certificate_page_bytes(name, transparency_values, svg_paths, svg_positions, icon_size_pt):
#     overlay_stream = draw_overlay_pdf(
#         name,
#         transparency_values,
#         svg_paths,
#         svg_positions,
#         icon_size_pt,
#         page_width,
#         page_height
#     )
#     overlay_reader = PdfReader(overlay_stream)

#     base_reader = PdfReader(io.BytesIO(TEMPLATE_BYTES))
#     page = base_reader.pages[0]
#     page.merge_page(overlay_reader.pages[0])

#     out_buf = io.BytesIO()
#     one = PdfWriter()
#     one.add_page(page)
#     one.write(out_buf)
#     out_buf.seek(0)
#     return out_buf.getvalue()


# # -----------------------------
# # MAIN
# # -----------------------------
# def main():
#     if not os.path.exists(EXCEL_FILE):
#         raise FileNotFoundError(f"Excel file not found at: {EXCEL_FILE}")

#     try:
#         df = pd.read_excel(EXCEL_FILE)
#     except Exception as e:
#         raise RuntimeError(
#             f"Failed to read '{EXCEL_FILE}'. If it's .xlsx, ensure 'openpyxl' is installed. Original error: {e}"
#         )

#     # Normalize headers
#     df.columns = [str(c).strip() for c in df.columns]

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     # Clean names
#     df['Name'] = df['Name'].astype(object)
#     df['Name'] = df['Name'].ffill()
#     df['Name'] = df['Name'].apply(lambda x: "" if pd.isna(x) else str(x).strip())

#     # Drop fully blank rows except Name
#     if df.shape[1] > 1:
#         non_name_cols = [c for c in df.columns if c != 'Name']

#         def _row_all_blank(s):
#             return all((pd.isna(v) or str(v).strip() == "") for v in s)

#         mask_keep = ~df[non_name_cols].apply(_row_all_blank, axis=1)
#         df = df[mask_keep].copy()

#     if DEDUP_BY_NAME:
#         before = len(df)
#         df = df.drop_duplicates(subset=['Name']).reset_index(drop=True)
#         after = len(df)
#         print(f"[INFO] Dedup by name: {before} -> {after}")

#     # Detect event columns + SVGs
#     event_cols, svg_paths = find_event_columns_and_svgs(df, SVG_DIR)
#     svg_positions, icon_size_pt, gap_pt, row_count = get_svg_positions_auto(len(svg_paths))

#     print("[INFO] Detected event columns:", event_cols)
#     print("[INFO] Matching SVG files:")
#     for col, path in zip(event_cols, svg_paths):
#         print(f"   {col} -> {path}")

#     print(f"[INFO] Computed icon size: {round(icon_size_pt, 2)} pt")
#     print(f"[INFO] Computed icon gap: {round(gap_pt, 2)} pt")
#     print(f"[INFO] Row count used: {row_count}")
#     print(f"[INFO] Total events: {len(svg_paths)}")

#     # Convert event flags to 0/1
#     def _to_flag(x):
#         if pd.isna(x):
#             return 0
#         s = str(x).strip().lower()
#         if s in {"1", "true", "yes", "y", "✓", "check", "x"}:
#             return 1
#         try:
#             return 1 if int(float(s)) != 0 else 0
#         except Exception:
#             return 0

#     df_events = df[event_cols].copy()
#     for col in event_cols:
#         df_events[col] = df_events[col].map(_to_flag)

#     total = len(df)
#     print(f"[INFO] Preparing {total} certificate(s)...")

#     merger = PdfMerger()
#     rendered_count = 0

#     for idx in range(total):
#         row = df.iloc[idx]
#         safe_name = "" if pd.isna(row['Name']) else str(row['Name']).strip()
#         if not safe_name:
#             safe_name = "PARTICIPANT"

#         flags = df_events.iloc[idx].tolist()

#         try:
#             page_bytes = create_certificate_page_bytes(
#                 safe_name,
#                 flags,
#                 svg_paths,
#                 svg_positions,
#                 icon_size_pt
#             )
#             merger.append(io.BytesIO(page_bytes))
#             rendered_count += 1
#             print(f"  - [{idx + 1}/{total}] {safe_name}")
#         except Exception as e:
#             print(f"[ERROR] Failed for row {idx + 1} (name={safe_name}): {e}", file=sys.stderr)
#             continue

#     if rendered_count == 0:
#         merger.close()
#         raise RuntimeError("No pages were rendered. Check your Excel and SVG paths.")

#     with open(OUTPUT_FILE, "wb") as out:
#         merger.write(out)
#     merger.close()

#     print(f"Combined certificates created successfully! Pages written: {rendered_count}/{total} -> {OUTPUT_FILE}")


# if __name__ == "__main__":
#     main()






# import io
# import os
# import sys
# import pandas as pd

# # PDF / drawing libs
# from reportlab.pdfgen import canvas
# from reportlab.pdfbase.ttfonts import TTFont
# from reportlab.pdfbase import pdfmetrics
# from reportlab.lib.colors import HexColor, Color
# from reportlab.graphics import renderPDF
# from reportlab.graphics.shapes import Path, String
# from svglib.svglib import svg2rlg

# # PDF readers/writers
# try:
#     from pypdf import PdfReader, PdfWriter, PdfMerger
# except Exception:
#     from PyPDF2 import PdfReader, PdfWriter, PdfMerger


# # -----------------------------
# # CONFIG / PATHS
# # -----------------------------
# TEMPLATE_PATH = r'./templates_and_suggested_fonts/Template2/template.pdf'
# EXCEL_FILE = 'comp.xlsx'
# OUTPUT_FILE = 'newcertis.pdf'
# FONT_PATH = r'./Hammersmith_One16/HammersmithOne-Regular.ttf'

# SVG_DIR = r'./events'
# DEDUP_BY_NAME = False


# # -----------------------------
# # ICON LAYOUT CONFIG
# # -----------------------------
# ICON_MM = 13
# MAX_ICON_SIZE_PT = ICON_MM * (72 / 25.4)

# # Lower bound only as a last-resort safety
# MIN_ICON_MM = 4.2
# MIN_ICON_SIZE_PT = MIN_ICON_MM * (72 / 25.4)

# # Small fixed padding on both sides
# SIDE_PADDING_PT = 18

# # Top row Y
# BASE_ICON_ROW_Y = 748

# # Draw slightly inside the slot so icons don't touch each other visually
# INNER_PAD_RATIO = 0.90

# # Gap tuning
# MIN_GAP_PT = 1.0
# MAX_GAP_PT = 10.0


# # -----------------------------
# # READ TEMPLATE + PAGE SIZE + FONT
# # -----------------------------
# if not os.path.exists(TEMPLATE_PATH):
#     raise FileNotFoundError(f"Template PDF not found at: {TEMPLATE_PATH}")

# with open(TEMPLATE_PATH, 'rb') as _tf:
#     TEMPLATE_BYTES = _tf.read()

# tmp_reader_for_size = PdfReader(io.BytesIO(TEMPLATE_BYTES))
# base_page = tmp_reader_for_size.pages[0]
# page_width = float(base_page.mediabox.width)
# page_height = float(base_page.mediabox.height)

# if not os.path.exists(FONT_PATH):
#     raise FileNotFoundError(f"Font not found at: {FONT_PATH}")

# pdfmetrics.registerFont(TTFont('HammersmithOne', FONT_PATH))


# # -----------------------------
# # HELPERS
# # -----------------------------
# def normalize_key(s: str) -> str:
#     """
#     Normalize Excel headers / filenames so matching is robust.
#     Example:
#     '333 OH' -> '333oh'
#     '333_oh' -> '333oh'
#     '333-oh' -> '333oh'
#     """
#     if s is None:
#         return ""
#     s = str(s).strip().lower()
#     for ch in [" ", "_", "-", "."]:
#         s = s.replace(ch, "")
#     return s


# def get_svg_positions_one_row(n):
#     """
#     Always keep all icons in ONE ROW.

#     Layout rules:
#     - uses full width between small fixed left/right paddings
#     - if events are fewer -> icons grow
#     - if events are more -> gap shrinks first, then icon size
#     - positions start after left padding and end before right padding

#     Returns:
#         positions, icon_size_pt, gap_pt, row_y
#     """
#     if n <= 0:
#         return [], MAX_ICON_SIZE_PT, 0.0, BASE_ICON_ROW_Y

#     usable_width = page_width - (2 * SIDE_PADDING_PT)
#     row_y = BASE_ICON_ROW_Y

#     if n == 1:
#         return [(page_width / 2.0, row_y)], MAX_ICON_SIZE_PT, 0.0, row_y

#     # Dynamic preferred gap based on event count
#     # Fewer icons -> bigger gap
#     # More icons -> smaller gap
#     if n <= 4:
#         preferred_gap = 10.0
#     elif n <= 6:
#         preferred_gap = 8.0
#     elif n <= 8:
#         preferred_gap = 6.0
#     elif n <= 10:
#         preferred_gap = 4.5
#     elif n <= 12:
#         preferred_gap = 3.5
#     elif n <= 15:
#         preferred_gap = 3.4
#     else:
#         preferred_gap = 3.6

#     gap_pt = max(MIN_GAP_PT, min(MAX_GAP_PT, preferred_gap))

#     # First compute icon size from usable width
#     icon_size_pt = (usable_width - (n - 1) * gap_pt) / n

#     # If icons are too large, cap size and spread extra width into the gaps
#     if icon_size_pt > MAX_ICON_SIZE_PT:
#         icon_size_pt = MAX_ICON_SIZE_PT
#         gap_pt = (usable_width - n * icon_size_pt) / (n - 1)
#         gap_pt = max(MIN_GAP_PT, min(gap_pt, MAX_GAP_PT))

#     # If gap got clamped and there is still extra width, give it back to icon size
#     total_width = n * icon_size_pt + (n - 1) * gap_pt
#     if total_width < usable_width:
#         extra = usable_width - total_width
#         grow_per_icon = extra / n
#         icon_size_pt = min(MAX_ICON_SIZE_PT, icon_size_pt + grow_per_icon)
#         total_width = n * icon_size_pt + (n - 1) * gap_pt

#     # If row is still too wide, reduce gap first
#     if total_width > usable_width:
#         gap_pt = max(MIN_GAP_PT, (usable_width - n * icon_size_pt) / (n - 1))
#         total_width = n * icon_size_pt + (n - 1) * gap_pt

#     # If still too wide, reduce icon size
#     if total_width > usable_width:
#         icon_size_pt = (usable_width - (n - 1) * gap_pt) / n
#         total_width = n * icon_size_pt + (n - 1) * gap_pt

#     # Final safety lower bound
#     if icon_size_pt < MIN_ICON_SIZE_PT:
#         icon_size_pt = MIN_ICON_SIZE_PT
#         gap_pt = (usable_width - n * icon_size_pt) / (n - 1)
#         if gap_pt < 0:
#             gap_pt = 0.0
#             icon_size_pt = usable_width / n
#         total_width = n * icon_size_pt + (n - 1) * gap_pt

#     # IMPORTANT:
#     # Start exactly after left padding, not re-centered to a narrower width
#     start_x = SIDE_PADDING_PT + (icon_size_pt / 2.0)

#     positions = []
#     for i in range(n):
#         cx = start_x + i * (icon_size_pt + gap_pt)
#         positions.append((float(round(cx, 2)), float(round(row_y, 2))))

#     return positions, float(icon_size_pt), float(gap_pt), float(row_y)


# def draw_svg_centered(can, svg_path, center_x, center_y, box_w, box_h, gray_out=False):
#     """
#     Draw SVG centered at (center_x, center_y) using canvas transforms.
#     This avoids mutating the SVG geometry and is safer for many small icons.
#     """
#     if not os.path.exists(svg_path):
#         return

#     drawing = svg2rlg(svg_path)
#     if drawing is None:
#         return

#     if gray_out:
#         change_color_to_gray(drawing)

#     x0, y0, x1, y1 = drawing.getBounds()
#     content_w = max(1.0, float(x1 - x0))
#     content_h = max(1.0, float(y1 - y0))

#     # Uniform scaling -> preserves shape
#     scale = min(
#         (box_w * INNER_PAD_RATIO) / content_w,
#         (box_h * INNER_PAD_RATIO) / content_h
#     )

#     can.saveState()
#     can.translate(center_x, center_y)
#     can.scale(scale, scale)
#     can.translate(-((x0 + x1) / 2.0), -((y0 + y1) / 2.0))
#     renderPDF.draw(drawing, can, 0, 0)
#     can.restoreState()


# def build_svg_lookup(svg_dir):
#     """
#     Scan SVG_DIR and build:
#     {
#         '333': '/full/path/333.svg',
#         '222': '/full/path/222.svg',
#         '333oh': '/full/path/333oh.svg',
#         ...
#     }
#     """
#     if not os.path.isdir(svg_dir):
#         raise FileNotFoundError(f"SVG folder not found: {svg_dir}")

#     svg_map = {}
#     for root, _, files in os.walk(svg_dir):
#         for file in files:
#             if file.lower().endswith(".svg"):
#                 stem = os.path.splitext(file)[0]
#                 key = normalize_key(stem)
#                 full_path = os.path.join(root, file)
#                 svg_map[key] = full_path
#     return svg_map


# def find_event_columns_and_svgs(df, svg_dir):
#     """
#     Looks at all columns after 'Name' and keeps only those whose header
#     matches an available SVG file.
#     """
#     cols = [str(c).strip() for c in df.columns]
#     df.columns = cols

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     name_idx = list(df.columns).index('Name')
#     candidate_cols = df.columns[name_idx + 1:]

#     svg_lookup = build_svg_lookup(svg_dir)

#     event_cols = []
#     svg_paths = []
#     missing_headers = []

#     for col in candidate_cols:
#         col_clean = str(col).strip()
#         if not col_clean:
#             continue

#         key = normalize_key(col_clean)

#         if key in svg_lookup:
#             event_cols.append(col)
#             svg_paths.append(svg_lookup[key])
#         else:
#             missing_headers.append(col)

#     if not event_cols:
#         raise RuntimeError(
#             "No event columns were matched with SVG files.\n"
#             f"Check your Excel headers and SVG names in: {svg_dir}"
#         )

#     if missing_headers:
#         print(
#             "[INFO] Ignored columns after 'Name' because no matching SVG was found:",
#             ", ".join(map(str, missing_headers))
#         )

#     return event_cols, svg_paths


# # -----------------------------
# # COLOR UTILITY
# # -----------------------------
# def change_color_to_gray(drawing):
#     light_gray = Color(0.7294117647, 0.6862745098, 0.6274509804)

#     def _recurse(el):
#         if hasattr(el, 'fillColor') and getattr(el, 'fillColor'):
#             el.fillColor = light_gray
#         if hasattr(el, 'strokeColor') and getattr(el, 'strokeColor'):
#             el.strokeColor = light_gray
#         if isinstance(el, (Path, String)):
#             el.fillColor = light_gray
#             try:
#                 el.strokeColor = light_gray
#             except Exception:
#                 pass
#         if hasattr(el, 'contents') and el.contents:
#             for child in el.contents:
#                 _recurse(child)

#     _recurse(drawing)


# # -----------------------------
# # OVERLAY BUILDER
# # -----------------------------
# def draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, icon_size_pt, page_w, page_h):
#     name = ("" if pd.isna(name) else str(name)).strip().upper()

#     packet = io.BytesIO()
#     can = canvas.Canvas(packet, pagesize=(page_w, page_h))

#     # Fit name width
#     max_width = page_w - 200
#     font_size = 45.3
#     while can.stringWidth(name, "HammersmithOne", font_size) > max_width and font_size > 10:
#         font_size -= 1.0

#     can.setFont("HammersmithOne", font_size)

#     fill_color = HexColor("#000000")
#     can.setFillColor(fill_color)
#     can.setStrokeColor(fill_color)

#     # Draw participant name
#     char_spacing = 6
#     text_width = can.stringWidth(name, "HammersmithOne", font_size) + max(0, len(name) - 1) * char_spacing
#     x = (page_w - text_width) / 2.0
#     y = 350

#     can.saveState()
#     can.setLineWidth(1.2)

#     txt = can.beginText()
#     txt.setTextOrigin(x, y)
#     txt.setFont("HammersmithOne", font_size)

#     try:
#         txt.setCharSpace(char_spacing)
#     except Exception:
#         pass

#     try:
#         txt.setTextRenderMode(2)
#     except Exception:
#         try:
#             can._code.append("2 Tr")
#         except Exception:
#             pass

#     txt.textLine(name)
#     can.drawText(txt)
#     can.restoreState()

#     # Event SVGs
#     for idx, (svg_path, (svg_cx, svg_cy), transparency) in enumerate(
#         zip(svg_paths, svg_positions, transparency_values)
#     ):
#         try:
#             try:
#                 tval = int(float(transparency)) if (transparency is not None and str(transparency) != "nan") else 0
#             except Exception:
#                 tval = 0

#             draw_svg_centered(
#                 can=can,
#                 svg_path=svg_path,
#                 center_x=svg_cx,
#                 center_y=svg_cy,
#                 box_w=icon_size_pt,
#                 box_h=icon_size_pt,
#                 gray_out=(tval == 0)
#             )

#         except Exception as e:
#             print(
#                 f"[WARN] SVG render failed (row name={name}, idx={idx}, path={svg_path}): {e}",
#                 file=sys.stderr
#             )
#             continue

#     can.save()
#     packet.seek(0)
#     return packet


# # -----------------------------
# # PAGE CREATOR
# # -----------------------------
# def create_certificate_page_bytes(name, transparency_values, svg_paths, svg_positions, icon_size_pt):
#     overlay_stream = draw_overlay_pdf(
#         name,
#         transparency_values,
#         svg_paths,
#         svg_positions,
#         icon_size_pt,
#         page_width,
#         page_height
#     )
#     overlay_reader = PdfReader(overlay_stream)

#     base_reader = PdfReader(io.BytesIO(TEMPLATE_BYTES))
#     page = base_reader.pages[0]
#     page.merge_page(overlay_reader.pages[0])

#     out_buf = io.BytesIO()
#     one = PdfWriter()
#     one.add_page(page)
#     one.write(out_buf)
#     out_buf.seek(0)
#     return out_buf.getvalue()


# # -----------------------------
# # MAIN
# # -----------------------------
# def main():
#     if not os.path.exists(EXCEL_FILE):
#         raise FileNotFoundError(f"Excel file not found at: {EXCEL_FILE}")

#     try:
#         df = pd.read_excel(EXCEL_FILE)
#     except Exception as e:
#         raise RuntimeError(
#             f"Failed to read '{EXCEL_FILE}'. If it's .xlsx, ensure 'openpyxl' is installed. Original error: {e}"
#         )

#     # Normalize headers
#     df.columns = [str(c).strip() for c in df.columns]

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     # Clean names
#     df['Name'] = df['Name'].astype(object)
#     df['Name'] = df['Name'].ffill()
#     df['Name'] = df['Name'].apply(lambda x: "" if pd.isna(x) else str(x).strip())

#     # Drop fully blank rows except Name
#     if df.shape[1] > 1:
#         non_name_cols = [c for c in df.columns if c != 'Name']

#         def _row_all_blank(s):
#             return all((pd.isna(v) or str(v).strip() == "") for v in s)

#         mask_keep = ~df[non_name_cols].apply(_row_all_blank, axis=1)
#         df = df[mask_keep].copy()

#     if DEDUP_BY_NAME:
#         before = len(df)
#         df = df.drop_duplicates(subset=['Name']).reset_index(drop=True)
#         after = len(df)
#         print(f"[INFO] Dedup by name: {before} -> {after}")

#     # Detect event columns + SVGs
#     event_cols, svg_paths = find_event_columns_and_svgs(df, SVG_DIR)
#     svg_positions, icon_size_pt, gap_pt, row_y = get_svg_positions_one_row(len(svg_paths))

#     print("[INFO] Detected event columns:", event_cols)
#     print("[INFO] Matching SVG files:")
#     for col, path in zip(event_cols, svg_paths):
#         print(f"   {col} -> {path}")

#     print(f"[INFO] Computed icon size: {round(icon_size_pt, 2)} pt")
#     print(f"[INFO] Computed icon gap: {round(gap_pt, 2)} pt")
#     print(f"[INFO] Computed row Y: {round(row_y, 2)}")
#     print(f"[INFO] Total events: {len(svg_paths)}")

#     # Convert event flags to 0/1
#     def _to_flag(x):
#         if pd.isna(x):
#             return 0
#         s = str(x).strip().lower()
#         if s in {"1", "true", "yes", "y", "✓", "check", "x"}:
#             return 1
#         try:
#             return 1 if int(float(s)) != 0 else 0
#         except Exception:
#             return 0

#     df_events = df[event_cols].copy()
#     for col in event_cols:
#         df_events[col] = df_events[col].map(_to_flag)

#     total = len(df)
#     print(f"[INFO] Preparing {total} certificate(s)...")

#     merger = PdfMerger()
#     rendered_count = 0

#     for idx in range(total):
#         row = df.iloc[idx]
#         safe_name = "" if pd.isna(row['Name']) else str(row['Name']).strip()
#         if not safe_name:
#             safe_name = "PARTICIPANT"

#         flags = df_events.iloc[idx].tolist()

#         try:
#             page_bytes = create_certificate_page_bytes(
#                 safe_name,
#                 flags,
#                 svg_paths,
#                 svg_positions,
#                 icon_size_pt
#             )
#             merger.append(io.BytesIO(page_bytes))
#             rendered_count += 1
#             print(f"  - [{idx + 1}/{total}] {safe_name}")
#         except Exception as e:
#             print(f"[ERROR] Failed for row {idx + 1} (name={safe_name}): {e}", file=sys.stderr)
#             continue

#     if rendered_count == 0:
#         merger.close()
#         raise RuntimeError("No pages were rendered. Check your Excel and SVG paths.")

#     with open(OUTPUT_FILE, "wb") as out:
#         merger.write(out)
#     merger.close()

#     print(f"Combined certificates created successfully! Pages written: {rendered_count}/{total} -> {OUTPUT_FILE}")


# if __name__ == "__main__":
#     main()





# import io
# import os
# import sys
# import pandas as pd

# # PDF / drawing libs
# from reportlab.pdfgen import canvas
# from reportlab.pdfbase.ttfonts import TTFont
# from reportlab.pdfbase import pdfmetrics
# from reportlab.lib.colors import HexColor, Color
# from reportlab.graphics import renderPDF
# from reportlab.graphics.shapes import Path, String
# from svglib.svglib import svg2rlg
# from copy import deepcopy

# # PDF readers/writers
# try:
#     from pypdf import PdfReader, PdfWriter, PdfMerger
# except Exception:
#     from PyPDF2 import PdfReader, PdfWriter, PdfMerger


# # -----------------------------
# # CONFIG / PATHS
# # -----------------------------
# TEMPLATE_PATH = r'./templates_and_suggested_fonts/Template2/template.pdf'
# EXCEL_FILE = 'comp.xlsx'
# OUTPUT_FILE = 'newcertis.pdf'
# FONT_PATH = r'./Hammersmith_One16/HammersmithOne-Regular.ttf'

# SVG_DIR = r'./events'
# DEDUP_BY_NAME = False


# # -----------------------------
# # # ICON LAYOUT CONFIG
# # # -----------------------------
# # ICON_MM = 13
# # MAX_ICON_SIZE_PT = ICON_MM * (72 / 25.4)

# # # Lower bound only as a last-resort safety
# # MIN_ICON_MM = 4.2
# # MIN_ICON_SIZE_PT = MIN_ICON_MM * (72 / 25.4)

# # # Small fixed padding on both sides
# # SIDE_PADDING_PT = 18

# # # Top row Y
# # BASE_ICON_ROW_Y = 748

# # # Draw slightly inside the slot so icons don't touch each other visually
# # INNER_PAD_RATIO = 0.90

# # # Gap tuning
# # MIN_GAP_PT = 1.0
# # MAX_GAP_PT = 10.0

# # -----------------------------
# # ICON AREA / GRID CONFIG
# # -----------------------------
# # IMPORTANT:
# # These values should target a CLEAN area in your template.
# # In your screenshot, the current top blue strip already has logos in it,
# # so drawing icons there will always look crowded.
# #
# # Tune these 4 numbers once for your template.
# ICON_BOX_LEFT = 52
# ICON_BOX_RIGHT_MARGIN = 52
# ICON_BOX_TOP = 735
# ICON_BOX_BOTTOM = 670

# ICON_COL_GAP_PT = 8
# ICON_ROW_GAP_PT = 8

# # breathing room inside each icon cell
# ICON_INNER_PAD_RATIO = 0.82

# # For your current use case, 2 rows is much cleaner than 1 row.
# MAX_ICON_ROWS = 2

# # -----------------------------
# # READ TEMPLATE + PAGE SIZE + FONT
# # -----------------------------
# if not os.path.exists(TEMPLATE_PATH):
#     raise FileNotFoundError(f"Template PDF not found at: {TEMPLATE_PATH}")

# with open(TEMPLATE_PATH, 'rb') as _tf:
#     TEMPLATE_BYTES = _tf.read()

# tmp_reader_for_size = PdfReader(io.BytesIO(TEMPLATE_BYTES))
# base_page = tmp_reader_for_size.pages[0]
# page_width = float(base_page.mediabox.width)
# page_height = float(base_page.mediabox.height)

# if not os.path.exists(FONT_PATH):
#     raise FileNotFoundError(f"Font not found at: {FONT_PATH}")

# pdfmetrics.registerFont(TTFont('HammersmithOne', FONT_PATH))


# # -----------------------------
# # HELPERS
# # -----------------------------
# def normalize_key(s: str) -> str:
#     """
#     Normalize Excel headers / filenames so matching is robust.
#     Example:
#     '333 OH' -> '333oh'
#     '333_oh' -> '333oh'
#     '333-oh' -> '333oh'
#     """
#     if s is None:
#         return ""
#     s = str(s).strip().lower()
#     for ch in [" ", "_", "-", "."]:
#         s = s.replace(ch, "")
#     return s


# # def get_svg_positions_one_row(n):
# #     """
# #     Always keep all icons in ONE ROW.

# #     Layout rules:
# #     - uses full width between small fixed left/right paddings
# #     - if events are fewer -> icons grow
# #     - if events are more -> gap shrinks first, then icon size
# #     - positions start after left padding and end before right padding

# #     Returns:
# #         positions, icon_size_pt, gap_pt, row_y
# #     """
# #     if n <= 0:
# #         return [], MAX_ICON_SIZE_PT, 0.0, BASE_ICON_ROW_Y

# #     usable_width = page_width - (2 * SIDE_PADDING_PT)
# #     row_y = BASE_ICON_ROW_Y

# #     if n == 1:
# #         return [(page_width / 2.0, row_y)], MAX_ICON_SIZE_PT, 0.0, row_y

# #     # Dynamic preferred gap based on event count
# #     # Fewer icons -> bigger gap
# #     # More icons -> smaller gap
# #     if n <= 4:
# #         preferred_gap = 10.0
# #     elif n <= 6:
# #         preferred_gap = 8.0
# #     elif n <= 8:
# #         preferred_gap = 6.0
# #     elif n <= 10:
# #         preferred_gap = 4.5
# #     elif n <= 12:
# #         preferred_gap = 3.5
# #     elif n <= 15:
# #         preferred_gap = 3.4
# #     else:
# #         preferred_gap = 3.6

# #     gap_pt = max(MIN_GAP_PT, min(MAX_GAP_PT, preferred_gap))

# #     # First compute icon size from usable width
# #     icon_size_pt = (usable_width - (n - 1) * gap_pt) / n

# #     # If icons are too large, cap size and spread extra width into the gaps
# #     if icon_size_pt > MAX_ICON_SIZE_PT:
# #         icon_size_pt = MAX_ICON_SIZE_PT
# #         gap_pt = (usable_width - n * icon_size_pt) / (n - 1)
# #         gap_pt = max(MIN_GAP_PT, min(gap_pt, MAX_GAP_PT))

# #     # If gap got clamped and there is still extra width, give it back to icon size
# #     total_width = n * icon_size_pt + (n - 1) * gap_pt
# #     if total_width < usable_width:
# #         extra = usable_width - total_width
# #         grow_per_icon = extra / n
# #         icon_size_pt = min(MAX_ICON_SIZE_PT, icon_size_pt + grow_per_icon)
# #         total_width = n * icon_size_pt + (n - 1) * gap_pt

# #     # If row is still too wide, reduce gap first
# #     if total_width > usable_width:
# #         gap_pt = max(MIN_GAP_PT, (usable_width - n * icon_size_pt) / (n - 1))
# #         total_width = n * icon_size_pt + (n - 1) * gap_pt

# #     # If still too wide, reduce icon size
# #     if total_width > usable_width:
# #         icon_size_pt = (usable_width - (n - 1) * gap_pt) / n
# #         total_width = n * icon_size_pt + (n - 1) * gap_pt

# #     # Final safety lower bound
# #     if icon_size_pt < MIN_ICON_SIZE_PT:
# #         icon_size_pt = MIN_ICON_SIZE_PT
# #         gap_pt = (usable_width - n * icon_size_pt) / (n - 1)
# #         if gap_pt < 0:
# #             gap_pt = 0.0
# #             icon_size_pt = usable_width / n
# #         total_width = n * icon_size_pt + (n - 1) * gap_pt

# #     # IMPORTANT:
# #     # Start exactly after left padding, not re-centered to a narrower width
# #     start_x = SIDE_PADDING_PT + (icon_size_pt / 2.0)

# #     positions = []
# #     for i in range(n):
# #         cx = start_x + i * (icon_size_pt + gap_pt)
# #         positions.append((float(round(cx, 2)), float(round(row_y, 2))))

# #     return positions, float(icon_size_pt), float(gap_pt), float(row_y)

# def split_evenly(n, rows):
#     base = n // rows
#     extra = n % rows
#     return [base + (1 if i < extra else 0) for i in range(rows)]


# def get_svg_cells(n):
#     """
#     Returns a list of square cells:
#         [(cx, cy, cell_w, cell_h), ...]

#     Layout rules:
#     - 1 row if icons are few
#     - 2 centered rows if icons are many
#     - rows are centered independently
#     - all icons get a square slot with padding
#     """
#     if n <= 0:
#         return []

#     left = ICON_BOX_LEFT
#     right = page_width - ICON_BOX_RIGHT_MARGIN
#     top = ICON_BOX_TOP
#     bottom = ICON_BOX_BOTTOM

#     usable_w = right - left
#     usable_h = top - bottom

#     if usable_w <= 0 or usable_h <= 0:
#         raise ValueError("Invalid icon box. Check ICON_BOX_LEFT/RIGHT/TOP/BOTTOM values.")

#     # Cleaner rule:
#     # few icons -> single row
#     # many icons -> 2 rows
#     rows = 1 if n <= 8 else min(MAX_ICON_ROWS, 2)

#     row_counts = split_evenly(n, rows)
#     row_h = (usable_h - (rows - 1) * ICON_ROW_GAP_PT) / rows

#     cells = []
#     current_top = top

#     for count in row_counts:
#         current_bottom = current_top - row_h

#         if count == 1:
#             slot_w = usable_w
#         else:
#             slot_w = (usable_w - (count - 1) * ICON_COL_GAP_PT) / count

#         # Keep square slots so icons remain visually consistent
#         side = min(slot_w, row_h)

#         row_total_w = count * side + (count - 1) * ICON_COL_GAP_PT
#         start_x = left + (usable_w - row_total_w) / 2.0
#         cy = (current_top + current_bottom) / 2.0

#         for i in range(count):
#             cx = start_x + (side / 2.0) + i * (side + ICON_COL_GAP_PT)
#             cells.append((
#                 float(round(cx, 2)),
#                 float(round(cy, 2)),
#                 float(round(side, 2)),
#                 float(round(side, 2)),
#             ))

#         current_top = current_bottom - ICON_ROW_GAP_PT

#     return cells


# # def draw_svg_centered(can, svg_path, center_x, center_y, box_w, box_h, gray_out=False):
# #     """
# #     Draw SVG centered at (center_x, center_y) using canvas transforms.
# #     This avoids mutating the SVG geometry and is safer for many small icons.
# #     """
# #     if not os.path.exists(svg_path):
# #         return

# #     drawing = svg2rlg(svg_path)
# #     if drawing is None:
# #         return

# #     if gray_out:
# #         change_color_to_gray(drawing)

# #     x0, y0, x1, y1 = drawing.getBounds()
# #     content_w = max(1.0, float(x1 - x0))
# #     content_h = max(1.0, float(y1 - y0))

# #     # Uniform scaling -> preserves shape
# #     scale = min(
# #         (box_w * INNER_PAD_RATIO) / content_w,
# #         (box_h * INNER_PAD_RATIO) / content_h
# #     )

# #     can.saveState()
# #     can.translate(center_x, center_y)
# #     can.scale(scale, scale)
# #     can.translate(-((x0 + x1) / 2.0), -((y0 + y1) / 2.0))
# #     renderPDF.draw(drawing, can, 0, 0)
# #     can.restoreState()

# def draw_svg_centered(can, svg_path, center_x, center_y, box_w, box_h, gray_out=False):
#     """
#     Draw one SVG into a square cell without distorting it.
#     """
#     if not os.path.exists(svg_path):
#         return

#     drawing = svg2rlg(svg_path)
#     if drawing is None:
#         return

#     drawing = deepcopy(drawing)

#     if gray_out:
#         change_color_to_gray(drawing)

#     x0, y0, x1, y1 = drawing.getBounds()
#     content_w = float(x1 - x0)
#     content_h = float(y1 - y0)

#     if content_w <= 0 or content_h <= 0:
#         return

#     # Uniform scale only
#     scale = min(
#         (box_w * ICON_INNER_PAD_RATIO) / content_w,
#         (box_h * ICON_INNER_PAD_RATIO) / content_h
#     )

#     final_w = content_w * scale
#     final_h = content_h * scale

#     render_x = center_x - (final_w / 2.0)
#     render_y = center_y - (final_h / 2.0)

#     can.saveState()
#     can.translate(render_x, render_y)
#     can.scale(scale, scale)
#     can.translate(-x0, -y0)
#     renderPDF.draw(drawing, can, 0, 0)
#     can.restoreState()

# def build_svg_lookup(svg_dir):
#     """
#     Scan SVG_DIR and build:
#     {
#         '333': '/full/path/333.svg',
#         '222': '/full/path/222.svg',
#         '333oh': '/full/path/333oh.svg',
#         ...
#     }
#     """
#     if not os.path.isdir(svg_dir):
#         raise FileNotFoundError(f"SVG folder not found: {svg_dir}")

#     svg_map = {}
#     for root, _, files in os.walk(svg_dir):
#         for file in files:
#             if file.lower().endswith(".svg"):
#                 stem = os.path.splitext(file)[0]
#                 key = normalize_key(stem)
#                 full_path = os.path.join(root, file)
#                 svg_map[key] = full_path
#     return svg_map


# def find_event_columns_and_svgs(df, svg_dir):
#     """
#     Looks at all columns after 'Name' and keeps only those whose header
#     matches an available SVG file.
#     """
#     cols = [str(c).strip() for c in df.columns]
#     df.columns = cols

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     name_idx = list(df.columns).index('Name')
#     candidate_cols = df.columns[name_idx + 1:]

#     svg_lookup = build_svg_lookup(svg_dir)

#     event_cols = []
#     svg_paths = []
#     missing_headers = []

#     for col in candidate_cols:
#         col_clean = str(col).strip()
#         if not col_clean:
#             continue

#         key = normalize_key(col_clean)

#         if key in svg_lookup:
#             event_cols.append(col)
#             svg_paths.append(svg_lookup[key])
#         else:
#             missing_headers.append(col)

#     if not event_cols:
#         raise RuntimeError(
#             "No event columns were matched with SVG files.\n"
#             f"Check your Excel headers and SVG names in: {svg_dir}"
#         )

#     if missing_headers:
#         print(
#             "[INFO] Ignored columns after 'Name' because no matching SVG was found:",
#             ", ".join(map(str, missing_headers))
#         )

#     return event_cols, svg_paths


# # -----------------------------
# # COLOR UTILITY
# # -----------------------------
# def change_color_to_gray(drawing):
#     light_gray = Color(0.7294117647, 0.6862745098, 0.6274509804)

#     def _recurse(el):
#         if hasattr(el, 'fillColor') and getattr(el, 'fillColor'):
#             el.fillColor = light_gray
#         if hasattr(el, 'strokeColor') and getattr(el, 'strokeColor'):
#             el.strokeColor = light_gray
#         if isinstance(el, (Path, String)):
#             el.fillColor = light_gray
#             try:
#                 el.strokeColor = light_gray
#             except Exception:
#                 pass
#         if hasattr(el, 'contents') and el.contents:
#             for child in el.contents:
#                 _recurse(child)

#     _recurse(drawing)


# # -----------------------------
# # OVERLAY BUILDER
# # -----------------------------
# # def draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, icon_size_pt, page_w, page_h):
# def draw_overlay_pdf(name, transparency_values, svg_paths, svg_cells, page_w, page_h):
#     name = ("" if pd.isna(name) else str(name)).strip().upper()

#     packet = io.BytesIO()
#     can = canvas.Canvas(packet, pagesize=(page_w, page_h))

#     # Fit name width
#     max_width = page_w - 200
#     font_size = 45.3
#     while can.stringWidth(name, "HammersmithOne", font_size) > max_width and font_size > 10:
#         font_size -= 1.0

#     can.setFont("HammersmithOne", font_size)

#     fill_color = HexColor("#000000")
#     can.setFillColor(fill_color)
#     can.setStrokeColor(fill_color)

#     # Draw participant name
#     char_spacing = 6
#     text_width = can.stringWidth(name, "HammersmithOne", font_size) + max(0, len(name) - 1) * char_spacing
#     x = (page_w - text_width) / 2.0
#     y = 350

#     can.saveState()
#     can.setLineWidth(1.2)

#     txt = can.beginText()
#     txt.setTextOrigin(x, y)
#     txt.setFont("HammersmithOne", font_size)

#     try:
#         txt.setCharSpace(char_spacing)
#     except Exception:
#         pass

#     try:
#         txt.setTextRenderMode(2)
#     except Exception:
#         try:
#             can._code.append("2 Tr")
#         except Exception:
#             pass

#     txt.textLine(name)
#     can.drawText(txt)
#     can.restoreState()

#     # # Event SVGs
#     # for idx, (svg_path, (svg_cx, svg_cy), transparency) in enumerate(
#     #     zip(svg_paths, svg_positions, transparency_values)
#     # ):
#     #     try:
#     #         try:
#     #             tval = int(float(transparency)) if (transparency is not None and str(transparency) != "nan") else 0
#     #         except Exception:
#     #             tval = 0

#     #         draw_svg_centered(
#     #             can=can,
#     #             svg_path=svg_path,
#     #             center_x=svg_cx,
#     #             center_y=svg_cy,
#     #             box_w=icon_size_pt,
#     #             box_h=icon_size_pt,
#     #             gray_out=(tval == 0)
#     #         )

#     #     except Exception as e:
#     #         print(
#     #             f"[WARN] SVG render failed (row name={name}, idx={idx}, path={svg_path}): {e}",
#     #             file=sys.stderr
#     #         )
#     #         continue

#     # Event SVGs
#     for idx, (svg_path, (svg_cx, svg_cy, cell_w, cell_h), transparency) in enumerate(
#         zip(svg_paths, svg_cells, transparency_values)
#     ):
#         try:
#             try:
#                 tval = int(float(transparency)) if (transparency is not None and str(transparency) != "nan") else 0
#             except Exception:
#                 tval = 0

#             draw_svg_centered(
#                 can=can,
#                 svg_path=svg_path,
#                 center_x=svg_cx,
#                 center_y=svg_cy,
#                 box_w=cell_w,
#                 box_h=cell_h,
#                 gray_out=(tval == 0)
#             )

#         except Exception as e:
#             print(
#                 f"[WARN] SVG render failed (row name={name}, idx={idx}, path={svg_path}): {e}",
#                 file=sys.stderr
#             )
#             continue

#     can.save()
#     packet.seek(0)
#     return packet


# # -----------------------------
# # PAGE CREATOR
# # -----------------------------
# # def create_certificate_page_bytes(name, transparency_values, svg_paths, svg_positions, icon_size_pt):
# def create_certificate_page_bytes(name, transparency_values, svg_paths, svg_cells):
#     # overlay_stream = draw_overlay_pdf(
#     #     name,
#     #     transparency_values,
#     #     svg_paths,
#     #     svg_positions,
#     #     icon_size_pt,
#     #     page_width,
#     #     page_height
#     # )
#     overlay_stream = draw_overlay_pdf(
#         name,
#         transparency_values,
#         svg_paths,
#         svg_cells,
#         page_width,
#         page_height
#     )

#     overlay_reader = PdfReader(overlay_stream)

#     base_reader = PdfReader(io.BytesIO(TEMPLATE_BYTES))
#     page = base_reader.pages[0]
#     page.merge_page(overlay_reader.pages[0])

#     out_buf = io.BytesIO()
#     one = PdfWriter()
#     one.add_page(page)
#     one.write(out_buf)
#     out_buf.seek(0)
#     return out_buf.getvalue()


# # -----------------------------
# # MAIN
# # -----------------------------
# def main():
#     if not os.path.exists(EXCEL_FILE):
#         raise FileNotFoundError(f"Excel file not found at: {EXCEL_FILE}")

#     try:
#         df = pd.read_excel(EXCEL_FILE)
#     except Exception as e:
#         raise RuntimeError(
#             f"Failed to read '{EXCEL_FILE}'. If it's .xlsx, ensure 'openpyxl' is installed. Original error: {e}"
#         )

#     # Normalize headers
#     df.columns = [str(c).strip() for c in df.columns]

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     # Clean names
#     df['Name'] = df['Name'].astype(object)
#     df['Name'] = df['Name'].ffill()
#     df['Name'] = df['Name'].apply(lambda x: "" if pd.isna(x) else str(x).strip())

#     # Drop fully blank rows except Name
#     if df.shape[1] > 1:
#         non_name_cols = [c for c in df.columns if c != 'Name']

#         def _row_all_blank(s):
#             return all((pd.isna(v) or str(v).strip() == "") for v in s)

#         mask_keep = ~df[non_name_cols].apply(_row_all_blank, axis=1)
#         df = df[mask_keep].copy()

#     if DEDUP_BY_NAME:
#         before = len(df)
#         df = df.drop_duplicates(subset=['Name']).reset_index(drop=True)
#         after = len(df)
#         print(f"[INFO] Dedup by name: {before} -> {after}")

#     # Detect event columns + SVGs
#     event_cols, svg_paths = find_event_columns_and_svgs(df, SVG_DIR)
#     # svg_positions, icon_size_pt, gap_pt, row_y = get_svg_positions_one_row(len(svg_paths))
#     svg_cells = get_svg_cells(len(svg_paths))
#     print("[INFO] Detected event columns:", event_cols)
#     print("[INFO] Matching SVG files:")
#     for col, path in zip(event_cols, svg_paths):
#         print(f"   {col} -> {path}")

#     # print(f"[INFO] Computed icon size: {round(icon_size_pt, 2)} pt")
#     # print(f"[INFO] Computed icon gap: {round(gap_pt, 2)} pt")
#     # print(f"[INFO] Computed row Y: {round(row_y, 2)}")
#     # print(f"[INFO] Total events: {len(svg_paths)}")

#     print("[INFO] Detected event columns:", event_cols)
#     print("[INFO] Matching SVG files:")
#     for col, path in zip(event_cols, svg_paths):
#         print(f"   {col} -> {path}")

#     print(f"[INFO] Total events: {len(svg_paths)}")
#     print(f"[INFO] Icon box: left={ICON_BOX_LEFT}, right={page_width - ICON_BOX_RIGHT_MARGIN}, top={ICON_BOX_TOP}, bottom={ICON_BOX_BOTTOM}")
#     print(f"[INFO] Computed cells: {len(svg_cells)}")

#     # Convert event flags to 0/1
#     def _to_flag(x):
#         if pd.isna(x):
#             return 0
#         s = str(x).strip().lower()
#         if s in {"1", "true", "yes", "y", "✓", "check", "x"}:
#             return 1
#         try:
#             return 1 if int(float(s)) != 0 else 0
#         except Exception:
#             return 0

#     df_events = df[event_cols].copy()
#     for col in event_cols:
#         df_events[col] = df_events[col].map(_to_flag)

#     total = len(df)
#     print(f"[INFO] Preparing {total} certificate(s)...")

#     merger = PdfMerger()
#     rendered_count = 0

#     for idx in range(total):
#         row = df.iloc[idx]
#         safe_name = "" if pd.isna(row['Name']) else str(row['Name']).strip()
#         if not safe_name:
#             safe_name = "PARTICIPANT"

#         flags = df_events.iloc[idx].tolist()

#         try:
#             # page_bytes = create_certificate_page_bytes(
#             #     safe_name,
#             #     flags,
#             #     svg_paths,
#             #     svg_positions,
#             #     icon_size_pt
#             # )
#             page_bytes = create_certificate_page_bytes(
#                 safe_name,
#                 flags,
#                 svg_paths,
#                 svg_cells
#             )
#             merger.append(io.BytesIO(page_bytes))
#             rendered_count += 1
#             print(f"  - [{idx + 1}/{total}] {safe_name}")
#         except Exception as e:
#             print(f"[ERROR] Failed for row {idx + 1} (name={safe_name}): {e}", file=sys.stderr)
#             continue

#     if rendered_count == 0:
#         merger.close()
#         raise RuntimeError("No pages were rendered. Check your Excel and SVG paths.")

#     with open(OUTPUT_FILE, "wb") as out:
#         merger.write(out)
#     merger.close()

#     print(f"Combined certificates created successfully! Pages written: {rendered_count}/{total} -> {OUTPUT_FILE}")


# if __name__ == "__main__":
#     main()






# import io
# import os
# import sys
# import pandas as pd

# # PDF / drawing libs
# from reportlab.pdfgen import canvas
# from reportlab.pdfbase.ttfonts import TTFont
# from reportlab.pdfbase import pdfmetrics
# from reportlab.lib.colors import HexColor, Color
# from reportlab.graphics import renderPDF
# from reportlab.graphics.shapes import Path, String
# from svglib.svglib import svg2rlg

# # PDF readers/writers
# try:
#     from pypdf import PdfReader, PdfWriter, PdfMerger
# except Exception:
#     from PyPDF2 import PdfReader, PdfWriter, PdfMerger


# # -----------------------------
# # CONFIG / PATHS
# # -----------------------------
# TEMPLATE_PATH = r'./templates_and_suggested_fonts/Template2/template.pdf'
# EXCEL_FILE = 'comp.xlsx'
# OUTPUT_FILE = 'newcertis.pdf'
# FONT_PATH = r'./Hammersmith_One16/HammersmithOne-Regular.ttf'

# SVG_DIR = r'./events'
# DEDUP_BY_NAME = False


# # -----------------------------
# # ICON LAYOUT CONFIG
# # -----------------------------
# ICON_MM = 13
# MAX_ICON_SIZE_PT = ICON_MM * (72 / 25.4)

# # Lower bound only as a last-resort safety
# MIN_ICON_MM = 4.2
# MIN_ICON_SIZE_PT = MIN_ICON_MM * (72 / 25.4)

# # Small fixed padding on both sides
# SIDE_PADDING_PT = 18

# # Top row Y
# BASE_ICON_ROW_Y = 748

# # Draw slightly inside the slot so icons don't touch each other visually
# INNER_PAD_RATIO = 0.90

# # Gap tuning
# MIN_GAP_PT = 1.0
# MAX_GAP_PT = 10.0


# # -----------------------------
# # READ TEMPLATE + PAGE SIZE + FONT
# # -----------------------------
# if not os.path.exists(TEMPLATE_PATH):
#     raise FileNotFoundError(f"Template PDF not found at: {TEMPLATE_PATH}")

# with open(TEMPLATE_PATH, 'rb') as _tf:
#     TEMPLATE_BYTES = _tf.read()

# tmp_reader_for_size = PdfReader(io.BytesIO(TEMPLATE_BYTES))
# base_page = tmp_reader_for_size.pages[0]
# page_width = float(base_page.mediabox.width)
# page_height = float(base_page.mediabox.height)

# if not os.path.exists(FONT_PATH):
#     raise FileNotFoundError(f"Font not found at: {FONT_PATH}")

# pdfmetrics.registerFont(TTFont('HammersmithOne', FONT_PATH))


# # -----------------------------
# # HELPERS
# # -----------------------------
# def normalize_key(s: str) -> str:
#     """
#     Normalize Excel headers / filenames so matching is robust.
#     Example:
#     '333 OH' -> '333oh'
#     '333_oh' -> '333oh'
#     '333-oh' -> '333oh'
#     """
#     if s is None:
#         return ""
#     s = str(s).strip().lower()
#     for ch in [" ", "_", "-", "."]:
#         s = s.replace(ch, "")
#     return s


# def get_svg_positions_one_row(n):
#     """
#     Always keep all icons in ONE ROW.

#     Layout rules:
#     - uses full width between small fixed left/right paddings
#     - if events are fewer -> icons grow
#     - if events are more -> gap shrinks first, then icon size
#     - positions start after left padding and end before right padding

#     Returns:
#         positions, icon_size_pt, gap_pt, row_y
#     """
#     if n <= 0:
#         return [], MAX_ICON_SIZE_PT, 0.0, BASE_ICON_ROW_Y

#     usable_width = page_width - (2 * SIDE_PADDING_PT)
#     row_y = BASE_ICON_ROW_Y

#     if n == 1:
#         return [(page_width / 2.0, row_y)], MAX_ICON_SIZE_PT, 0.0, row_y

#     # Dynamic preferred gap based on event count
#     # Fewer icons -> bigger gap
#     # More icons -> smaller gap
#     if n <= 4:
#         preferred_gap = 10.0
#     elif n <= 6:
#         preferred_gap = 8.0
#     elif n <= 8:
#         preferred_gap = 6.0
#     elif n <= 10:
#         preferred_gap = 4.5
#     elif n <= 12:
#         preferred_gap = 3.5
#     elif n <= 15:
#         preferred_gap = 3.4
#     else:
#         preferred_gap = 3.6

#     gap_pt = max(MIN_GAP_PT, min(MAX_GAP_PT, preferred_gap))

#     # First compute icon size from usable width
#     icon_size_pt = (usable_width - (n - 1) * gap_pt) / n

#     # If icons are too large, cap size and spread extra width into the gaps
#     if icon_size_pt > MAX_ICON_SIZE_PT:
#         icon_size_pt = MAX_ICON_SIZE_PT
#         gap_pt = (usable_width - n * icon_size_pt) / (n - 1)
#         gap_pt = max(MIN_GAP_PT, min(gap_pt, MAX_GAP_PT))

#     # If gap got clamped and there is still extra width, give it back to icon size
#     total_width = n * icon_size_pt + (n - 1) * gap_pt
#     if total_width < usable_width:
#         extra = usable_width - total_width
#         grow_per_icon = extra / n
#         icon_size_pt = min(MAX_ICON_SIZE_PT, icon_size_pt + grow_per_icon)
#         total_width = n * icon_size_pt + (n - 1) * gap_pt

#     # If row is still too wide, reduce gap first
#     if total_width > usable_width:
#         gap_pt = max(MIN_GAP_PT, (usable_width - n * icon_size_pt) / (n - 1))
#         total_width = n * icon_size_pt + (n - 1) * gap_pt

#     # If still too wide, reduce icon size
#     if total_width > usable_width:
#         icon_size_pt = (usable_width - (n - 1) * gap_pt) / n
#         total_width = n * icon_size_pt + (n - 1) * gap_pt

#     # Final safety lower bound
#     if icon_size_pt < MIN_ICON_SIZE_PT:
#         icon_size_pt = MIN_ICON_SIZE_PT
#         gap_pt = (usable_width - n * icon_size_pt) / (n - 1)
#         if gap_pt < 0:
#             gap_pt = 0.0
#             icon_size_pt = usable_width / n
#         total_width = n * icon_size_pt + (n - 1) * gap_pt

#     # IMPORTANT:
#     # Start exactly after left padding, not re-centered to a narrower width
#     start_x = SIDE_PADDING_PT + (icon_size_pt / 2.0)

#     positions = []
#     for i in range(n):
#         cx = start_x + i * (icon_size_pt + gap_pt)
#         positions.append((float(round(cx, 2)), float(round(row_y, 2))))

#     return positions, float(icon_size_pt), float(gap_pt), float(row_y)


# def draw_svg_centered(can, svg_path, center_x, center_y, box_w, box_h, gray_out=False):
#     """
#     Draw SVG centered at (center_x, center_y) using canvas transforms.
#     This avoids mutating the SVG geometry and is safer for many small icons.
#     """
#     if not os.path.exists(svg_path):
#         return

#     drawing = svg2rlg(svg_path)
#     if drawing is None:
#         return

#     if gray_out:
#         change_color_to_gray(drawing)

#     x0, y0, x1, y1 = drawing.getBounds()
#     content_w = max(1.0, float(x1 - x0))
#     content_h = max(1.0, float(y1 - y0))

#     # Uniform scaling -> preserves shape
#     scale = min(
#         (box_w * INNER_PAD_RATIO) / content_w,
#         (box_h * INNER_PAD_RATIO) / content_h
#     )

#     can.saveState()
#     can.translate(center_x, center_y)
#     can.scale(scale, scale)
#     can.translate(-((x0 + x1) / 2.0), -((y0 + y1) / 2.0))
#     renderPDF.draw(drawing, can, 0, 0)
#     can.restoreState()


# def build_svg_lookup(svg_dir):
#     """
#     Scan SVG_DIR and build:
#     {
#         '333': '/full/path/333.svg',
#         '222': '/full/path/222.svg',
#         '333oh': '/full/path/333oh.svg',
#         ...
#     }
#     """
#     if not os.path.isdir(svg_dir):
#         raise FileNotFoundError(f"SVG folder not found: {svg_dir}")

#     svg_map = {}
#     for root, _, files in os.walk(svg_dir):
#         for file in files:
#             if file.lower().endswith(".svg"):
#                 stem = os.path.splitext(file)[0]
#                 key = normalize_key(stem)
#                 full_path = os.path.join(root, file)
#                 svg_map[key] = full_path
#     return svg_map


# def find_event_columns_and_svgs(df, svg_dir):
#     """
#     Looks at all columns after 'Name' and keeps only those whose header
#     matches an available SVG file.
#     """
#     cols = [str(c).strip() for c in df.columns]
#     df.columns = cols

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     name_idx = list(df.columns).index('Name')
#     candidate_cols = df.columns[name_idx + 1:]

#     svg_lookup = build_svg_lookup(svg_dir)

#     event_cols = []
#     svg_paths = []
#     missing_headers = []

#     for col in candidate_cols:
#         col_clean = str(col).strip()
#         if not col_clean:
#             continue

#         key = normalize_key(col_clean)

#         if key in svg_lookup:
#             event_cols.append(col)
#             svg_paths.append(svg_lookup[key])
#         else:
#             missing_headers.append(col)

#     if not event_cols:
#         raise RuntimeError(
#             "No event columns were matched with SVG files.\n"
#             f"Check your Excel headers and SVG names in: {svg_dir}"
#         )

#     if missing_headers:
#         print(
#             "[INFO] Ignored columns after 'Name' because no matching SVG was found:",
#             ", ".join(map(str, missing_headers))
#         )

#     return event_cols, svg_paths


# # -----------------------------
# # COLOR UTILITY
# # -----------------------------
# def change_color_to_gray(drawing):
#     light_gray = Color(0.7294117647, 0.6862745098, 0.6274509804)

#     def _recurse(el):
#         if hasattr(el, 'fillColor') and getattr(el, 'fillColor'):
#             el.fillColor = light_gray
#         if hasattr(el, 'strokeColor') and getattr(el, 'strokeColor'):
#             el.strokeColor = light_gray
#         if isinstance(el, (Path, String)):
#             el.fillColor = light_gray
#             try:
#                 el.strokeColor = light_gray
#             except Exception:
#                 pass
#         if hasattr(el, 'contents') and el.contents:
#             for child in el.contents:
#                 _recurse(child)

#     _recurse(drawing)


# # -----------------------------
# # OVERLAY BUILDER
# # -----------------------------
# def draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, icon_size_pt, page_w, page_h):
#     name = ("" if pd.isna(name) else str(name)).strip().upper()

#     packet = io.BytesIO()
#     can = canvas.Canvas(packet, pagesize=(page_w, page_h))

#     # Fit name width
#     max_width = page_w - 200
#     font_size = 45.3
#     while can.stringWidth(name, "HammersmithOne", font_size) > max_width and font_size > 10:
#         font_size -= 1.0

#     can.setFont("HammersmithOne", font_size)

#     fill_color = HexColor("#000000")
#     can.setFillColor(fill_color)
#     can.setStrokeColor(fill_color)

#     # Draw participant name
#     char_spacing = 6
#     text_width = can.stringWidth(name, "HammersmithOne", font_size) + max(0, len(name) - 1) * char_spacing
#     x = (page_w - text_width) / 2.0
#     y = 350

#     can.saveState()
#     can.setLineWidth(1.2)

#     txt = can.beginText()
#     txt.setTextOrigin(x, y)
#     txt.setFont("HammersmithOne", font_size)

#     try:
#         txt.setCharSpace(char_spacing)
#     except Exception:
#         pass

#     try:
#         txt.setTextRenderMode(2)
#     except Exception:
#         try:
#             can._code.append("2 Tr")
#         except Exception:
#             pass

#     txt.textLine(name)
#     can.drawText(txt)
#     can.restoreState()

#     # Event SVGs
#     for idx, (svg_path, (svg_cx, svg_cy), transparency) in enumerate(
#         zip(svg_paths, svg_positions, transparency_values)
#     ):
#         try:
#             try:
#                 tval = int(float(transparency)) if (transparency is not None and str(transparency) != "nan") else 0
#             except Exception:
#                 tval = 0

#             draw_svg_centered(
#                 can=can,
#                 svg_path=svg_path,
#                 center_x=svg_cx,
#                 center_y=svg_cy,
#                 box_w=icon_size_pt,
#                 box_h=icon_size_pt,
#                 gray_out=(tval == 0)
#             )

#         except Exception as e:
#             print(
#                 f"[WARN] SVG render failed (row name={name}, idx={idx}, path={svg_path}): {e}",
#                 file=sys.stderr
#             )
#             continue

#     can.save()
#     packet.seek(0)
#     return packet


# # -----------------------------
# # PAGE CREATOR
# # -----------------------------
# def create_certificate_page_bytes(name, transparency_values, svg_paths, svg_positions, icon_size_pt):
#     overlay_stream = draw_overlay_pdf(
#         name,
#         transparency_values,
#         svg_paths,
#         svg_positions,
#         icon_size_pt,
#         page_width,
#         page_height
#     )
#     overlay_reader = PdfReader(overlay_stream)

#     base_reader = PdfReader(io.BytesIO(TEMPLATE_BYTES))
#     page = base_reader.pages[0]
#     page.merge_page(overlay_reader.pages[0])

#     out_buf = io.BytesIO()
#     one = PdfWriter()
#     one.add_page(page)
#     one.write(out_buf)
#     out_buf.seek(0)
#     return out_buf.getvalue()


# # -----------------------------
# # MAIN
# # -----------------------------
# def main():
#     if not os.path.exists(EXCEL_FILE):
#         raise FileNotFoundError(f"Excel file not found at: {EXCEL_FILE}")

#     try:
#         df = pd.read_excel(EXCEL_FILE)
#     except Exception as e:
#         raise RuntimeError(
#             f"Failed to read '{EXCEL_FILE}'. If it's .xlsx, ensure 'openpyxl' is installed. Original error: {e}"
#         )

#     # Normalize headers
#     df.columns = [str(c).strip() for c in df.columns]

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     # Clean names
#     df['Name'] = df['Name'].astype(object)
#     df['Name'] = df['Name'].ffill()
#     df['Name'] = df['Name'].apply(lambda x: "" if pd.isna(x) else str(x).strip())

#     # Drop fully blank rows except Name
#     if df.shape[1] > 1:
#         non_name_cols = [c for c in df.columns if c != 'Name']

#         def _row_all_blank(s):
#             return all((pd.isna(v) or str(v).strip() == "") for v in s)

#         mask_keep = ~df[non_name_cols].apply(_row_all_blank, axis=1)
#         df = df[mask_keep].copy()

#     if DEDUP_BY_NAME:
#         before = len(df)
#         df = df.drop_duplicates(subset=['Name']).reset_index(drop=True)
#         after = len(df)
#         print(f"[INFO] Dedup by name: {before} -> {after}")

#     # Detect event columns + SVGs
#     event_cols, svg_paths = find_event_columns_and_svgs(df, SVG_DIR)
#     svg_positions, icon_size_pt, gap_pt, row_y = get_svg_positions_one_row(len(svg_paths))

#     print("[INFO] Detected event columns:", event_cols)
#     print("[INFO] Matching SVG files:")
#     for col, path in zip(event_cols, svg_paths):
#         print(f"   {col} -> {path}")

#     print(f"[INFO] Computed icon size: {round(icon_size_pt, 2)} pt")
#     print(f"[INFO] Computed icon gap: {round(gap_pt, 2)} pt")
#     print(f"[INFO] Computed row Y: {round(row_y, 2)}")
#     print(f"[INFO] Total events: {len(svg_paths)}")

#     # Convert event flags to 0/1
#     def _to_flag(x):
#         if pd.isna(x):
#             return 0
#         s = str(x).strip().lower()
#         if s in {"1", "true", "yes", "y", "✓", "check", "x"}:
#             return 1
#         try:
#             return 1 if int(float(s)) != 0 else 0
#         except Exception:
#             return 0

#     df_events = df[event_cols].copy()
#     for col in event_cols:
#         df_events[col] = df_events[col].map(_to_flag)

#     total = len(df)
#     print(f"[INFO] Preparing {total} certificate(s)...")

#     merger = PdfMerger()
#     rendered_count = 0

#     for idx in range(total):
#         row = df.iloc[idx]
#         safe_name = "" if pd.isna(row['Name']) else str(row['Name']).strip()
#         if not safe_name:
#             safe_name = "PARTICIPANT"

#         flags = df_events.iloc[idx].tolist()

#         try:
#             page_bytes = create_certificate_page_bytes(
#                 safe_name,
#                 flags,
#                 svg_paths,
#                 svg_positions,
#                 icon_size_pt
#             )
#             merger.append(io.BytesIO(page_bytes))
#             rendered_count += 1
#             print(f"  - [{idx + 1}/{total}] {safe_name}")
#         except Exception as e:
#             print(f"[ERROR] Failed for row {idx + 1} (name={safe_name}): {e}", file=sys.stderr)
#             continue

#     if rendered_count == 0:
#         merger.close()
#         raise RuntimeError("No pages were rendered. Check your Excel and SVG paths.")

#     with open(OUTPUT_FILE, "wb") as out:
#         merger.write(out)
#     merger.close()

#     print(f"Combined certificates created successfully! Pages written: {rendered_count}/{total} -> {OUTPUT_FILE}")


# if __name__ == "__main__":
#     main()



# import io
# import os
# import sys
# import math
# import pandas as pd

# # PDF / drawing libs
# from reportlab.pdfgen import canvas
# from reportlab.pdfbase.ttfonts import TTFont
# from reportlab.pdfbase import pdfmetrics
# from reportlab.lib.colors import HexColor, Color
# from reportlab.graphics import renderPDF
# from reportlab.graphics.shapes import Path, String
# from svglib.svglib import svg2rlg

# # PDF readers/writers
# try:
#     from pypdf import PdfReader, PdfWriter, PdfMerger
# except Exception:
#     from PyPDF2 import PdfReader, PdfWriter, PdfMerger


# # -----------------------------
# # CONFIG / PATHS
# # -----------------------------
# TEMPLATE_PATH = r'./templates_and_suggested_fonts/Template2/template.pdf'
# EXCEL_FILE = 'comp.xlsx'
# OUTPUT_FILE = 'newcertis.pdf'
# FONT_PATH = r'./Hammersmith_One16/HammersmithOne-Regular.ttf'

# SVG_DIR = r'./events'
# DEDUP_BY_NAME = False


# # -----------------------------
# # READ TEMPLATE + PAGE SIZE + FONT
# # -----------------------------
# if not os.path.exists(TEMPLATE_PATH):
#     raise FileNotFoundError(f"Template PDF not found at: {TEMPLATE_PATH}")

# with open(TEMPLATE_PATH, 'rb') as _tf:
#     TEMPLATE_BYTES = _tf.read()

# tmp_reader_for_size = PdfReader(io.BytesIO(TEMPLATE_BYTES))
# base_page = tmp_reader_for_size.pages[0]
# page_width = float(base_page.mediabox.width)
# page_height = float(base_page.mediabox.height)

# if not os.path.exists(FONT_PATH):
#     raise FileNotFoundError(f"Font not found at: {FONT_PATH}")

# pdfmetrics.registerFont(TTFont('HammersmithOne', FONT_PATH))


# # -----------------------------
# # MANUAL ICON LAYOUT CONFIG
# # -----------------------------
# # This is your dedicated editable space.
# #
# # For each number of events (1 to 17), you can control:
# #   start_x        -> x position of first icon center
# #   start_y        -> y position of first row center
# #   icon_size      -> size box for each icon
# #   gap_x          -> horizontal gap between icons
# #   gap_y          -> vertical gap between rows
# #   rows           -> how many rows
# #   per_row        -> list of icons in each row
# #   inner_pad_ratio-> padding INSIDE each icon box
# #   left_pad       -> informational / for your own tuning
# #   right_pad      -> informational / for your own tuning
# #
# # Example:
# #   5 events with rows=1 and per_row=[5] => one row of 5 icons
# #   10 events with rows=2 and per_row=[5,5] => two rows of 5 icons each
# #   11 events with rows=2 and per_row=[6,5] => first row 6, second row 5
# #
# # IMPORTANT:
# # start_x and start_y refer to the FIRST icon center of the FIRST row.

# ICON_LAYOUTS = {
#     1:  {"start_x": 33.0, "start_y": 736, "icon_size": 42.0, "gap_x": 22, "gap_y": 0, "rows": 1, "per_row": [1],  "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     2:  {"start_x": 33.0, "start_y": 736, "icon_size": 42.0, "gap_x": 22, "gap_y": 0, "rows": 1, "per_row": [2],  "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     3:  {"start_x": 33.0, "start_y": 736, "icon_size": 42.0, "gap_x": 22, "gap_y": 0, "rows": 1, "per_row": [3],  "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     4:  {"start_x": 33.0, "start_y": 736, "icon_size": 42.0, "gap_x": 16, "gap_y": 0, "rows": 1, "per_row": [4],  "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     5:  {"start_x": 33.0, "start_y": 736, "icon_size": 42.0, "gap_x": 16, "gap_y": 0, "rows": 1, "per_row": [5],  "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     6:  {"start_x": 33.0, "start_y": 736, "icon_size": 42.0, "gap_x": 10, "gap_y": 0, "rows": 1, "per_row": [6],  "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     7:  {"start_x": 33.0, "start_y": 736, "icon_size": 42.0, "gap_x": 10, "gap_y": 0, "rows": 1, "per_row": [7],  "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     8:  {"start_x": 33.0, "start_y": 736, "icon_size": 42.0, "gap_x": 10, "gap_y": 0, "rows": 1, "per_row": [8],  "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     9:  {"start_x": 33.0, "start_y": 736, "icon_size": 42.0, "gap_x":  6, "gap_y": 0, "rows": 1, "per_row": [9],  "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     10: {"start_x": 33.0, "start_y": 736, "icon_size": 42.0, "gap_x":  6, "gap_y": 0, "rows": 1, "per_row": [10], "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     11: {"start_x": 33.0, "start_y": 736, "icon_size": 42.0, "gap_x":  6, "gap_y": 0, "rows": 1, "per_row": [11], "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     12: {"start_x": 33.0, "start_y": 736, "icon_size": 42.0, "gap_x":  6, "gap_y": 0, "rows": 1, "per_row": [12], "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     13: {"start_x": 32.1, "start_y": 736, "icon_size": 40.3, "gap_x":  4, "gap_y": 0, "rows": 1, "per_row": [13], "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     14: {"start_x": 30.6, "start_y": 736, "icon_size": 37.1, "gap_x":  4, "gap_y": 0, "rows": 1, "per_row": [14], "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     15: {"start_x": 29.2, "start_y": 736, "icon_size": 34.4, "gap_x":  4, "gap_y": 0, "rows": 1, "per_row": [15], "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     16: {"start_x": 28.0, "start_y": 736, "icon_size": 32.0, "gap_x":  4, "gap_y": 0, "rows": 1, "per_row": [16], "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
#     17: {"start_x": 26.9, "start_y": 736, "icon_size": 29.9, "gap_x":  4, "gap_y": 0, "rows": 1, "per_row": [17], "inner_pad_ratio": 0.84, "left_pad": 12, "right_pad": 12},
# }

# # Fallback if event count is somehow not present in ICON_LAYOUTS
# DEFAULT_ICON_LAYOUT = {
#     "start_x": 140,
#     "start_y": 744,
#     "icon_size": 22,
#     "gap_x": 6,
#     "gap_y": 17,
#     "rows": 2,
#     "per_row": [9, 8],
#     "inner_pad_ratio": 0.84,
#     "left_pad": 0,
#     "right_pad": 0,
# }


# # -----------------------------
# # HELPERS
# # -----------------------------
# def normalize_key(s: str) -> str:
#     """
#     Normalize Excel headers / filenames so matching is robust.
#     Example:
#     '333 OH' -> '333oh'
#     '333_oh' -> '333oh'
#     '333-oh' -> '333oh'
#     """
#     if s is None:
#         return ""
#     s = str(s).strip().lower()
#     for ch in [" ", "_", "-", "."]:
#         s = s.replace(ch, "")
#     return s


# def build_svg_lookup(svg_dir):
#     """
#     Scan SVG_DIR and build:
#     {
#         '333': '/full/path/333.svg',
#         '222': '/full/path/222.svg',
#         '333oh': '/full/path/333oh.svg',
#         ...
#     }
#     """
#     if not os.path.isdir(svg_dir):
#         raise FileNotFoundError(f"SVG folder not found: {svg_dir}")

#     svg_map = {}
#     for root, _, files in os.walk(svg_dir):
#         for file in files:
#             if file.lower().endswith(".svg"):
#                 stem = os.path.splitext(file)[0]
#                 key = normalize_key(stem)
#                 full_path = os.path.join(root, file)
#                 svg_map[key] = full_path
#     return svg_map


# def find_event_columns_and_svgs(df, svg_dir):
#     """
#     Looks at all columns after 'Name' and keeps only those whose header
#     matches an available SVG file.
#     """
#     cols = [str(c).strip() for c in df.columns]
#     df.columns = cols

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     name_idx = list(df.columns).index('Name')
#     candidate_cols = df.columns[name_idx + 1:]

#     svg_lookup = build_svg_lookup(svg_dir)

#     event_cols = []
#     svg_paths = []
#     missing_headers = []

#     for col in candidate_cols:
#         col_clean = str(col).strip()
#         if not col_clean:
#             continue

#         key = normalize_key(col_clean)

#         if key in svg_lookup:
#             event_cols.append(col)
#             svg_paths.append(svg_lookup[key])
#         else:
#             missing_headers.append(col)

#     if not event_cols:
#         raise RuntimeError(
#             "No event columns were matched with SVG files.\n"
#             f"Check your Excel headers and SVG names in: {svg_dir}"
#         )

#     if missing_headers:
#         print(
#             "[INFO] Ignored columns after 'Name' because no matching SVG was found:",
#             ", ".join(map(str, missing_headers))
#         )

#     return event_cols, svg_paths


# def get_layout_for_event_count(n):
#     """
#     Returns the dedicated manual layout for this event count.
#     """
#     if n in ICON_LAYOUTS:
#         layout = ICON_LAYOUTS[n].copy()
#     else:
#         layout = DEFAULT_ICON_LAYOUT.copy()

#     per_row = layout.get("per_row", [])
#     rows = int(layout.get("rows", 1))

#     if not per_row:
#         per_row = [n]
#         rows = 1

#     if sum(per_row) != n:
#         raise ValueError(
#             f"Invalid ICON_LAYOUTS for event count {n}. "
#             f"Sum of per_row={per_row} is {sum(per_row)} but expected {n}."
#         )

#     if len(per_row) != rows:
#         raise ValueError(
#             f"Invalid ICON_LAYOUTS for event count {n}. "
#             f"rows={rows} but per_row has length {len(per_row)}."
#         )

#     return layout


# def get_svg_layout_positions(n):
#     """
#     Build exact positions from the manual layout config.

#     Returns:
#         positions: list of (cx, cy)
#         icon_size_pt
#         inner_pad_ratio
#         gap_x
#         gap_y
#     """
#     if n <= 0:
#         return [], 24.0, 0.85, 0.0, 0.0

#     layout = get_layout_for_event_count(n)

#     start_x = float(layout["start_x"])
#     start_y = float(layout["start_y"])
#     icon_size = float(layout["icon_size"])
#     gap_x = float(layout["gap_x"])
#     gap_y = float(layout["gap_y"])
#     per_row = list(layout["per_row"])
#     inner_pad_ratio = float(layout.get("inner_pad_ratio", 0.85))

#     positions = []
#     current_y = start_y

#     for row_count in per_row:
#         current_x = start_x
#         for _ in range(row_count):
#             positions.append((round(current_x, 2), round(current_y, 2)))
#             current_x += icon_size + gap_x
#         current_y -= icon_size + gap_y

#     return positions, icon_size, inner_pad_ratio, gap_x, gap_y


# # -----------------------------
# # COLOR UTILITY
# # -----------------------------
# def change_color_to_gray(drawing):
#     light_gray = Color(0.7294117647, 0.6862745098, 0.6274509804)

#     def _recurse(el):
#         if hasattr(el, 'fillColor') and getattr(el, 'fillColor'):
#             el.fillColor = light_gray
#         if hasattr(el, 'strokeColor') and getattr(el, 'strokeColor'):
#             el.strokeColor = light_gray
#         if isinstance(el, (Path, String)):
#             el.fillColor = light_gray
#             try:
#                 el.strokeColor = light_gray
#             except Exception:
#                 pass
#         if hasattr(el, 'contents') and el.contents:
#             for child in el.contents:
#                 _recurse(child)

#     _recurse(drawing)


# # -----------------------------
# # SVG DRAWER
# # -----------------------------
# def draw_svg_centered(can, svg_path, center_x, center_y, box_w, box_h, inner_pad_ratio=0.85, gray_out=False):
#     """
#     Draw SVG centered at (center_x, center_y) with uniform scaling.
#     This keeps aspect ratio safe.
#     """
#     if not os.path.exists(svg_path):
#         return

#     drawing = svg2rlg(svg_path)
#     if drawing is None:
#         return

#     if gray_out:
#         change_color_to_gray(drawing)

#     x0, y0, x1, y1 = drawing.getBounds()
#     content_w = max(1.0, float(x1 - x0))
#     content_h = max(1.0, float(y1 - y0))

#     scale = min(
#         (box_w * inner_pad_ratio) / content_w,
#         (box_h * inner_pad_ratio) / content_h
#     )

#     can.saveState()
#     can.translate(center_x, center_y)
#     can.scale(scale, scale)
#     can.translate(-((x0 + x1) / 2.0), -((y0 + y1) / 2.0))
#     renderPDF.draw(drawing, can, 0, 0)
#     can.restoreState()


# # -----------------------------
# # OVERLAY BUILDER
# # -----------------------------
# def draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, icon_size_pt, inner_pad_ratio, page_w, page_h):
#     name = ("" if pd.isna(name) else str(name)).strip().upper()

#     packet = io.BytesIO()
#     can = canvas.Canvas(packet, pagesize=(page_w, page_h))

#     # Fit name width
#     max_width = page_w - 200
#     font_size = 45.3
#     while can.stringWidth(name, "HammersmithOne", font_size) > max_width and font_size > 10:
#         font_size -= 1.0

#     can.setFont("HammersmithOne", font_size)

#     fill_color = HexColor("#000000")
#     can.setFillColor(fill_color)
#     can.setStrokeColor(fill_color)

#     # Draw participant name
#     char_spacing = 6
#     text_width = can.stringWidth(name, "HammersmithOne", font_size) + max(0, len(name) - 1) * char_spacing
#     x = (page_w - text_width) / 2.0
#     y = 350

#     can.saveState()
#     can.setLineWidth(1.2)

#     txt = can.beginText()
#     txt.setTextOrigin(x, y)
#     txt.setFont("HammersmithOne", font_size)

#     try:
#         txt.setCharSpace(char_spacing)
#     except Exception:
#         pass

#     try:
#         txt.setTextRenderMode(2)
#     except Exception:
#         try:
#             can._code.append("2 Tr")
#         except Exception:
#             pass

#     txt.textLine(name)
#     can.drawText(txt)
#     can.restoreState()

#     # Event SVGs
#     for idx, (svg_path, (svg_cx, svg_cy), transparency) in enumerate(
#         zip(svg_paths, svg_positions, transparency_values)
#     ):
#         try:
#             try:
#                 tval = int(float(transparency)) if (transparency is not None and str(transparency) != "nan") else 0
#             except Exception:
#                 tval = 0

#             draw_svg_centered(
#                 can=can,
#                 svg_path=svg_path,
#                 center_x=svg_cx,
#                 center_y=svg_cy,
#                 box_w=icon_size_pt,
#                 box_h=icon_size_pt,
#                 inner_pad_ratio=inner_pad_ratio,
#                 gray_out=(tval == 0)
#             )

#         except Exception as e:
#             print(
#                 f"[WARN] SVG render failed (row name={name}, idx={idx}, path={svg_path}): {e}",
#                 file=sys.stderr
#             )
#             continue

#     can.save()
#     packet.seek(0)
#     return packet


# # -----------------------------
# # PAGE CREATOR
# # -----------------------------
# def create_certificate_page_bytes(name, transparency_values, svg_paths, svg_positions, icon_size_pt, inner_pad_ratio):
#     overlay_stream = draw_overlay_pdf(
#         name,
#         transparency_values,
#         svg_paths,
#         svg_positions,
#         icon_size_pt,
#         inner_pad_ratio,
#         page_width,
#         page_height
#     )
#     overlay_reader = PdfReader(overlay_stream)

#     base_reader = PdfReader(io.BytesIO(TEMPLATE_BYTES))
#     page = base_reader.pages[0]
#     page.merge_page(overlay_reader.pages[0])

#     out_buf = io.BytesIO()
#     one = PdfWriter()
#     one.add_page(page)
#     one.write(out_buf)
#     out_buf.seek(0)
#     return out_buf.getvalue()


# # -----------------------------
# # MAIN
# # -----------------------------
# def main():
#     if not os.path.exists(EXCEL_FILE):
#         raise FileNotFoundError(f"Excel file not found at: {EXCEL_FILE}")

#     try:
#         df = pd.read_excel(EXCEL_FILE)
#     except Exception as e:
#         raise RuntimeError(
#             f"Failed to read '{EXCEL_FILE}'. If it's .xlsx, ensure 'openpyxl' is installed. Original error: {e}"
#         )

#     # Normalize headers
#     df.columns = [str(c).strip() for c in df.columns]

#     if 'Name' not in df.columns:
#         for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
#             if alt in df.columns:
#                 df.rename(columns={alt: 'Name'}, inplace=True)
#                 break

#     if 'Name' not in df.columns:
#         raise KeyError("Could not find a 'Name' column in the Excel file.")

#     # Clean names
#     df['Name'] = df['Name'].astype(object)
#     df['Name'] = df['Name'].ffill()
#     df['Name'] = df['Name'].apply(lambda x: "" if pd.isna(x) else str(x).strip())

#     # Drop fully blank rows except Name
#     if df.shape[1] > 1:
#         non_name_cols = [c for c in df.columns if c != 'Name']

#         def _row_all_blank(s):
#             return all((pd.isna(v) or str(v).strip() == "") for v in s)

#         mask_keep = ~df[non_name_cols].apply(_row_all_blank, axis=1)
#         df = df[mask_keep].copy()

#     if DEDUP_BY_NAME:
#         before = len(df)
#         df = df.drop_duplicates(subset=['Name']).reset_index(drop=True)
#         after = len(df)
#         print(f"[INFO] Dedup by name: {before} -> {after}")

#     # Detect event columns + SVGs
#     event_cols, svg_paths = find_event_columns_and_svgs(df, SVG_DIR)
#     event_count = len(svg_paths)

#     svg_positions, icon_size_pt, inner_pad_ratio, gap_x, gap_y = get_svg_layout_positions(event_count)
#     active_layout = get_layout_for_event_count(event_count)

#     print("[INFO] Detected event columns:", event_cols)
#     print("[INFO] Matching SVG files:")
#     for col, path in zip(event_cols, svg_paths):
#         print(f"   {col} -> {path}")

#     print(f"[INFO] Total events: {event_count}")
#     print(f"[INFO] Using manual layout for {event_count} event(s):")
#     print(f"       start_x={active_layout['start_x']}, start_y={active_layout['start_y']}")
#     print(f"       icon_size={active_layout['icon_size']}, gap_x={active_layout['gap_x']}, gap_y={active_layout['gap_y']}")
#     print(f"       rows={active_layout['rows']}, per_row={active_layout['per_row']}")
#     print(f"       inner_pad_ratio={active_layout['inner_pad_ratio']}")
#     print(f"       left_pad={active_layout.get('left_pad', 0)}, right_pad={active_layout.get('right_pad', 0)}")

#     # Convert event flags to 0/1
#     def _to_flag(x):
#         if pd.isna(x):
#             return 0
#         s = str(x).strip().lower()
#         if s in {"1", "true", "yes", "y", "✓", "check", "x"}:
#             return 1
#         try:
#             return 1 if int(float(s)) != 0 else 0
#         except Exception:
#             return 0

#     df_events = df[event_cols].copy()
#     for col in event_cols:
#         df_events[col] = df_events[col].map(_to_flag)

#     total = len(df)
#     print(f"[INFO] Preparing {total} certificate(s)...")

#     merger = PdfMerger()
#     rendered_count = 0

#     for idx in range(total):
#         row = df.iloc[idx]
#         safe_name = "" if pd.isna(row['Name']) else str(row['Name']).strip()
#         if not safe_name:
#             safe_name = "PARTICIPANT"

#         flags = df_events.iloc[idx].tolist()

#         try:
#             page_bytes = create_certificate_page_bytes(
#                 safe_name,
#                 flags,
#                 svg_paths,
#                 svg_positions,
#                 icon_size_pt,
#                 inner_pad_ratio
#             )
#             merger.append(io.BytesIO(page_bytes))
#             rendered_count += 1
#             print(f"  - [{idx + 1}/{total}] {safe_name}")
#         except Exception as e:
#             print(f"[ERROR] Failed for row {idx + 1} (name={safe_name}): {e}", file=sys.stderr)
#             continue

#     if rendered_count == 0:
#         merger.close()
#         raise RuntimeError("No pages were rendered. Check your Excel and SVG paths.")

#     with open(OUTPUT_FILE, "wb") as out:
#         merger.write(out)
#     merger.close()

#     print(f"Combined certificates created successfully! Pages written: {rendered_count}/{total} -> {OUTPUT_FILE}")


# if __name__ == "__main__":
#     main()



import io
import os
import sys
from copy import deepcopy

import pandas as pd

# PDF / drawing libs
from reportlab.pdfgen import canvas
from reportlab.pdfbase.ttfonts import TTFont
from reportlab.pdfbase import pdfmetrics
from reportlab.lib.colors import HexColor, Color
from reportlab.graphics import renderPDF
from reportlab.graphics.shapes import Path, String
from svglib.svglib import svg2rlg

# PDF readers/writers
try:
    from pypdf import PdfReader, PdfWriter, PdfMerger
except Exception:
    from PyPDF2 import PdfReader, PdfWriter, PdfMerger


# -----------------------------
# CONFIG / PATHS
# -----------------------------
TEMPLATE_PATH = r'./templates_and_suggested_fonts/Template2/template.pdf'
EXCEL_FILE = 'comp.xlsx'
OUTPUT_FILE = 'newcertis.pdf'
FONT_PATH = r'./Hammersmith_One16/HammersmithOne-Regular.ttf'

SVG_DIR = r'./events'
DEDUP_BY_NAME = False

# -----------------------------
# ICON LAYOUT CONFIG
# -----------------------------
# Dedicated top strip where icons should appear.
# Tune these values only.
ICON_AREA_LEFT = 12
ICON_AREA_RIGHT = 12
ICON_ROW_Y = 736

# Global icon sizing controls
MAX_ICON_SIZE_PT = 42.0
MIN_ICON_SIZE_PT = 24.0

# Gap rules
PREFERRED_MAX_GAP_PT = 22.0
PREFERRED_MIN_GAP_PT = 4.0

# Extra breathing room inside each icon slot
# Lower value -> more empty space around the icon
DENSE_INNER_PAD_RATIO = 0.76
NORMAL_INNER_PAD_RATIO = 0.78
LOOSE_INNER_PAD_RATIO = 0.82

# If True, only draw icons for participated events (flag == 1)
# If False, draw all icons and gray out the inactive ones
DRAW_ONLY_ACTIVE_EVENTS = False


# -----------------------------
# READ TEMPLATE + PAGE SIZE + FONT
# -----------------------------
if not os.path.exists(TEMPLATE_PATH):
    raise FileNotFoundError(f"Template PDF not found at: {TEMPLATE_PATH}")

with open(TEMPLATE_PATH, 'rb') as _tf:
    TEMPLATE_BYTES = _tf.read()

tmp_reader_for_size = PdfReader(io.BytesIO(TEMPLATE_BYTES))
base_page = tmp_reader_for_size.pages[0]
page_width = float(base_page.mediabox.width)
page_height = float(base_page.mediabox.height)

if not os.path.exists(FONT_PATH):
    raise FileNotFoundError(f"Font not found at: {FONT_PATH}")

pdfmetrics.registerFont(TTFont('HammersmithOne', FONT_PATH))


# -----------------------------
# HELPERS
# -----------------------------
def normalize_key(s: str) -> str:
    """
    Normalize Excel headers / filenames so matching is robust.
    Example:
    '333 OH' -> '333oh'
    '333_oh' -> '333oh'
    '333-oh' -> '333oh'
    """
    if s is None:
        return ""
    s = str(s).strip().lower()
    for ch in [" ", "_", "-", "."]:
        s = s.replace(ch, "")
    return s


def build_svg_lookup(svg_dir):
    """
    Scan SVG_DIR and build:
    {
        '333': '/full/path/333.svg',
        '222': '/full/path/222.svg',
        '333oh': '/full/path/333oh.svg',
        ...
    }
    """
    if not os.path.isdir(svg_dir):
        raise FileNotFoundError(f"SVG folder not found: {svg_dir}")

    svg_map = {}
    for root, _, files in os.walk(svg_dir):
        for file in files:
            if file.lower().endswith(".svg"):
                stem = os.path.splitext(file)[0]
                key = normalize_key(stem)
                full_path = os.path.join(root, file)
                svg_map[key] = full_path
    return svg_map


def find_event_columns_and_svgs(df, svg_dir):
    """
    Looks at all columns after 'Name' and keeps only those whose header
    matches an available SVG file.
    """
    cols = [str(c).strip() for c in df.columns]
    df.columns = cols

    if 'Name' not in df.columns:
        for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
            if alt in df.columns:
                df.rename(columns={alt: 'Name'}, inplace=True)
                break

    if 'Name' not in df.columns:
        raise KeyError("Could not find a 'Name' column in the Excel file.")

    name_idx = list(df.columns).index('Name')
    candidate_cols = df.columns[name_idx + 1:]

    svg_lookup = build_svg_lookup(svg_dir)

    event_cols = []
    svg_paths = []
    missing_headers = []

    for col in candidate_cols:
        col_clean = str(col).strip()
        if not col_clean:
            continue

        key = normalize_key(col_clean)

        if key in svg_lookup:
            event_cols.append(col)
            svg_paths.append(svg_lookup[key])
        else:
            missing_headers.append(col)

    if not event_cols:
        raise RuntimeError(
            "No event columns were matched with SVG files.\n"
            f"Check your Excel headers and SVG names in: {svg_dir}"
        )

    if missing_headers:
        print(
            "[INFO] Ignored columns after 'Name' because no matching SVG was found:",
            ", ".join(map(str, missing_headers))
        )

    return event_cols, svg_paths


def choose_inner_pad_ratio(n):
    if n >= 15:
        return DENSE_INNER_PAD_RATIO
    if n >= 10:
        return NORMAL_INNER_PAD_RATIO
    return LOOSE_INNER_PAD_RATIO


def get_svg_layout_positions(n):
    """
    Create one clean centered row across the certificate width.

    Returns:
        positions: [(cx, cy), ...]
        icon_size_pt
        inner_pad_ratio
        gap_x
    """
    if n <= 0:
        return [], MAX_ICON_SIZE_PT, NORMAL_INNER_PAD_RATIO, 0.0

    usable_width = page_width - ICON_AREA_LEFT - ICON_AREA_RIGHT
    inner_pad_ratio = choose_inner_pad_ratio(n)

    if n == 1:
        return [(round(page_width / 2.0, 2), round(ICON_ROW_Y, 2))], MAX_ICON_SIZE_PT, inner_pad_ratio, 0.0

    # Start with max icon size and compute natural gap
    icon_size = MAX_ICON_SIZE_PT
    gap_x = (usable_width - (n * icon_size)) / (n - 1)

    # Clamp gap to preferred range
    if gap_x > PREFERRED_MAX_GAP_PT:
        gap_x = PREFERRED_MAX_GAP_PT
    if gap_x < PREFERRED_MIN_GAP_PT:
        gap_x = PREFERRED_MIN_GAP_PT
        icon_size = (usable_width - ((n - 1) * gap_x)) / n

    # Final safety lower bound
    if icon_size < MIN_ICON_SIZE_PT:
        icon_size = MIN_ICON_SIZE_PT
        gap_x = (usable_width - (n * icon_size)) / (n - 1)
        if gap_x < 0:
            gap_x = 0.0
            icon_size = usable_width / n

    total_width = (n * icon_size) + ((n - 1) * gap_x)
    start_x = (page_width - total_width) / 2.0 + (icon_size / 2.0)

    positions = []
    for i in range(n):
        cx = start_x + i * (icon_size + gap_x)
        positions.append((round(cx, 2), round(ICON_ROW_Y, 2)))

    return positions, float(icon_size), float(inner_pad_ratio), float(gap_x)


# -----------------------------
# COLOR UTILITY
# -----------------------------
def change_color_to_gray(drawing):
    light_gray = Color(0.7294117647, 0.6862745098, 0.6274509804)

    def _recurse(el):
        if hasattr(el, 'fillColor') and getattr(el, 'fillColor'):
            el.fillColor = light_gray
        if hasattr(el, 'strokeColor') and getattr(el, 'strokeColor'):
            el.strokeColor = light_gray
        if isinstance(el, (Path, String)):
            el.fillColor = light_gray
            try:
                el.strokeColor = light_gray
            except Exception:
                pass
        if hasattr(el, 'contents') and el.contents:
            for child in el.contents:
                _recurse(child)

    _recurse(drawing)


# -----------------------------
# SVG DRAWER - FIXED VERSION
# -----------------------------
def draw_svg_centered(can, svg_path, center_x, center_y, box_w, box_h, inner_pad_ratio=0.78, gray_out=False):
    """
    Draw SVG centered in a box with safer normalization.
    This avoids the visual distortion caused by odd SVG bounds/viewBox offsets.
    """
    if not os.path.exists(svg_path):
        return

    drawing = svg2rlg(svg_path)
    if drawing is None:
        return

    drawing = deepcopy(drawing)

    if gray_out:
        change_color_to_gray(drawing)

    x0, y0, x1, y1 = drawing.getBounds()
    content_w = float(x1 - x0)
    content_h = float(y1 - y0)

    if content_w <= 0 or content_h <= 0:
        return

    scale = min(
        (box_w * inner_pad_ratio) / content_w,
        (box_h * inner_pad_ratio) / content_h
    )

    final_w = content_w * scale
    final_h = content_h * scale

    render_x = center_x - (final_w / 2.0)
    render_y = center_y - (final_h / 2.0)

    can.saveState()
    can.translate(render_x, render_y)
    can.scale(scale, scale)
    can.translate(-x0, -y0)
    renderPDF.draw(drawing, can, 0, 0)
    can.restoreState()


# -----------------------------
# OVERLAY BUILDER
# -----------------------------
def draw_overlay_pdf(name, transparency_values, svg_paths, svg_positions, icon_size_pt, inner_pad_ratio, page_w, page_h):
    name = ("" if pd.isna(name) else str(name)).strip().upper()

    packet = io.BytesIO()
    can = canvas.Canvas(packet, pagesize=(page_w, page_h))

    # Fit name width
    max_width = page_w - 200
    font_size = 45.3
    while can.stringWidth(name, "HammersmithOne", font_size) > max_width and font_size > 10:
        font_size -= 1.0

    can.setFont("HammersmithOne", font_size)

    fill_color = HexColor("#000000")
    can.setFillColor(fill_color)
    can.setStrokeColor(fill_color)

    # Draw participant name
    char_spacing = 6
    text_width = can.stringWidth(name, "HammersmithOne", font_size) + max(0, len(name) - 1) * char_spacing
    x = (page_w - text_width) / 2.0
    y = 350

    can.saveState()
    can.setLineWidth(1.2)

    txt = can.beginText()
    txt.setTextOrigin(x, y)
    txt.setFont("HammersmithOne", font_size)

    try:
        txt.setCharSpace(char_spacing)
    except Exception:
        pass

    try:
        txt.setTextRenderMode(2)
    except Exception:
        try:
            can._code.append("2 Tr")
        except Exception:
            pass

    txt.textLine(name)
    can.drawText(txt)
    can.restoreState()

    # Event SVGs
    for idx, (svg_path, (svg_cx, svg_cy), transparency) in enumerate(
        zip(svg_paths, svg_positions, transparency_values)
    ):
        try:
            try:
                tval = int(float(transparency)) if (transparency is not None and str(transparency) != "nan") else 0
            except Exception:
                tval = 0

            if DRAW_ONLY_ACTIVE_EVENTS and tval == 0:
                continue

            draw_svg_centered(
                can=can,
                svg_path=svg_path,
                center_x=svg_cx,
                center_y=svg_cy,
                box_w=icon_size_pt,
                box_h=icon_size_pt,
                inner_pad_ratio=inner_pad_ratio,
                gray_out=(False if DRAW_ONLY_ACTIVE_EVENTS else (tval == 0))
            )

        except Exception as e:
            print(
                f"[WARN] SVG render failed (row name={name}, idx={idx}, path={svg_path}): {e}",
                file=sys.stderr
            )
            continue

    can.save()
    packet.seek(0)
    return packet


# -----------------------------
# PAGE CREATOR
# -----------------------------
def create_certificate_page_bytes(name, transparency_values, svg_paths, svg_positions, icon_size_pt, inner_pad_ratio):
    overlay_stream = draw_overlay_pdf(
        name,
        transparency_values,
        svg_paths,
        svg_positions,
        icon_size_pt,
        inner_pad_ratio,
        page_width,
        page_height
    )
    overlay_reader = PdfReader(overlay_stream)

    base_reader = PdfReader(io.BytesIO(TEMPLATE_BYTES))
    page = base_reader.pages[0]
    page.merge_page(overlay_reader.pages[0])

    out_buf = io.BytesIO()
    one = PdfWriter()
    one.add_page(page)
    one.write(out_buf)
    out_buf.seek(0)
    return out_buf.getvalue()


# -----------------------------
# MAIN
# -----------------------------
def main():
    if not os.path.exists(EXCEL_FILE):
        raise FileNotFoundError(f"Excel file not found at: {EXCEL_FILE}")

    try:
        df = pd.read_excel(EXCEL_FILE)
    except Exception as e:
        raise RuntimeError(
            f"Failed to read '{EXCEL_FILE}'. If it's .xlsx, ensure 'openpyxl' is installed. Original error: {e}"
        )

    # Normalize headers
    df.columns = [str(c).strip() for c in df.columns]

    if 'Name' not in df.columns:
        for alt in ['name', 'Full Name', 'Participant', 'Participant Name']:
            if alt in df.columns:
                df.rename(columns={alt: 'Name'}, inplace=True)
                break

    if 'Name' not in df.columns:
        raise KeyError("Could not find a 'Name' column in the Excel file.")

    # Clean names
    df['Name'] = df['Name'].astype(object)
    df['Name'] = df['Name'].ffill()
    df['Name'] = df['Name'].apply(lambda x: "" if pd.isna(x) else str(x).strip())

    # Drop fully blank rows except Name
    if df.shape[1] > 1:
        non_name_cols = [c for c in df.columns if c != 'Name']

        def _row_all_blank(s):
            return all((pd.isna(v) or str(v).strip() == "") for v in s)

        mask_keep = ~df[non_name_cols].apply(_row_all_blank, axis=1)
        df = df[mask_keep].copy()

    if DEDUP_BY_NAME:
        before = len(df)
        df = df.drop_duplicates(subset=['Name']).reset_index(drop=True)
        after = len(df)
        print(f"[INFO] Dedup by name: {before} -> {after}")

    # Detect event columns + SVGs
    event_cols, svg_paths = find_event_columns_and_svgs(df, SVG_DIR)
    event_count = len(svg_paths)

    svg_positions, icon_size_pt, inner_pad_ratio, gap_x = get_svg_layout_positions(event_count)

    print("[INFO] Detected event columns:", event_cols)
    print("[INFO] Matching SVG files:")
    for col, path in zip(event_cols, svg_paths):
        print(f"   {col} -> {path}")

    print(f"[INFO] Total events: {event_count}")
    print(f"[INFO] Page width: {round(page_width, 2)}")
    print(f"[INFO] Icon area: left={ICON_AREA_LEFT}, right={ICON_AREA_RIGHT}, row_y={ICON_ROW_Y}")
    print(f"[INFO] Computed icon size: {round(icon_size_pt, 2)} pt")
    print(f"[INFO] Computed gap_x: {round(gap_x, 2)} pt")
    print(f"[INFO] Computed inner_pad_ratio: {inner_pad_ratio}")

    # Convert event flags to 0/1
    def _to_flag(x):
        if pd.isna(x):
            return 0
        s = str(x).strip().lower()
        if s in {"1", "true", "yes", "y", "✓", "check", "x"}:
            return 1
        try:
            return 1 if int(float(s)) != 0 else 0
        except Exception:
            return 0

    df_events = df[event_cols].copy()
    for col in event_cols:
        df_events[col] = df_events[col].map(_to_flag)

    total = len(df)
    print(f"[INFO] Preparing {total} certificate(s)...")

    merger = PdfMerger()
    rendered_count = 0

    for idx in range(total):
        row = df.iloc[idx]
        safe_name = "" if pd.isna(row['Name']) else str(row['Name']).strip()
        if not safe_name:
            safe_name = "PARTICIPANT"

        flags = df_events.iloc[idx].tolist()

        try:
            page_bytes = create_certificate_page_bytes(
                safe_name,
                flags,
                svg_paths,
                svg_positions,
                icon_size_pt,
                inner_pad_ratio
            )
            merger.append(io.BytesIO(page_bytes))
            rendered_count += 1
            print(f"  - [{idx + 1}/{total}] {safe_name}")
        except Exception as e:
            print(f"[ERROR] Failed for row {idx + 1} (name={safe_name}): {e}", file=sys.stderr)
            continue

    if rendered_count == 0:
        merger.close()
        raise RuntimeError("No pages were rendered. Check your Excel and SVG paths.")

    with open(OUTPUT_FILE, "wb") as out:
        merger.write(out)
    merger.close()

    print(f"Combined certificates created successfully! Pages written: {rendered_count}/{total} -> {OUTPUT_FILE}")


if __name__ == "__main__":
    main()